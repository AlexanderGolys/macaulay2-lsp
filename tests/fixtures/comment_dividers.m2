----------------------------------------------------------- -
-- primitive
----------------------------------------------------------- -
primitive = (L) -> (
    n := #L - 1;
    apply(L, i -> i)
);

----------------------------------------------------------- -
-- toZZ
----------------------------------------------------------- -
toZZ = (L) -> (
    d := apply(L, e -> denominator e);
    apply(L, e -> numerator e)
);
