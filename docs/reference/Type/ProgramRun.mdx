---
title: "ProgramRun"
kind: Type
---

# ProgramRun

**ProgramRun  -- result of running an external program**

## Description

A hash table returned by [runProgram](../Method/op_Coredollar_runProgram.mdx) with the following strings as keys:
- `"command"` the command that was used to run the program.
- `"output"` the output of the program to `stdout`.
- `"error"` the output of the program to `stderr`.
- `"return value"` the return value of the program possibly multiplied by 256 (see [run](../Function/run.mdx) ). Note that this is what is displayed when printing a `ProgramRun` object.

In addition if [runProgram](../Method/op_Coredollar_runProgram.mdx) is called with the `KeepFiles` option set to [true](../Instance/op_Coredollar_true.mdx) then the following keys will be present:
- `"output file"` the path to a file containing the output of the program to `stdout`.
- `"error file"` the path to a file containing the output of the program to `stderr`.



## Additional Information

### See also


- Macaulay2Doc:: runProgram
- Macaulay2Doc:: Program << Thing
- Macaulay2Doc:: Program

[status(ProgramRun)](../Instance/status(ProgramRun).mdx)

## Debug info

```python
{
    "obj_type": "Type",
    "name": "ProgramRun",
    "safe_name": "op_Coredollar_ProgramRun",
    "type": null,
    "description": "A hash table returned by [runProgram](../Method/op_Coredollar_runProgram.mdx) with the following str...",
    "parent": null,
    "subtypes": [],
    "extra": {
        "isSymbol": false,
        "codeName": "Core$ProgramRun",
        "parent": "HashTable",
        "methods": [
            [
                "net",
                "ProgramRun"
            ],
            [
                "status",
                "ProgramRun"
            ],
            [
                "toString",
                "ProgramRun"
            ]
        ],
        "instances": [],
        "full_doc": "... (1850 bytes) ...",
        "parsed_headline": "ProgramRun  -- result of running an external program"
    }
}
```
