---
title: "Iterator"
kind: Type
---

# Iterator

**Iterator  -- class for iterators**

## Description

This is a class designed to simplify writing [iterator](../Method/iterator.mdx) methods. Each instance is a nullary [FunctionClosure](../Type/op_Coredollar_FunctionClosure.mdx) that serves as the [next](../Method/op_Coredollar_next.mdx) method for the iterator.Each call of `next iter` is equivalent to `iter()`.Every `Iterator` object is an iterator for itself.However we cannot "rewind" an `Iterator` object.  Every time that it is iterated upon using [for](../Method/op_Coredollar_for.mdx)[scan](../Function/op_Coredollar_scan.mdx) etc. iteration will resume where it left off previously.Contrast this with most other classes with the [iterator](../Method/iterator.mdx) method installed like strings for which a new `Iterator` object is created every time it is iterated upon and so iteration starts over from the beginning

## Examples

```macaulay2
primes = Iterator (
         p := 2;
         () -> (
             r := p;
             p = nextPrime(p + 1);
             r));
```

## Additional Information

- Macaulay2Doc:: iterator
- Macaulay2Doc:: next
- Macaulay2Doc:: StopIteration

## Debug info

```python
{
    "obj_type": "Type",
    "name": "Iterator",
    "safe_name": "Iterator",
    "type": null,
    "description": "This is a class designed to simplify writing [iterator](../Method/iterator.mdx) methods. Each instan...",
    "parent": null,
    "subtypes": [],
    "extra": {
        "isSymbol": false,
        "codeName": "Iterator",
        "parent": "FunctionClosure",
        "methods": [
            [
                "next",
                "Iterator"
            ],
            [
                "net",
                "Iterator"
            ],
            [
                "|",
                "Iterator",
                "Iterator"
            ],
            [
                "iterator",
                "Iterator"
            ]
        ],
        "instances": [],
        "full_doc": "... (4310 bytes) ...",
        "parsed_headline": "Iterator  -- class for iterators"
    }
}
```
