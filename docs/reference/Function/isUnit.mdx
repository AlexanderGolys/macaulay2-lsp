---
title: "isUnit"
kind: Function
---

# isUnit

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

isUnit, -- whether a ring element is a unit, class => element, Usage: , isUnit r, class => language-macaulay2, Inputs:, r, , , a , Macaulay2Doc :: RingElement, ring element, Outputs:, a , Macaulay2Doc :: Boolean, Boolean value, , , Macaulay2Doc :: true, if the , Macaulay2Doc :: RingElement, ring element, , r, generates an ideal containing the multiplicative identity , 1, and , false, otherwise, Description, class => examples, i1 : R = QQ[z]/(z^2+1);, class => language-macaulay2, i2 : isUnit z

o2 = true, class => language-macaulay2, i3 : S = QQ[x,y]/(1-(x-1)*(y-1));, class => language-macaulay2, i4 : isUnit (x^2 - 2*x + 1)

o4 = true, class => language-macaulay2, i5 : isUnit x

o5 = false, class => language-macaulay2, class => waystouse, Ways to use , isUnit, :, isUnit(Number), isUnit(RingElement), isUnit(ZZ), class => waystouse, For the programmer, The object , Macaulay2Doc :: isUnit, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/isUnit-doc.m2:25:0, .

## Installations

Type signatures for this function:

- `isUnit(RingElement)`
- `isUnit(ZZ)`
- `isUnit(Number)`

## Examples

```macaulay2
i1 : R = QQ[z]/(z^2+1);

i2 : isUnit z

o2 = true
```

## See Also

