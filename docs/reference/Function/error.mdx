---
title: "error"
kind: Function
---

# error

**Type:** `Function`

**Instance of:** [FunctionClosure](../Type/FunctionClosure.mdx)

## Description

error, -- deliver error message, class => element, Usage: , error s, class => language-macaulay2, Inputs:, s, , , a , Macaulay2Doc :: String, string, , , a string or a sequence of things that can be converted to strings, Consequences:, an error message is displayed, Description, error s, causes an error message s to be displayed., , The error message , s, (which should be a string or a sequence of things that can be converted to strings and concatenated) is printed. Execution of the code is interrupted, and control is returned to top level. Here is an explicit example showing how this command is used:, exampleCode = p -> (
 if not isPrime p
 then error "expected a prime integer";
 if p == 2
 then error "expected an odd prime";
 lift((p+1)/2, ZZ)
 );, class => language-macaulay2, Eventually we will have a means of ensuring that the line number printed out with the error message will have more significance, but currently it is the location in the code of the error expression itself., If the error message begins with a hyphen, then the word "error" will not be inserted into the error message., class => waystouse, For the programmer, The object , Macaulay2Doc :: error, is , a , Macaulay2Doc :: FunctionClosure, function closure, ., , class => waystouse, The source of this document is in , Macaulay2Doc/ov_debugging.m2:329:0, .

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

