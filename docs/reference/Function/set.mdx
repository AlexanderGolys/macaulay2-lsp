---
title: "set"
kind: Function
---

# set

**Type:** `Function`

**Instance of:** [MethodFunctionSingle](../Type/MethodFunctionSingle.mdx)

## Description

set, -- make a set, class => element, Usage: , set v, class => language-macaulay2, set(v1,v2,...), class => language-macaulay2, Inputs:, v, , , a , Macaulay2Doc :: VisibleList, visible list, or , a , Macaulay2Doc :: HashTable, hash table, Outputs:, a , Macaulay2Doc :: Set, set, , , the set whose elements are the members of the list v or keys of the hash table v, Description, class => examples, i1 : v = {1,2,3,2,1

o1 = {1, 2, 3, 2, 1

o1 : List, class => language-macaulay2, i2 : S = set v

o2 = set {1, 2, 3

o2 : Set, class => language-macaulay2, i3 : T = set(a,b,c,a,b,d)

o3 = set {c, d, a, b

o3 : Set, class => language-macaulay2, See also, Macaulay2Doc :: Set, class => waystouse, Ways to use , set, :, set(HashTable), set(Set), set(VisibleList), class => waystouse, For the programmer, The object , Macaulay2Doc :: set, is , a , Macaulay2Doc :: MethodFunctionSingle, method function with a single argument, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/set-doc.m2:20:0, .

## Installations

Type signatures for this function:

- `set(Set)`
- `set(HashTable)`
- `set(VisibleList)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

