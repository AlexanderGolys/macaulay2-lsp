---
title: "submatrix"
kind: Function
---

# submatrix

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

submatrix, -- select part of a matrix, class => element, Usage: , submatrix(f, rows, cols), class => language-macaulay2, submatrix(f,,cols), class => language-macaulay2, submatrix(f,rows,), class => language-macaulay2, Inputs:, f, , , a map between free modules, rows, , , a list of integers denoting the choice of rows. If omitted, use all rows, cols, , , a list of integers denoting the choice of columns. If omitted, use all columns, Outputs:, a , Macaulay2Doc :: Matrix, matrix, , , the submatrix of , f, corresponding to the lists , rows, and , cols, , Description, Yields an r by c matrix, where r is the length of the list of integers , rows, , and c is the length of the list of integers , cols, . The , (i,j), -th entry of the result is , f_(rows_i, cols_j), ., , Each list of integers may contain ranges and repetitions, as in , {3, 5..7, 3:0, , If , rows, or , cols, is omitted, all the corresponding indices are used., , Both , rows, and/or , cols, may contain duplicate values, in which case the result will contain duplicate rows and/or columns., class => examples, i1 : R = ZZ/101[a .. o];, class => language-macaulay2, i2 : f = genericMatrix(R, a, 3, 5)

o2 = | a d g j m |
 | b e h k n |
 | c f i l o |

 3 5
o2 : Matrix R <-- R, class => language-macaulay2, i3 : submatrix(f, {1,2,0, {0..2, 4)

o3 = | b e h n |
 | c f i o |
 | a d g m |

 3 4
o3 : Matrix R <-- R, class => language-macaulay2, , class => examples, i4 : submatrix(f, {1,2, )

o4 = | b e h k n |
 | c f i l o |

 2 5
o4 : Matrix R <-- R, class => language-macaulay2, submatrix(f,,cols), can be written as , submatrix(f,cols), , or , f_cols, ., class => examples, i5 : submatrix(f,,{0,1)

o5 = | a d |
 | b e |
 | c f |

 3 2
o5 : Matrix R <-- R, class => language-macaulay2, i6 : f_{0,1

o6 = | a d |
 | b e |
 | c f |

 3 2
o6 : Matrix R <-- R, class => language-macaulay2, See also, Macaulay2Doc :: submatrix', Macaulay2Doc :: Matrix _ List, Macaulay2Doc :: Matrix ^ List, Macaulay2Doc :: submatrix(Matrix,VisibleList), Macaulay2Doc :: Matrix _ Array, Macaulay2Doc :: Matrix ^ Array, class => waystouse, Ways to use , submatrix, :, submatrix(Matrix,VisibleList,VisibleList), submatrix(MutableMatrix,VisibleList,VisibleList), Macaulay2Doc :: submatrix(Matrix,VisibleList), submatrix(MutableMatrix,VisibleList), -- see , Macaulay2Doc :: submatrix(Matrix,VisibleList), class => waystouse, For the programmer, The object , Macaulay2Doc :: submatrix, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/submatrix-doc.m2:113:0, .

## Installations

Type signatures for this function:

- `submatrix(Matrix, VisibleList, VisibleList)`
- `submatrix(Matrix, VisibleList, Nothing)`
- `submatrix(Matrix, VisibleList)`
- `submatrix(Matrix, Nothing, VisibleList)`
- `submatrix(Matrix, Nothing, Nothing)`
- `submatrix(MutableMatrix, VisibleList, VisibleList)`
- `submatrix(MutableMatrix, VisibleList)`
- `submatrix(MutableMatrix, Nothing, VisibleList)`
- `submatrix(MutableMatrix, VisibleList, Nothing)`
- `submatrix(MutableMatrix, Nothing, Nothing)`

## Examples

```macaulay2
i1 : R = ZZ/101[a .. o];

i2 : f = genericMatrix(R, a, 3, 5)

o2 = | a d g j m |
     | b e h k n |
     | c f i l o |

             3      5
o2 : Matrix R  <-- R
```

## See Also

