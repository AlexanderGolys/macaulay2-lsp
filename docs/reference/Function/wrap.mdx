---
title: "wrap"
kind: Function
---

# wrap

**wrap**

**Type:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

 The inputs `wid`  and `sep`  are optional and can be given in either order.  The default for `wid`  is `printWidth`  and the default for `sep`  is null.

## Examples

```macaulay2
wrap(widseps)
```

```macaulay2
wrap(10"abcdefghijklmnopqrstuvwxyz")

```

```text
abcdefghij
     klmnopqrst
     uvwxyz
```

```macaulay2
wrap(10"-""abcdefghijklmnopqrstuvwxyz")

```

```text
abcdefghij
     ----------
     klmnopqrst
     ----------
     uvwxyz
```

## Additional Information

- Inputs:
- `wid` an [ integer](../Type/ZZ.mdx)
- `sep` a [ string](../Type/op_Coredollar_String.mdx)
- `s` a [ string](../Type/op_Coredollar_String.mdx)
- Outputs:
- a string obtained by wrapping the string `s` in case it is wider than the number `wid` so that it occupies multiple lines separated by lines filled with the single character in the string `sep` if provided

## Debug info

```python
{
    "obj_type": "Function",
    "name": "wrap",
    "safe_name": "op_Coredollar_wrap",
    "type": "CompiledFunction",
    "description": " The inputs `wid`  and `sep`  are optional and can be given in either order.  The default for `wid` ...",
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$wrap",
        "full_doc": "... (1445 bytes) ...",
        "parsed_headline": "wrap"
    }
}
```
