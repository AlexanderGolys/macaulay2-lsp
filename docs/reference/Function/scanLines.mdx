---
title: "scanLines"
kind: Function
---

# scanLines

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

scanLines, -- apply a function to each line of a file, class => element, Usage: , scanLines(f,fn), class => language-macaulay2, Inputs:, f, fn, , , the name of a file, or a list of names of files, Outputs:, returns , Macaulay2Doc :: null, unless the function uses , break x, with a non-null value for , x, , in which case scanning stops and , x, is returned immediately, Consequences:, applies , f, to each line of the file(s), Description, The file is read and processed one block at a time, making this procedure potentially much better at conserving memory than , scan(lines get fn,f), when the file is very large., class => waystouse, Ways to use , scanLines, :, scanLines(Function,List), scanLines(Function,String), class => waystouse, For the programmer, The object , Macaulay2Doc :: scanLines, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/ov_system.m2:359:0, .

## Installations

Type signatures for this function:

- `scanLines(Function, String)`
- `scanLines(Function, List)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

