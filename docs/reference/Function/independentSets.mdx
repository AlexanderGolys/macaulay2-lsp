---
title: "independentSets"
kind: Function
---

# independentSets

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

independentSets, -- some size-maximal independent subsets of variables modulo an ideal, class => element, Usage: , independentSets J, class => language-macaulay2, Inputs:, J, , , an , Macaulay2Doc :: Ideal, ideal, , or , a , Macaulay2Doc :: MonomialIdeal, monomial ideal, Macaulay2Doc :: using functions with optional inputs, Optional inputs, :, Limit, => , an , Macaulay2Doc :: ZZ, integer, , , default value , infinity, , , the maximum number of independent sets to be found, Outputs:, a , Macaulay2Doc :: List, list, , , of products of variables. The support of any one of these products is a maximal independent subset of variables modulo , J, Description, An independent set of variables of an ideal , J, in a polynomial ring , R, is a set of variables that are algebraically independent modulo , J, (i.e. there is no polynomial in , J, involving only these sets of variables., , If the Krull dimension of , R/J, is , d, , then a maximal independent set is an independent set having size , d, ., , class => examples, i1 : R = QQ[a..h];, class => language-macaulay2, i2 : I = minors(2,genericMatrix(R,a,2,4))

o2 = ideal (- b*c + a*d, - b*e + a*f, - d*e + c*f, - b*g + a*h, - d*g + c*h,
 ------------------------------------------------------------------------
 - f*g + e*h)

o2 : Ideal of R, class => language-macaulay2, i3 : inI = ideal leadTerm I

o3 = ideal (f*g, d*g, b*g, d*e, b*e, b*c)

o3 : Ideal of R, class => language-macaulay2, i4 : independentSets I

o4 = {a*b*d*f*h, a*c*d*f*h, a*c*e*f*h, a*c*e*g*h

o4 : List, class => language-macaulay2, i5 : independentSets inI

o5 = {a*b*d*f*h, a*c*d*f*h, a*c*e*f*h, a*c*e*g*h

o5 : List, class => language-macaulay2, , The independent sets returned correspond one for one with the minimal primes of smallest codimension of the ideal of lead terms of J., class => examples, i6 : I = ideal"abc,bcd,cde,adf,cgh,b3f,a3g"

 3 3
o6 = ideal (a*b*c, b*c*d, c*d*e, a*d*f, c*g*h, b f, a g)

o6 : Ideal of R, class => language-macaulay2, i7 : minimalPrimes I

o7 = {ideal (c, b, a), ideal (f, c, a), ideal (g, d, b), ideal (g, e, b, a),
 ------------------------------------------------------------------------
 ideal (g, f, c), ideal (g, f, d, a), ideal (g, f, e, b), ideal (h, d, b,
 ------------------------------------------------------------------------
 a), ideal (h, e, b, a), ideal (h, f, d, a)

o7 : List, class => language-macaulay2, i8 : independentSets I

o8 = {a*b*d*e*h, a*c*e*f*h, b*d*e*g*h, d*e*f*g*h

o8 : List, class => language-macaulay2, The optional Limit argument is useful if you need only one, or several such independent sets., class => examples, i9 : L = independentSets(I, Limit=>1)

o9 = {a*b*d*e*h

o9 : List, class => language-macaulay2, , Often, you want the list of the variables in a maximal independent set, or the list of those not in the set., class => examples, i10 : support L_0

o10 = {a, b, d, e, h

o10 : List, class => language-macaulay2, i11 : rsort toList(set gens R - set support L_0)

o11 = {c, f, g

o11 : List, class => language-macaulay2, , This function is useful as a subroutine to primary decomposition algorithms., See also, PrimaryDecomposition :: PrimaryDecomposition, MinimalPrimes :: minimalPrimes, Macaulay2Doc :: support, Macaulay2Doc :: rsort, class => waystouse, Ways to use , independentSets, :, independentSets(Ideal), independentSets(MonomialIdeal), class => waystouse, For the programmer, The object , Macaulay2Doc :: independentSets, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/independentSets-doc.m2:56:0, .

## Installations

Type signatures for this function:

- `independentSets(Ideal)`
- `independentSets(MonomialIdeal)`

## Examples

```macaulay2
i1 : R = QQ[a..h];

i2 : I = minors(2,genericMatrix(R,a,2,4))

o2 = ideal (- b*c + a*d, - b*e + a*f, - d*e + c*f, - b*g + a*h, - d*g + c*h,
     ------------------------------------------------------------------------
     - f*g + e*h)

o2 : Ideal of R
```

## See Also

