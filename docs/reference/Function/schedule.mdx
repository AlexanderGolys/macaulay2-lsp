---
title: "schedule"
kind: Function
---

# schedule

**Type:** `Function`

**Instance of:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

schedule, -- schedule a task for execution, class => element, Usage: , schedule(f,x), class => language-macaulay2, schedule f, class => language-macaulay2, schedule t, class => language-macaulay2, Inputs:, f, , , a , Macaulay2Doc :: Function, function, , , x, , , a , Macaulay2Doc :: Thing, thing, , , t, , , a , Macaulay2Doc :: Task, task, , , Outputs:, a new task computing the value of the , f(x), , or, if , x, is not provided, then of , f(), . Alternatively, the task , t, , created previously with , Macaulay2Doc :: createTask, , is scheduled for execution., Description, The computation proceeds in the background, in a thread. The status of the task can be observed by printing , t, . When the computation is finished, as can be detected with , Macaulay2Doc :: isReady(Task), , the final value can be retrieved with , Macaulay2Doc :: taskResult(Task), ., If , Macaulay2Doc :: notify, is set to , Macaulay2Doc :: true, , then useful messages are printed when the task changes state., class => examples, i1 : f = x -> 2^x

o1 = f

o1 : FunctionClosure, class => language-macaulay2, i2 : t = createTask(f,3)

o2 = <<task, created>>

o2 : Task, class => language-macaulay2, i3 : schedule t

o3 = <<task, created>>

o3 : Task, class => language-macaulay2, i4 : taskResult t

o4 = 8, class => language-macaulay2, i5 : u = schedule(f,4)

o5 = <<task, created>>

o5 : Task, class => language-macaulay2, i6 : taskResult u

o6 = 16, class => language-macaulay2, class => waystouse, Ways to use , schedule, :, schedule(Function), schedule(Function,Thing), schedule(Task), class => waystouse, For the programmer, The object , Macaulay2Doc :: schedule, is , a , Macaulay2Doc :: CompiledFunction, compiled function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/ov_threads.m2:498:0, .

## Installations

Type signatures for this function:

- `schedule(Function)`
- `schedule(Task)`
- `schedule(Function, Thing)`

## Examples

```macaulay2
i1 : f = x -> 2^x

o1 = f

o1 : FunctionClosure

i2 : t = createTask(f,3)

o2 = <<task, created>>

o2 : Task
```

## See Also

