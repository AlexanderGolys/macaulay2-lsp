---
title: "globalAssign"
kind: Function
---

# globalAssign

**globalAssign  -- global assignment in function form**

**Type:** [FunctionClosure](../Type/FunctionClosure.mdx)

## Description

This function mimics what happens in the interpreter when an assignment to a global variable occurs and can be useful if the name of the symbol is not known when the code is written.  If the value changes then [GlobalReleaseHook](../Instance/GlobalReleaseHook.mdx)  and [GlobalAssignHook](../Method/op_Coredollar_GlobalAssignHook.mdx)  are called appropriately.

## Examples

```macaulay2
globalAssign(sv)
```

```macaulay2
globalAssign(x4)

```

```text
4
```

## Additional Information

- Inputs:
- `s` a [ symbol](../Type/op_Coredollar_Symbol.mdx) the symbol whose value is to be set
- `v` a [ thing](../Type/Thing.mdx) the value to be assigned to `s`
- Outputs:
- `v` a [ thing](../Type/Thing.mdx)

## Debug info

```python
{
    "obj_type": "Function",
    "name": "globalAssign",
    "safe_name": "op_Coredollar_globalAssign",
    "type": "FunctionClosure",
    "description": "This function mimics what happens in the interpreter when an assignment to a global variable occurs ...",
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$globalAssign",
        "full_doc": "... (1470 bytes) ...",
        "parsed_headline": "globalAssign  -- global assignment in function form"
    }
}
```
