---
title: "assert"
kind: Function
---

# assert

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

assert, -- check whether something is true, raise an error if not, class => element, Usage: , assert x, class => language-macaulay2, Inputs:, x, , , a , Macaulay2Doc :: Thing, thing, , , Description, assert x, raises an error if , x, isn't true., class => examples, i1 : stopIfError = false;, class => language-macaulay2, i2 : assert( (2+2) === 4), class => language-macaulay2, i3 : assert(rank matrix {{1, 2, {2, 4 == 2)
stdio:3:6:(3): error: assertion failed, class => language-macaulay2, If , x, is an , Macaulay2Doc :: Expression, that evaluates to false, then a partially evaluated form is printed with the error message to assist in debugging., class => examples, i4 : assert Equation(rank matrix {{1, 2, {2, 4, 2)
stdio:4:6:(3): error: assertion failed:
1 == 2 is false, class => language-macaulay2, See also, Macaulay2Doc :: generateAssertions, Macaulay2Doc :: stopIfError, class => waystouse, Ways to use , assert, :, assert(Expression), assert(Thing), class => waystouse, For the programmer, The object , Macaulay2Doc :: assert, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/ov_debugging.m2:471:0, .

## Installations

Type signatures for this function:

- `assert(Expression)`
- `assert(Thing)`

## Examples

```macaulay2
i1 : stopIfError = false;

i2 : assert( (2+2) === 4)
```

## See Also

