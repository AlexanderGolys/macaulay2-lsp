---
title: "addHook"
kind: Function
---

# addHook

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

addHook, -- add a hook function to an object for later processing, class => element, Usage: , addHook(key, hook), class => language-macaulay2, addHook(store, key, hook), class => language-macaulay2, Inputs:, key, , , a , Macaulay2Doc :: Sequence, sequence, or , a , Macaulay2Doc :: Symbol, symbol, , , hook, , , a , Macaulay2Doc :: Function, function, , , store, , , a , Macaulay2Doc :: MutableHashTable, mutable hash table, , , Macaulay2Doc :: using functions with optional inputs, Optional inputs, :, Strategy, => , a , Macaulay2Doc :: Thing, thing, , , default value , null, , , specifies the name for the hook, Consequences:, the function , hook, is added to the (possibly absent) hash table of hooks, which is either stored in the mutable hash table , store, or under the , Macaulay2Doc :: youngest, , Macaulay2Doc :: Type, , T, listed in the , Macaulay2Doc :: method, key , key, . In the latter case, the hash table is either stored in , T.Hooks#key, if , T, is mutable, or in , T.cache.Hooks#key, otherwise. If no appropriate object is found, or if , key, is a , Macaulay2Doc :: Symbol, , then the hook is stored under the hash table , GlobalHookStore, Description, For an explanation and examples of hooks see , Macaulay2Doc :: using hooks, ., class => waystouse, Code, ../../m2/methods.m2:626:46-636:36: --source code:
addHook(MutableHashTable, Thing, Function) := opts -> (store, key, hook) -> (
 -- this is the hashtable of Hooks for a specific key, which stores HookAlgorithms and HookPriority
 store#key ??= new MutableHashTable from {
 HookAlgorithms => new MutableHashTable, -- a mutable hash table "strategy key" => "strategy code"
 HookPriority => new MutableList; -- a mutable list of strategy keys, in order
 store = store#key;
 ind := #store.HookPriority; -- index to add the hook in the list; TODO: use Priority to insert in the middle?
 alg := if opts.Strategy =!= null then opts.Strategy else ind;
 if not store.HookAlgorithms#?alg then
 store.HookPriority#ind = alg;
 store.HookAlgorithms#alg = hook), class => language-macaulay2, See also, Macaulay2Doc :: hooks, Macaulay2Doc :: runHooks, class => waystouse, Ways to use , addHook, :, addHook(MutableHashTable,Thing,Function), addHook(Sequence,Function), addHook(Symbol,Function), class => waystouse, For the programmer, The object , Macaulay2Doc :: addHook, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/hooks-doc.m2:181:0, .

## Installations

Type signatures for this function:

- `addHook(Symbol, Function)`
- `addHook(Sequence, Function)`
- `addHook(MutableHashTable, Thing, Function)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

