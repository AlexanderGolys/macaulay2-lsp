---
title: "precision"
kind: Function
---

# precision

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

precision, , class => element, Usage: , precision x, class => language-macaulay2, Inputs:, x, , , a , Macaulay2Doc :: Ring, ring, , , a , Macaulay2Doc :: Matrix, matrix, , , a , Macaulay2Doc :: RingElement, ring element, , or , a , Macaulay2Doc :: Number, number, Outputs:, an , Macaulay2Doc :: ZZ, integer, , , the precision to which , x, or its instances are stored, Description, class => examples, i1 : precision 3p111

o1 = 111, class => language-macaulay2, i2 : precision (RR[x])

o2 = 53, class => language-macaulay2, i3 : precision 3

o3 = infinity

o3 : InfiniteNumber, class => language-macaulay2, class => waystouse, Ways to use , precision, :, precision(FractionField), precision(GaloisField), precision(InexactField), precision(InexactNumber), precision(Matrix), precision(MutableMatrix), precision(Number), precision(PolynomialRing), precision(QuotientRing), precision(Ring), precision(RingElement), class => waystouse, For the programmer, The object , Macaulay2Doc :: precision, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/doc3.m2:103:0, .

## Installations

Type signatures for this function:

- `precision(PolynomialRing)`
- `precision(RingElement)`
- `precision(Matrix)`
- `precision(QuotientRing)`
- `precision(MutableMatrix)`
- `precision(FractionField)`
- `precision(GaloisField)`
- `precision(Number)`
- `precision(Ring)`
- `precision(InexactField)`
- `precision(InexactNumber)`

## Examples

```macaulay2
i1 : precision 3p111

o1 = 111

i2 : precision (RR[x])

o2 = 53
```

## See Also

