---
title: "forceGB"
kind: Function
---

# forceGB

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

forceGB, -- declare that the columns of a matrix are a Gröbner basis, class => element, Usage: , forceGB f, class => language-macaulay2, Inputs:, f, , , a , Macaulay2Doc :: Matrix, matrix, Macaulay2Doc :: using functions with optional inputs, Optional inputs, :, Macaulay2Doc :: forceGB(...,ChangeMatrix=>...), ChangeMatrix, => , ..., , , default value , null, , , specify the change of basis matrix from a Gröbner basis to the generators of a module, Macaulay2Doc :: forceGB(...,MinimalMatrix=>...), MinimalMatrix, => , ..., , , default value , null, , , specify the minimal generator matrix, Macaulay2Doc :: forceGB(...,SyzygyMatrix=>...), SyzygyMatrix, => , ..., , , default value , null, , , specify the syzygy matrix, Outputs:, a , Macaulay2Doc :: GroebnerBasis, Gröbner basis, Description, Declares that the columns of the matrix , f, constitute a Gröbner basis, autoreduces it, minimizes it, sorts it, and returns a Gröbner basis object declaring itself complete, without computing any S-pairs., , Sometimes one knows that a set of polynomials (or columns of such) form a Gröbner basis, but , Macaulay2, doesn't. This is the way to inform the system of this fact., class => examples, i1 : gbTrace = 3;, class => language-macaulay2, i2 : R = ZZ[x,y,z];, class => language-macaulay2, i3 : f = matrix{{x^2-3, y^3-1, z^4-2;

 1 3
o3 : Matrix R <-- R, class => language-macaulay2, i4 : g = forceGB f

o4 = GroebnerBasis[status: done; S-pairs encountered up to degree 0]

o4 : GroebnerBasis, class => language-macaulay2, This Gröbner basis object is stored with the matrix and can be obtained as usual:, class => examples, i5 : g === gb(f, StopBeforeComputation=>true)

o5 = true, class => language-macaulay2, Requesting a Gröbner basis for , f, requires no computation., class => examples, i6 : gens gb f

o6 = | x2-3 y3-1 z4-2 |

 1 3
o6 : Matrix R <-- R, class => language-macaulay2, If an autoreduced Gröbner basis is desired, replace , f, by , gens forceGB f, first., Caveat, If the columns do not form a Gröbner basis, nonsensical answers may result, See also, Macaulay2Doc :: Gröbner bases, Macaulay2Doc :: forceGB(...,ChangeMatrix=>...), Macaulay2Doc :: forceGB(...,MinimalMatrix=>...), Macaulay2Doc :: forceGB(...,SyzygyMatrix=>...), class => waystouse, Ways to use , forceGB, :, forceGB(Matrix), class => waystouse, For the programmer, The object , Macaulay2Doc :: forceGB, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/forceGB-doc.m2:46:0, .

## Installations

Type signatures for this function:

- `forceGB(Matrix)`

## Examples

```macaulay2
i1 : gbTrace = 3;

i2 : R = ZZ[x,y,z];
```

## See Also

