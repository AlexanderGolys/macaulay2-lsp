---
title: "coimage"
kind: Function
---

# coimage

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

coimage, -- coimage of a map, class => element, Usage: , coimage f, class => language-macaulay2, Inputs:, f, , , a , Macaulay2Doc :: RingMap, ring map, or a , Macaulay2Doc :: Matrix, matrix, from , A, to , B, Outputs:, The object , A/(kernel f), Description, In each of these cases, the coimage is isomorphic to the image, but the coimage is presented as a quotient object of the source of the map, whereas the image is presented as a subobject of the target of the map. For rings, we can represent quotient rings, but not subrings, in Macaulay2. Hence, for ring maps, we can compute the coimage, but not the image., The isomorphism between coimage and image is not always obvious, as the following example shows., class => examples, i1 : R = QQ[a..d];, class => language-macaulay2, i2 : M = matrix{{a^3,b^3-c^3,a*b*c,a*(b^2-c^2)

o2 = | a3 b3-c3 abc ab2-ac2 |

 1 4
o2 : Matrix R <-- R, class => language-macaulay2, i3 : image M

o3 = image | a3 b3-c3 abc ab2-ac2 |

 1
o3 : R-module, submodule of R, class => language-macaulay2, i4 : coimage M

o4 = cokernel {3 | 0 -bc 0 -b2+c2 |
 {3 | a 0 0 0 |
 {3 | b-c a2 b2-c2 0 |
 {3 | -b-c 0 -bc a2 |

 4
o4 : R-module, quotient of R, class => language-macaulay2, i5 : kernel M

o5 = image {3 | 0 -bc 0 -b2+c2 |
 {3 | a 0 0 0 |
 {3 | b-c a2 b2-c2 0 |
 {3 | -b-c 0 -bc a2 |

 4
o5 : R-module, submodule of R, class => language-macaulay2, See also, Macaulay2Doc :: image, Macaulay2Doc :: cokernel, Macaulay2Doc :: kernel, Macaulay2Doc :: comodule, class => waystouse, Ways to use , coimage, :, coimage(Matrix), coimage(RingMap), OldChainComplexes :: coimage(ChainComplexMap), OldChainComplexes :: coimage(GradedModuleMap), Varieties :: coimage(SheafMap), class => waystouse, For the programmer, The object , Macaulay2Doc :: coimage, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/coimage-doc.m2:35:0, .

## Installations

Type signatures for this function:

- `coimage(Matrix)`
- `coimage(RingMap)`
- `coimage(ChainComplexMap)`
- `coimage(GradedModuleMap)`
- `coimage(SheafMap)`

## Examples

```macaulay2
i1 : R = QQ[a..d];
```

## See Also

