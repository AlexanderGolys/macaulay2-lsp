---
title: "createTask"
kind: Function
---

# createTask

**createTask  -- create a task**

**Type:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

createTask  -- create a task

## Examples

```macaulay2
createTask(fx)
createTask f
```

```macaulay2
t = createTask(f3)

```

```text
<<task created>>

Task
```

## Additional Information

- Inputs:
- `f` a [ function](../Type/Function.mdx)
- `x` a [ thing](../Type/Thing.mdx)
- Outputs:
- a [ task](../Type/op_Coredollar_Task.mdx) which when scheduled will apply the function `f` to the argument `x`. In the second form where `x` is not specified it is take to be `()`.

## Debug info

```python
{
    "obj_type": "Function",
    "name": "createTask",
    "safe_name": "op_Coredollar_createTask",
    "type": "CompiledFunction",
    "description": "createTask  -- create a task...",
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$createTask",
        "full_doc": "... (1480 bytes) ...",
        "parsed_headline": "createTask  -- create a task"
    }
}
```
