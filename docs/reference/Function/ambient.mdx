---
title: "ambient"
kind: Function
---

# ambient

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

ambient, -- ambient free module of a subquotient, or ambient ring, Description, Each module M in Macaulay2 is a quotient, submodule or subquotient of a free module, called the ambient free module of M., , The ambient ring of a quotient ring R/I is the polynomial ring of that it is a quotient. The ambient ring of a Galois field is the quotient ring that it was constructed from., , See also, Macaulay2Doc :: GF, Macaulay2Doc :: cover, Macaulay2Doc :: super, class => waystouse, Ways to use , ambient, :, Varieties :: ambient(AffineVariety), ambient(ProjectiveVariety), -- see , Varieties :: ambient(AffineVariety), Varieties :: ambient(CoherentSheaf), Macaulay2Doc :: ambient(GaloisField), OldChainComplexes :: ambient(GradedModule), Macaulay2Doc :: ambient(Matrix), Macaulay2Doc :: ambient(Module), ambient(PolynomialRing), -- see , Macaulay2Doc :: ambient(Ring), ambient(QuotientRing), -- see , Macaulay2Doc :: ambient(Ring), Macaulay2Doc :: ambient(Ring), Varieties :: ambient(SheafMap), class => waystouse, For the programmer, The object , Macaulay2Doc :: ambient, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/ambient-doc.m2:18:0, .

## Installations

Type signatures for this function:

- `ambient(GaloisField)`
- `ambient(Matrix)`
- `ambient(QuotientRing)`
- `ambient(Module)`
- `ambient(Ring)`
- `ambient(GradedModule)`
- `ambient(AffineVariety)`
- `ambient(ProjectiveVariety)`
- `ambient(CoherentSheaf)`
- `ambient(SheafMap)`
- `ambient(PolynomialRing)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

