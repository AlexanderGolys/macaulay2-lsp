---
title: "about"
kind: Function
---

# about

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

about, -- search the documentation, class => element, Usage: , about s, class => language-macaulay2, Inputs:, s, , , a , Macaulay2Doc :: String, string, , , a , Macaulay2Doc :: Function, function, , , a , Macaulay2Doc :: Symbol, symbol, , or , a , Macaulay2Doc :: Type, type, , , Macaulay2Doc :: using functions with optional inputs, Optional inputs, :, Body, => , a , Macaulay2Doc :: Boolean, Boolean value, , , default value , false, , , whether also to search the bodies of the documentation nodes. By default, just their keys are searched., Outputs:, a , Macaulay2Doc :: NumberedVerticalList, numbered vertical list, , , a list of documentation node keys matching the regular expression in the string , s, , if , s, is a string. Otherwise the search matches against the name of , s, as a complete word., Description, The documentation corresponding to the keys in the list returned can be displayed by applying the function , Macaulay2Doc :: help, to it. To see the documentation corresponding to just one or some of the keys, give , Macaulay2Doc :: help, an integer or a list of integers to be used as indices in the list returned by the most recent application of , Macaulay2Doc :: about, . The functions , Macaulay2Doc :: viewHelp, and , Macaulay2Doc :: infoHelp, can also be given an integer for viewing the documentation., The packages searched are the loaded packages and the packages installed under one of the prefixes listed in , Macaulay2Doc :: prefixPath, . The first search will take a few seconds while it reads all the documentation keys into memory., class => examples, i1 : about firstFunction

o1 = {0 => FirstPackage :: firstFunction 
 {1 => FirstPackage :: firstFunction(ZZ)

o1 : NumberedVerticalList, class => language-macaulay2, i2 : help 1

o2 = FirstPackage::firstFunction(ZZ) -- a silly first function
 *********************************************************

 * Usage: 
 firstFunction n
 * Inputs:
 * n, an "integer", 
 * Outputs:
 * a "string", a silly string, depending on the value of n

 Here we show an example.

 +------------------------+
 | i1 : firstFunction 1 |
 | |
 | o1 = Hello World! |
 +------------------------+
 | i2 : firstFunction 0 |
 | |
 | o2 = D'oh! |
 +------------------------+
 ------------------------------------------------------------------------

 The source of this document is in FirstPackage.m2:53:0., class => language-macaulay2, It is also possible to view a table of headlines corresponding to the results., class => examples, i3 : headlines about firstFunction

o3 = 0. "FirstPackage :: firstFunction" -- a silly first function
 1. "FirstPackage :: firstFunction(ZZ)" -- a silly first function, class => language-macaulay2, Caveat, Since , s, is taken as a regular expression, parentheses serve for grouping subexpressions, rather than matching themselves., See also, Macaulay2Doc :: ? Symbol, Macaulay2Doc :: apropos, Macaulay2Doc :: headlines, Macaulay2Doc :: findSynonyms, Macaulay2Doc :: regular expressions, Macaulay2Doc :: examples(ZZ), class => waystouse, Ways to use , about, :, about(Function), about(String), about(Symbol), about(Type), Macaulay2Doc :: about(ScriptedFunctor), class => waystouse, For the programmer, The object , Macaulay2Doc :: about, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/help-doc.m2:349:0, .

## Installations

Type signatures for this function:

- `about(ScriptedFunctor)`
- `about(Symbol)`
- `about(Type)`
- `about(Function)`
- `about(String)`

## Examples

```macaulay2
i2 : help 1

o2 = FirstPackage::firstFunction(ZZ) -- a silly first function
     *********************************************************

       * Usage: 
             firstFunction n
       * Inputs:
           * n, an "integer", 
       * Outputs:
           * a "string", a silly string, depending on the value of n

     Here we show an example.

     +------------------------+
     |  i1 : firstFunction 1  |
     |                        |
     |  o1 = Hello World!     |
     +------------------------+
     |  i2 : firstFunction 0  |
     |                        |
     |  o2 = D'oh!            |
     +------------------------+
     ------------------------------------------------------------------------

     The source of this document is in FirstPackage.m2:53:0.
```

## See Also

