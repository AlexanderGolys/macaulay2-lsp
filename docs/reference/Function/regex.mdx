---
title: "regex"
kind: Function
---

# regex

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

regex, -- evaluate a regular expression search, class => element, Usage: , regex(re, str), class => language-macaulay2, regex(re, start, str), class => language-macaulay2, regex(re, start, range, str), class => language-macaulay2, Inputs:, re, , , a , Macaulay2Doc :: String, string, , , a , Macaulay2Doc :: regular expressions, regular expression, describing a pattern, start, , , an , Macaulay2Doc :: ZZ, integer, , , positive, the position in , str, at which to begin the search. when omitted, the search starts at the beginning of the string., range, , , an , Macaulay2Doc :: ZZ, integer, , , restricts matches to those beginning at a position between , start, and , start + range, ; when 0, the pattern is matched only at the starting position; when negative, only positions to the left of the starting position are examined for matches; when omitted, the search extends to the end of the string., str, , , a , Macaulay2Doc :: String, string, , , the subject string to be searched, Macaulay2Doc :: using functions with optional inputs, Optional inputs, :, POSIX, => , a , Macaulay2Doc :: Boolean, Boolean value, , , default value , false, , , if true, interpret the , re, using the POSIX Extended flavor, otherwise the Perl flavor, Outputs:, a , Macaulay2Doc :: List, list, , , a list of pairs of integers; each pair denotes the beginning position and the length of a substring. Only the leftmost matching substring of , str, and the capturing groups within it are returned. If no match is found, the output is , Macaulay2Doc :: null, ., Description, The value returned is a list of pairs of integers corresponding to the parenthesized subexpressions successfully matched, suitable for use as the first argument of , Macaulay2Doc :: substring, . The first member of each pair is the offset within , str, of the substring matched, and the second is the length., See , Macaulay2Doc :: regular expressions, for a brief introduction to the topic., class => examples, i1 : s = "The cat is black.";, class => language-macaulay2, i2 : m = regex("(\\w+) (\\w+) (\\w+)",s)

o2 = {(0, 10), (0, 3), (4, 3), (8, 2)

o2 : List, class => language-macaulay2, i3 : substring(m#0, s)

o3 = The cat is, class => language-macaulay2, i4 : substring(m#1, s)

o4 = The, class => language-macaulay2, i5 : substring(m#2, s)

o5 = cat, class => language-macaulay2, i6 : substring(m#3, s)

o6 = is, class => language-macaulay2, i7 : s = "aa aaaa";, class => language-macaulay2, i8 : m = regex("a+", 0, s)

o8 = {(0, 2)

o8 : List, class => language-macaulay2, i9 : substring(m#0, s)

o9 = aa, class => language-macaulay2, i10 : m = regex("a+", 2, s)

o10 = {(7, 4)

o10 : List, class => language-macaulay2, i11 : substring(m#0, s)

o11 = aaaa, class => language-macaulay2, i12 : m = regex("a+", 2, 3, s), class => language-macaulay2, i13 : s = "line 1\nline 2\r\nline 3";, class => language-macaulay2, i14 : m = regex("^.*$", 8, -8, s)

o14 = {(7, 6)

o14 : List, class => language-macaulay2, i15 : substring(m#0, s)

o15 = line 2, class => language-macaulay2, i16 : m = regex("^", 10, -10, s)

o16 = {(7, 0)

o16 : List, class => language-macaulay2, i17 : substring(0, m#0#0, s)

o17 = line 1, class => language-macaulay2, i18 : substring(m#0#0, s)

o18 = line 2
 line 3, class => language-macaulay2, i19 : m = regex("^.*$", 4, -10, s)

o19 = {(0, 6)

o19 : List, class => language-macaulay2, i20 : substring(m#0, s)

o20 = line 1, class => language-macaulay2, i21 : m = regex("a.*$", 4, -10, s), class => language-macaulay2, By default, the regular expressions are interpreted using the Perl flavor, which supports features such as lookaheads and lookbehinds for fine-tuning the matches. This syntax is used in Perl and JavaScript languages., class => examples, i22 : regex("A(?!C)", "AC AB")

o22 = {(3, 1)

o22 : List, class => language-macaulay2, i23 : regex("A(?=B)", "AC AB")

o23 = {(3, 1)

o23 : List, class => language-macaulay2, Alternatively, one can choose the POSIX Extended flavor of regex using , POSIX => true, . This syntax is similar to the one used by the Unix utilities , egrep, and , awk, and enforces the , leftmost, longest, rule for finding matches. If there's a tie, the rule is applied to the first subexpression., class => examples, i24 : s = "<b>bold</b> and <b>strong</b>";, class => language-macaulay2, i25 : m = regex("<b>(.*)</b>", s, POSIX => true);, class => language-macaulay2, i26 : substring(m#1, s)

o26 = bold</b> and <b>strong, class => language-macaulay2, In the Perl flavor, one can specify whether repetitions should be possessive or non-greedy., class => examples, i27 : m = regex("<b>(.*?)</b>", s);, class => language-macaulay2, i28 : substring(m#1, s)

o28 = bold, class => language-macaulay2, See also, Macaulay2Doc :: regular expressions, Macaulay2Doc :: strings and nets, Macaulay2Doc :: match, Macaulay2Doc :: separate, Macaulay2Doc :: replace(String,String,String), Macaulay2Doc :: select(String,String,String), Macaulay2Doc :: regexQuote, Macaulay2Doc :: substring, class => waystouse, Ways to use , regex, :, regex(String,String), regex(String,ZZ,String), regex(String,ZZ,ZZ,String), class => waystouse, For the programmer, The object , Macaulay2Doc :: regex, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/regex-doc.m2:104:0, .

## Installations

Type signatures for this function:

- `regex(String, String)`
- `regex(String, ZZ, String)`
- `regex(String, ZZ, ZZ, String)`

## Examples

```macaulay2
i1 : s = "The cat is black.";
```

## See Also

