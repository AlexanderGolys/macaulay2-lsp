---
title: "prune"
kind: Function
---

# prune

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

prune, -- prune, e.g., compute a minimal presentation, empty documentation body, See also, Macaulay2Doc :: minimalPresentation, Macaulay2Doc :: pruningMap, class => waystouse, Ways to use , prune, :, prune(CoherentSheaf), -- see , Varieties :: minimalPresentation(CoherentSheaf), prune(SheafMap), -- see , Varieties :: minimalPresentation(CoherentSheaf), prune(Ideal), -- see , Macaulay2Doc :: minimalPresentation(Ideal), prune(Matrix), -- see , Macaulay2Doc :: minimalPresentation(Matrix), prune(Module), -- see , Macaulay2Doc :: minimalPresentation(Module), prune(Ring), -- see , Macaulay2Doc :: minimalPresentation(Ring), OldChainComplexes :: prune(ChainComplex), OldChainComplexes :: prune(ChainComplexMap), OldChainComplexes :: prune(GradedModule), OldChainComplexes :: prune(GradedModuleMap), Varieties :: prune(SheafOfRings), class => waystouse, For the programmer, The object , Macaulay2Doc :: prune, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/minimalPresentation-doc.m2:22:0, .

## Installations

Type signatures for this function:

- `prune(Ideal)`
- `prune(Ring)`
- `prune(Module)`
- `prune(Matrix)`
- `prune(ChainComplex)`
- `prune(ChainComplexMap)`
- `prune(GradedModule)`
- `prune(GradedModuleMap)`
- `prune(SheafOfRings)`
- `prune(CoherentSheaf)`
- `prune(SheafMap)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

