---
title: "flatten"
kind: Function
---

# flatten

**Type:** `Function`

**Instance of:** [MethodFunctionSingle](../Type/MethodFunctionSingle.mdx)

## Description

flatten, -- flatten a nested list or a matrix, class => element, Usage: , flatten x, class => language-macaulay2, Inputs:, x, , , a , Macaulay2Doc :: List, list, , , or , Macaulay2Doc :: Matrix, Outputs:, a , Macaulay2Doc :: List, list, , , or matrix obtained by removing braces one level in, Description, If , x, is a list, then , flatten x, effectively removes the braces surrounding any elements of , x, that happen to be lists., class => examples, i1 : flatten {{2,3,4, {{5, 6, 7

o1 = {2, 3, 4, {5, 6, 7

o1 : List, class => language-macaulay2, If , x, is a matrix, then , flatten x, puts the elements of , x, in a single row, , ordered by column, . If , x, is an $m\times n$ matrix, then , flatten x, is a $1\times mn$ matrix., class => examples, i2 : R = ZZ/101[a,b,c];, class => language-macaulay2, i3 : m = matrix {{2, a,{b^2, 23,{c, c^3

o3 = | 2 a |
 | b2 23 |
 | c c3 |

 3 2
o3 : Matrix R <-- R, class => language-macaulay2, i4 : flatten m

o4 = | 2 b2 c a 23 c3 |

 1 6
o4 : Matrix R <-- R, class => language-macaulay2, Caveat, The matrix produced by , flatten m, is , not, the same as the matrix given by unnesting the list used to define the matrix., See also, Macaulay2Doc :: deepSplice, Macaulay2Doc :: splice, Macaulay2Doc :: lists and sequences, class => waystouse, Ways to use , flatten, :, flatten(Matrix), flatten(VisibleList), class => waystouse, For the programmer, The object , Macaulay2Doc :: flatten, is , a , Macaulay2Doc :: MethodFunctionSingle, method function with a single argument, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/flatten-doc.m2:41:0, .

## Installations

Type signatures for this function:

- `flatten(Matrix)`
- `flatten(VisibleList)`

## Examples

```macaulay2
i2 : R = ZZ/101[a,b,c];
```

## See Also

