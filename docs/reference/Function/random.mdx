---
title: "random"
kind: Function
---

# random

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

random, -- get a random object, Description, This function can be used to get randomized objects of various sorts., Macaulay2Doc :: random(ZZ,ZZ), Macaulay2Doc :: random(QQ), Macaulay2Doc :: random(Type), Macaulay2Doc :: random(List), Macaulay2Doc :: random(ZZ,Ideal), Macaulay2Doc :: random(ZZ,Ring), Macaulay2Doc :: random(Module), Macaulay2Doc :: random(Module,Module), Macaulay2Doc :: setRandomSeed, Macaulay2Doc :: randomSubset, class => waystouse, Ways to use , random, :, Varieties :: random(CoherentSheaf,CoherentSheaf), Macaulay2Doc :: random(List), Macaulay2Doc :: random(List,Module), random(Module), -- see , Macaulay2Doc :: random(List,Module), random(ZZ,Module), -- see , Macaulay2Doc :: random(List,Module), Macaulay2Doc :: random(Module,Module), Macaulay2Doc :: random(QQ), Macaulay2Doc :: random(Type), random(List,Ideal), -- see , Macaulay2Doc :: random(ZZ,Ideal), Macaulay2Doc :: random(ZZ,Ideal), random(List,Ring), -- see , Macaulay2Doc :: random(ZZ,Ring), Macaulay2Doc :: random(ZZ,Ring), random(RR), -- see , Macaulay2Doc :: random(ZZ,ZZ), random(RR,RR), -- see , Macaulay2Doc :: random(ZZ,ZZ), random(ZZ), -- see , Macaulay2Doc :: random(ZZ,ZZ), Macaulay2Doc :: random(ZZ,ZZ), class => waystouse, For the programmer, The object , Macaulay2Doc :: random, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/random-doc.m2:262:0, .

## Installations

Type signatures for this function:

- `random(GaloisField)`
- `random(List, Ring)`
- `random(ZZ, Ring)`
- `random(Module, Module)`
- `random(List, Module)`
- `random(ZZ, Module)`
- `random(Module)`
- `random(ZZ, Ideal)`
- `random(List, Ideal)`
- `random(ZZ)`
- `random(ZZ, ZZ)`
- `random(List)`
- `random(Type)`
- `random(QuotientRing)`
- `random(RR)`
- `random(RR, RR)`
- `random(RingFamily)`
- `random(QQ)`
- `random(CoherentSheaf, CoherentSheaf)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

