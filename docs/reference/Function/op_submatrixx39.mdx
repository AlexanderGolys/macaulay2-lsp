---
title: "submatrix'"
kind: Function
---

# submatrix'

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

submatrix', -- exclude rows and/or columns of a matrix, class => element, Usage: , submatrix'(f, rows, cols), class => language-macaulay2, submatrix'(f,,cols), class => language-macaulay2, submatrix'(f,cols), class => language-macaulay2, submatrix'(f,rows,), class => language-macaulay2, Inputs:, f, , , a map between free modules, rows, , , a , Macaulay2Doc :: VisibleList, visible list, , , a list of integers denoting the choice of rows to exclude. If omitted, exclude no rows, cols, , , a , Macaulay2Doc :: VisibleList, visible list, , , a list of integers denoting the choice of columns to exclude. If omitted, exclude no columns, Outputs:, a , Macaulay2Doc :: Matrix, matrix, , , the submatrix of , f, with , rows, and , cols, left out, Description, , Each list of integers may contain ranges and repetitions, as in , {3, 5..7, 3:0, . Excluding a row or column more than once is allowed but has no extra effect., , class => examples, i1 : R = ZZ/101[a .. o];, class => language-macaulay2, i2 : f = genericMatrix(R, a, 3, 5)

o2 = | a d g j m |
 | b e h k n |
 | c f i l o |

 3 5
o2 : Matrix R <-- R, class => language-macaulay2, i3 : submatrix'(f, {1, {2)

o3 = | a d j m |
 | c f l o |

 2 4
o3 : Matrix R <-- R, class => language-macaulay2, i4 : submatrix'(f, {1, {2,3,3,2,2)

o4 = | a d m |
 | c f o |

 2 3
o4 : Matrix R <-- R, class => language-macaulay2, , class => examples, i5 : submatrix'(f, {1,3, )

o5 = | a d g j m |
 | c f i l o |

 2 5
o5 : Matrix R <-- R, class => language-macaulay2, i6 : submatrix'(f, , {1,3 )

o6 = | a g m |
 | b h n |
 | c i o |

 3 3
o6 : Matrix R <-- R, class => language-macaulay2, i7 : submatrix'(f, {1,3 )

o7 = | a g m |
 | b h n |
 | c i o |

 3 3
o7 : Matrix R <-- R, class => language-macaulay2, See also, Macaulay2Doc :: submatrix, class => waystouse, Ways to use , submatrix', :, submatrix'(Matrix,Nothing,Nothing), submatrix'(Matrix,Nothing,VisibleList), submatrix'(Matrix,VisibleList), submatrix'(Matrix,VisibleList,Nothing), submatrix'(Matrix,VisibleList,VisibleList), class => waystouse, For the programmer, The object , Macaulay2Doc :: submatrix', is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/submatrix-doc.m2:184:0, .

## Installations

Type signatures for this function:

- `submatrix'(Matrix, VisibleList, VisibleList)`
- `submatrix'(Matrix, VisibleList, Nothing)`
- `submatrix'(Matrix, VisibleList)`
- `submatrix'(Matrix, Nothing, VisibleList)`
- `submatrix'(Matrix, Nothing, Nothing)`

## Examples

```macaulay2
i1 : R = ZZ/101[a .. o];

i2 : f = genericMatrix(R, a, 3, 5)

o2 = | a d g j m |
     | b e h k n |
     | c f i l o |

             3      5
o2 : Matrix R  <-- R
```

## See Also

