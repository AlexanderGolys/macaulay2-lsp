---
title: "index"
kind: Function
---

# index

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

index, -- numeric index of a ring variable, class => element, Usage: , index v, class => language-macaulay2, Inputs:, v, , , a , Macaulay2Doc :: RingElement, ring element, Outputs:, an , Macaulay2Doc :: ZZ, integer, , , the integer index, starting at 0, of v, if it is a variable. If v is not a variable, then , Macaulay2Doc :: null, is returned, Description, Variables are indexed in the following way: the first variable has index 0, the second index 1, and so on, until n-1, where n is the number of generators of R, the ring of v. Then, if the coefficient ring is a polynomial ring, those variables are numbered starting at n, and so on., class => examples, i1 : R = ZZ/101[a..d,t]

o1 = R

o1 : PolynomialRing, class => language-macaulay2, i2 : index a

o2 = 0, class => language-macaulay2, i3 : index t

o3 = 4, class => language-macaulay2, class => examples, i4 : A = ZZ[a..d]; B = A[r,s,t]; C = B[x,y,z]

o6 = C

o6 : PolynomialRing, class => language-macaulay2, i7 : index x

o7 = 0, class => language-macaulay2, i8 : index z

o8 = 2, class => language-macaulay2, i9 : index r

o9 = 0, class => language-macaulay2, Notice that r is an element of B, and so indices are taken from that ring. If we consider r as an element of C, we get a different answer. Variables of coefficient rings of coefficient rings have an index too., class => examples, i10 : index(r*1_C)

o10 = 3, class => language-macaulay2, i11 : index(b*1_C)

o11 = 7, class => language-macaulay2, , The symbol , index, is also as a key used in , Macaulay2Doc :: Monoid, s, to store a table that is used to map generator names to the position of the generator in the list of generators., See also, Macaulay2Doc :: indices, Macaulay2Doc :: support, Macaulay2Doc :: Ring _ ZZ, class => waystouse, Ways to use , index, :, index(RingElement), class => waystouse, For the programmer, The object , Macaulay2Doc :: index, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/index-doc.m2:44:0, .

## Installations

Type signatures for this function:

- `index(RingElement)`

## Examples

```macaulay2
i1 : R = ZZ/101[a..d,t]

o1 = R

o1 : PolynomialRing

i2 : index a

o2 = 0
```

## See Also

