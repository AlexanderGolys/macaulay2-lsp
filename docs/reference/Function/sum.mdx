---
title: "sum"
kind: Function
---

# sum

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

sum, -- compute the sum, Description, sum, provides the sum of the members of a list, set, or chain complex, optionally with a function applied to each one., Macaulay2Doc :: sum(List), Macaulay2Doc :: sum(ZZ,Function), Macaulay2Doc :: sum(VisibleList,Function), Macaulay2Doc :: sum(VisibleList,VisibleList,Function), class => waystouse, Ways to use , sum, :, OldChainComplexes :: sum(ChainComplex), OldChainComplexes :: sum(ChainComplexMap), Macaulay2Doc :: sum(List), Macaulay2Doc :: sum(Set), Macaulay2Doc :: sum(VirtualTally), Macaulay2Doc :: sum(VisibleList,Function), Macaulay2Doc :: sum(VisibleList,VisibleList,Function), Macaulay2Doc :: sum(ZZ,Function), class => waystouse, For the programmer, The object , Macaulay2Doc :: sum, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/sum-doc.m2:13:0, .

## Installations

Type signatures for this function:

- `sum(List)`
- `sum(ZZ, Function)`
- `sum(VisibleList, Function)`
- `sum(VisibleList, VisibleList, Function)`
- `sum(VirtualTally)`
- `sum(Set)`
- `sum(ChainComplex)`
- `sum(ChainComplexMap)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

