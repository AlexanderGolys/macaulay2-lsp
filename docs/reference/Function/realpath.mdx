---
title: "realpath"
kind: Function
---

# realpath

**realpath  -- convert a filename to one passing through no symbolic links**

**Type:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

The empty string is interpreted as a reference to the current directory.

## Examples

```macaulay2
realpath fn
```

```macaulay2
realpath "."

```

```text
/tmp/M2-704383-0/88-rundir/
```

```macaulay2
realpath q

```

```text
/tmp/M2-709659-0/0
```

```macaulay2
realpath ""

```

```text
/tmp/M2-704383-0/88-rundir/
```

## Additional Information

- Inputs:
- `fn` a [ string](../Type/op_Coredollar_String.mdx) a filename or path to a file
- Outputs:
- a [ string](../Type/op_Coredollar_String.mdx) a pathname to `fn` passing through no symbolic links and ending with a slash if `fn` refers to a directory


### Caveat

 Every component of the path must exist in the file system and be accessible to the user. Terminal slashes will be dropped.  Warning: under most operating systems the value returned is an absolute path (one starting at the root of the file system) but under Solaris this system call may in certain circumstances return a relative path when given a relative path.
### See also


- Macaulay2Doc:: readlink

## Debug info

```python
{
    "obj_type": "Function",
    "name": "realpath",
    "safe_name": "realpath",
    "type": "CompiledFunction",
    "description": "The empty string is interpreted as a reference to the current directory....",
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "realpath",
        "full_doc": "... (2320 bytes) ...",
        "parsed_headline": "realpath  -- convert a filename to one passing through no symbolic links"
    }
}
```
