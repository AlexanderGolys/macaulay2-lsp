---
title: "unstack"
kind: Function
---

# unstack

**Type:** `Function`

**Instance of:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

unstack, -- list the rows of a net, Description, unstack x, -- produces a list of strings, each containing the characters in one row of the , Net, , x, ., , The original net, adjusted so its height is 1, may be recovered with , Macaulay2Doc :: stack, . The individual strings will have all trailing spaces removed, unless this would make all of them narrower than the original net, in which case the first string retains its trailing spaces., class => waystouse, Ways to use , unstack, :, unstack(Net), unstack(String), class => waystouse, For the programmer, The object , Macaulay2Doc :: unstack, is , a , Macaulay2Doc :: CompiledFunction, compiled function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/ov_strings.m2:283:0, .

## Installations

Type signatures for this function:

- `unstack(Net)`
- `unstack(String)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

