---
title: "coefficientRing"
kind: Function
---

# coefficientRing

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

coefficientRing, -- get the coefficient ring, class => element, Usage: , coefficientRing R, class => language-macaulay2, Inputs:, R, , , a , Macaulay2Doc :: Ring, ring, Outputs:, a , Macaulay2Doc :: Ring, ring, , , the coefficient ring of , R, Description, If , R, is a polynomial ring, then the coefficient ring is the base ring from which the coefficients are drawn. If , R, is constructed from a polynomial ring as a quotient ring or a fraction ring or a sequence of such operations, then the original coefficient ring is returned., class => examples, i1 : coefficientRing(ZZ/101[a][b])

 ZZ
o1 = ---[a]
 101

o1 : PolynomialRing, class => language-macaulay2, i2 : ultimate(coefficientRing,ZZ/101[a][b])

 ZZ
o2 = ---
 101

o2 : QuotientRing, class => language-macaulay2, See also, Macaulay2Doc :: ultimate, Macaulay2Doc :: baseRings, class => waystouse, Ways to use , coefficientRing, :, coefficientRing(Ring), class => waystouse, For the programmer, The object , Macaulay2Doc :: coefficientRing, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/coefficientRing-doc.m2:33:0, .

## Installations

Type signatures for this function:

- `coefficientRing(FractionField)`
- `coefficientRing(QuotientRing)`
- `coefficientRing(PolynomialRing)`
- `coefficientRing(Ring)`

## Examples

```macaulay2
i1 : coefficientRing(ZZ/101[a][b])

      ZZ
o1 = ---[a]
     101

o1 : PolynomialRing

i2 : ultimate(coefficientRing,ZZ/101[a][b])

      ZZ
o2 = ---
     101

o2 : QuotientRing
```

## See Also

