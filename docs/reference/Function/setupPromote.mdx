---
title: "setupPromote"
kind: Function
---

# setupPromote

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

setupPromote, -- set up promote from one ring to another, class => element, Usage: , setupPromote f, class => language-macaulay2, Inputs:, f, , , a , Macaulay2Doc :: RingMap, ring map, , , Description, This defines promotion from one ring to another as the application of a ring map. After calling , setupPromote, , any operation that is given an element of the source of , f, but expects an element of the target of , f, will automatically , Macaulay2Doc :: promote, it by applying , f, ., class => examples, i1 : R=QQ[x_1,x_2]

o1 = R

o1 : PolynomialRing, class => language-macaulay2, i2 : R'=QQ[e_1,e_2,Degrees=>{1,2]

o2 = R'

o2 : PolynomialRing, class => language-macaulay2, i3 : setupPromote map(R,R',{x_1+x_2,x_1*x_2), class => language-macaulay2, i4 : promote(e_1^2,R)

 2 2
o4 = x + 2x x + x
 1 1 2 2

o4 : R, class => language-macaulay2, i5 : e_1*x_1

 2
o5 = x + x x
 1 1 2

o5 : R, class => language-macaulay2, i6 : e_2==x_1*x_2

o6 = true, class => language-macaulay2, See also, Macaulay2Doc :: setupLift, class => waystouse, Ways to use , setupPromote, :, setupPromote(RingMap), class => waystouse, For the programmer, The object , Macaulay2Doc :: setupPromote, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/setupPromote-doc.m2:33:0, .

## Installations

Type signatures for this function:

- `setupPromote(RingMap, Ring, Ring, Function)`
- `setupPromote(Function, Ring, Ring, Function)`
- `setupPromote(Function, Ring, Ring)`
- `setupPromote(RingMap, Ring, Ring)`
- `setupPromote(RingMap)`
- `setupPromote(Ring, Ring)`

## Examples

```macaulay2
i1 : R=QQ[x_1,x_2]

o1 = R

o1 : PolynomialRing
```

## See Also

