---
title: "subsets"
kind: Function
---

# subsets

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

subsets, -- produce the subsets of a set or list, class => element, Usage: , subsets(A), class => language-macaulay2, subsets(A, n), class => language-macaulay2, Inputs:, A, , , a , Macaulay2Doc :: List, list, , , , sequence, set or integer, n, , , an , Macaulay2Doc :: ZZ, integer, , , optional input to specify subsets of a particular size, Outputs:, L, , , a , Macaulay2Doc :: List, list, , , of subsets (of size , n, if given), Description, If , A, is an integer, , subsets(A), lists the subsets of , \{0, 1, ..., A-1\, ., class => examples, i1 : subsets(3)

o1 = {{, {0, {1, {0, 1, {2, {0, 2, {1, 2, {0, 1, 2

o1 : List, class => language-macaulay2, i2 : subsets(5, 3)

o2 = {{0, 1, 2, {0, 1, 3, {0, 2, 3, {1, 2, 3, {0, 1, 4, {0, 2, 4, {1,
 ------------------------------------------------------------------------
 2, 4, {0, 3, 4, {1, 3, 4, {2, 3, 4

o2 : List, class => language-macaulay2, , A, can be a list, sequence, or set. The elements need not be of the same type., class => examples, i3 : subsets({"apple", "banana", {1,2,3, 7.1, 3)

o3 = {{apple, banana, {1, 2, 3, {apple, banana, 7.1, {apple, {1, 2, 3,
 ------------------------------------------------------------------------
 7.1, {banana, {1, 2, 3, 7.1

o3 : List, class => language-macaulay2, If a list contains repetitions, so will the subsets of that list. Since a , Macaulay2Doc :: Set, has no repetitions, neither do its subsets. Also, the subsets of a set will again be sets (while the subsets of a list are lists)., class => examples, i4 : subsets({"apple", "apple", "banana")

o4 = {{, {apple, {apple, {apple, apple, {banana, {apple, banana,
 ------------------------------------------------------------------------
 {apple, banana, {apple, apple, banana

o4 : List, class => language-macaulay2, i5 : subsets(set{"apple", "apple", "banana")

o5 = {set {, set {banana, set {apple, set {banana, apple

o5 : List, class => language-macaulay2, The subsets of a Sequence are lists, not sequences. Also, a subset size , must, be specified when calling , subsets, on a sequence., See also, Macaulay2Doc :: partition, Macaulay2Doc :: set, Macaulay2Doc :: sublists, Macaulay2Doc :: substring, Macaulay2Doc :: lists and sequences, class => waystouse, Ways to use , subsets, :, subsets(List), subsets(List,ZZ), subsets(Sequence,ZZ), subsets(Set), subsets(Set,ZZ), subsets(ZZ), subsets(ZZ,ZZ), class => waystouse, For the programmer, The object , Macaulay2Doc :: subsets, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/subsets-doc.m2:54:0, .

## Installations

Type signatures for this function:

- `subsets(ZZ, ZZ)`
- `subsets(Set, ZZ)`
- `subsets(List, ZZ)`
- `subsets(Sequence, ZZ)`
- `subsets(ZZ)`
- `subsets(Set)`
- `subsets(List)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

