---
title: "promote"
kind: Function
---

# promote

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

promote, -- promote to another ring, class => element, Usage: , promote(f,R), class => language-macaulay2, Inputs:, f, , , a , Macaulay2Doc :: RingElement, ring element, , , an , Macaulay2Doc :: Ideal, ideal, , or , a , Macaulay2Doc :: Matrix, matrix, over some base ring of R, R, , , a , Macaulay2Doc :: Ring, ring, Outputs:, a , Macaulay2Doc :: RingElement, ring element, , , or , a , Macaulay2Doc :: Matrix, matrix, , over R, Description, Promote the given ring element or matrix , f, to an element or matrix of , R, , via the natural map to , R, . This is semantically equivalent to creating the natural ring map from , ring f --> R, and mapping f via this map., class => examples, i1 : R = QQ[a..d]; f = a^2;, class => language-macaulay2, i3 : S = R/(a^2-b-1);, class => language-macaulay2, i4 : promote(2/3,S)

 2
o4 = -
 3

o4 : S, class => language-macaulay2, i5 : F = map(R,QQ); F(2/3)

o5 : RingMap R <-- QQ

 2
o6 = -
 3

o6 : R, class => language-macaulay2, i7 : promote(f,S)

o7 = b + 1

o7 : S, class => language-macaulay2, i8 : G = map(S,R); G(f)

o8 : RingMap S <-- R

o9 = b + 1

o9 : S, class => language-macaulay2, Promotion of real numbers to rational numbers is accomplished by using all of the bits of the internal representation., class => examples, i10 : promote(101.,QQ)

o10 = 101

o10 : QQ, class => language-macaulay2, i11 : promote(.101,QQ)

 3638908498915361
o11 = -----------------
 36028797018963968

o11 : QQ, class => language-macaulay2, i12 : factor denominator oo

 55
o12 = 2

o12 : Expression of class Product, class => language-macaulay2, i13 : ooo + 0.

o13 = .101

o13 : RR (of precision 53), class => language-macaulay2, i14 : oo === .101

o14 = true, class => language-macaulay2, For promotion of ring elements, there is the following shorter notation., class => examples, i15 : 13_R

o15 = 13

o15 : R, class => language-macaulay2, If you wish to promote a module to another ring, either promote the corresponding matrices, use the natural ring map, or use tensor product of matrices or modules., class => examples, i16 : use R;, class => language-macaulay2, i17 : I = ideal(a^2,a^3,a^4)

 2 3 4
o17 = ideal (a , a , a )

o17 : Ideal of R, class => language-macaulay2, i18 : promote(I,S)

 2
o18 = ideal (b + 1, a*b + a, b + 2b + 1)

o18 : Ideal of S, class => language-macaulay2, i19 : m = image matrix{{a^2,a^3,a^4

o19 = image | a2 a3 a4 |

 1
o19 : R-module, submodule of R, class => language-macaulay2, i20 : promote(gens m,S)

o20 = | b+1 ab+a b2+2b+1 |

 1 3
o20 : Matrix S <-- S, class => language-macaulay2, i21 : G m

o21 = image | b+1 ab+a b2+2b+1 |

 1
o21 : S-module, submodule of S, class => language-macaulay2, i22 : m ** S

o22 = cokernel {2 | a 0 |
 {3 | -1 a |
 {4 | 0 -1 |

 3
o22 : S-module, quotient of S, class => language-macaulay2, A special feature is that if , f, is rational, and , R, is not an algebra over , Macaulay2Doc :: QQ, , then an element of , R, is provided by attempting the evident division., See also, Macaulay2Doc :: baseRings, Macaulay2Doc :: lift, Macaulay2Doc :: liftable, Macaulay2Doc :: substitution and maps between rings, Macaulay2Doc :: substitute, Macaulay2Doc :: Matrix ** Ring, Macaulay2Doc :: setupPromote, class => waystouse, Ways to use , promote, :, promote(RR,type of QQ), Macaulay2Doc :: promote(Constant,type of InexactNumber'), Macaulay2Doc :: promote(Constant,type of RingElement), Macaulay2Doc :: promote(IndexedVariable,type of RingElement), Varieties :: promote(SheafMap,type of Nothing), Varieties :: promote(SheafMap,type of RingElement), class => waystouse, For the programmer, The object , Macaulay2Doc :: promote, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/promote-doc.m2:155:0, .

## Installations

Type signatures for this function:

- `promote(QQ, RingElement)`
- `promote(ZZ, RingElement)`
- `promote(ZZ, ZZ)`
- `promote(Ideal, Number)`
- `promote(Ideal, RingElement)`
- `promote(Matrix, InexactNumber)`
- `promote(Matrix, Number)`
- `promote(Matrix, InexactNumber')`
- `promote(Matrix, RingElement)`
- `promote(List, QQ, QQ)`
- `promote(List, ZZ, ZZ)`
- `promote(Matrix, ZZ, ZZ)`
- `promote(Matrix, QQ, QQ)`
- `promote(Matrix, ZZ, RRi_*)`
- `promote(Matrix, CC_*, CC_*)`
- `promote(Matrix, ZZ, CC_*)`
- `promote(Matrix, RR_*, RR_*)`
- `promote(Matrix, RR_*, CC_*)`
- `promote(Matrix, RR_*, RRi_*)`
- `promote(Matrix, ZZ, RR_*)`
- `promote(Matrix, ZZ, QQ)`
- `promote(Matrix, QQ, RR_*)`
- `promote(Matrix, QQ, RRi_*)`
- `promote(List, ZZ, QQ)`
- `promote(List, CC_*, CC_*)`
- `promote(List, RR_*, RRi_*)`
- `promote(List, RR_*, CC_*)`
- `promote(List, RR_*, RR_*)`
- `promote(List, QQ, CC_*)`
- `promote(List, QQ, RR_*)`

*... and 41 more*

## Examples

```macaulay2
i1 : R = QQ[a..d]; f = a^2;
```

## See Also

