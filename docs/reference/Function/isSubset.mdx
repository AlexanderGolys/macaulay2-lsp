---
title: "isSubset"
kind: Function
---

# isSubset

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

isSubset, -- whether one object is a subset of another, empty documentation body, class => waystouse, Ways to use , isSubset, :, Macaulay2Doc :: isSubset(Ideal,Ideal), isSubset(Ideal,Module), -- see , Macaulay2Doc :: isSubset(Module,Module), isSubset(Module,Ideal), -- see , Macaulay2Doc :: isSubset(Module,Module), Macaulay2Doc :: isSubset(Module,Module), Macaulay2Doc :: isSubset(RRi,RRi), Macaulay2Doc :: isSubset(Set,Set), isSubset(Set,VisibleList), -- see , Macaulay2Doc :: isSubset(Set,Set), isSubset(VisibleList,Set), -- see , Macaulay2Doc :: isSubset(Set,Set), isSubset(VisibleList,VisibleList), -- see , Macaulay2Doc :: isSubset(Set,Set), class => waystouse, For the programmer, The object , Macaulay2Doc :: isSubset, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/isSubset-doc.m2:10:0, .

## Installations

Type signatures for this function:

- `isSubset(RRi, RRi)`
- `isSubset(Module, Ideal)`
- `isSubset(Ideal, Ideal)`
- `isSubset(Ideal, Module)`
- `isSubset(Module, Module)`
- `isSubset(Set, Set)`
- `isSubset(VisibleList, Set)`
- `isSubset(VisibleList, VisibleList)`
- `isSubset(Set, VisibleList)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

