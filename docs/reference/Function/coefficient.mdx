---
title: "coefficient"
kind: Function
---

# coefficient

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

coefficient, -- coefficient of a monomial, class => element, Usage: , coefficient(m,f), class => language-macaulay2, Inputs:, m, , , a , Macaulay2Doc :: RingElement, ring element, , , a monomial, f, , , a , Macaulay2Doc :: RingElement, ring element, , , in the same ring , R, as , m, Outputs:, a , Macaulay2Doc :: RingElement, ring element, , , the coefficient of the monomial , m, in , f, as an element of the coefficient ring of , R, Description, class => examples, i1 : R = GF(25,Variable=>a)[x,y,z];, class => language-macaulay2, i2 : f = ((a+1)*x+a*y+a^2*z)^2

 2 2 
o2 = (- 2a - 1)x + (- a + 1)x*y + (a - 2)y + 2x*z + (- 2a + 1)y*z + (2a +
 ------------------------------------------------------------------------
 2
 2)z

o2 : R, class => language-macaulay2, i3 : coefficient(y^2,f)

o3 = a - 2

o3 : GF 25, class => language-macaulay2, The returned value is an element of the coefficient ring, even in the case when that ring is another polynomial ring., class => examples, i4 : S = R[r,s,t];, class => language-macaulay2, i5 : coefficient(r,a*x*(r+a*s))

o5 = a*x

o5 : R, class => language-macaulay2, See also, Macaulay2Doc :: coefficients, Macaulay2Doc :: monomials, Macaulay2Doc :: coefficientRing, class => waystouse, For the programmer, The object , Macaulay2Doc :: coefficient, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/coefficient-doc.m2:35:0, .

## Installations

Type signatures for this function:

- `coefficient(MonoidElement, RingElement)`
- `coefficient(RingElement, RingElement)`

## Examples

```macaulay2
i1 : R = GF(25,Variable=>a)[x,y,z];

i2 : f = ((a+1)*x+a*y+a^2*z)^2

                2                          2                               
o2 = (- 2a - 1)x  + (- a + 1)x*y + (a - 2)y  + 2x*z + (- 2a + 1)y*z + (2a +
     ------------------------------------------------------------------------
        2
     2)z

o2 : R
```

## See Also

