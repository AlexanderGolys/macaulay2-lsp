---
title: "symbolBody"
kind: Function
---

# symbolBody

**Type:** `Function`

**Instance of:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

symbolBody, -- symbol bodies, Description, A Macaulay2 symbol is much like a function closure, in that it comes equipped with a pointer to a frame that contains a value for it. That value can be recovered with the function , Macaulay2Doc :: value, , as follows., class => examples, i1 : f = x -> symbol x

o1 = f

o1 : FunctionClosure, class => language-macaulay2, i2 : s = f 1

o2 = x

o2 : Symbol, class => language-macaulay2, i3 : t = f 2

o3 = x

o3 : Symbol, class => language-macaulay2, The two symbols created in the example above have something in common -- they are created by the same bit of code (the function , f, ), but they have different values., class => examples, i4 : value s

o4 = 1, class => language-macaulay2, i5 : value t

o5 = 2, class => language-macaulay2, To allow the user to determine whether two symbols are created by the same bit of code, Macaulay2 has the notion of symbol body, which parallel to the notion of function body. It's essentially the symbol, but without the pointer to the frame that contains the value., class => examples, i6 : symbolBody s

o6 = x

o6 : SymbolBody, class => language-macaulay2, i7 : symbolBody t

o7 = x

o7 : SymbolBody, class => language-macaulay2, i8 : symbolBody s === symbolBody t

o8 = true, class => language-macaulay2, All such symbol bodies are members of the class , SymbolBody, ., class => examples, i9 : class symbolBody s

o9 = SymbolBody

o9 : Type, class => language-macaulay2, class => waystouse, For the programmer, The object , Macaulay2Doc :: symbolBody, is , a , Macaulay2Doc :: CompiledFunction, compiled function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/ov_repl.m2:947:0, .

## Examples

```macaulay2
i1 : f = x -> symbol x

o1 = f

o1 : FunctionClosure

i2 : s = f 1

o2 = x

o2 : Symbol
```

## See Also

