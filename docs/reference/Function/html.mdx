---
title: "html"
kind: Function
---

# html

**Type:** `Function`

**Instance of:** [MethodFunctionSingle](../Type/MethodFunctionSingle.mdx)

## Description

html, -- convert to HTML format, class => element, Usage: , html x, class => language-macaulay2, Inputs:, x, , , a , Macaulay2Doc :: Thing, thing, , , any Macaulay2 object, Outputs:, a , Macaulay2Doc :: String, string, , , the HTML rendering of , x, Description, html x, converts , x, to HTML format., The return value is a string that is suitable for use in a .html file readable by a browser. , Macaulay2Doc :: hypertext, elements are translated into the corresponding HTML elements. When no HTML conversion is available, , Macaulay2Doc :: tex, is called. $\LaTeX$ can be rendered in the browser using MathJax or $\KaTeX$., See also, Macaulay2Doc :: show, Macaulay2Doc :: mathML, Text :: html(TEX), class => waystouse, Ways to use , html, :, Text :: html(TEX), class => waystouse, For the programmer, The object , Macaulay2Doc :: html, is , a , Macaulay2Doc :: MethodFunctionSingle, method function with a single argument, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/html-doc.m2:34:0, .

## Installations

Type signatures for this function:

- `html(Package)`
- `html(Nothing)`
- `html(Command)`
- `html(FunctionBody)`
- `html(IndeterminateNumber)`
- `html(Pseudocode)`
- `html(Function)`
- `html(File)`
- `html(Manipulator)`
- `html(Time)`
- `html(Program)`
- `html(Type)`
- `html(VerticalList)`
- `html(NumberedVerticalList)`
- `html(Dictionary)`
- `html(Boolean)`
- `html(Descent)`
- `html(String)`
- `html(FilePosition)`
- `html(Net)`
- `html(Holder)`
- `html(RowExpression)`
- `html(GroebnerBasis)`
- `html(Monoid)`
- `html(RingFamily)`
- `html(Ring)`
- `html(Thing)`
- `html(Bag)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

