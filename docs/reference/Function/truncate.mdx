---
title: "truncate"
kind: Function
---

# truncate

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

truncate, , Description, This function is a method function, defined in the core so multiple packages can add methods to it., See also, Truncations :: Truncations, class => waystouse, Ways to use , truncate, :, Macaulay2Doc :: truncate(BettiTally,InfiniteNumber,InfiniteNumber), Macaulay2Doc :: truncate(BettiTally,InfiniteNumber,ZZ), Macaulay2Doc :: truncate(BettiTally,ZZ,InfiniteNumber), Macaulay2Doc :: truncate(BettiTally,ZZ,ZZ), Truncations :: truncate(List,Matrix), truncate(ZZ,Matrix), -- see , Truncations :: truncate(List,Matrix), truncate(List,Ideal), -- see , Truncations :: truncate(List,Module), Truncations :: truncate(List,Module), truncate(List,Ring), -- see , Truncations :: truncate(List,Module), truncate(ZZ,Ideal), -- see , Truncations :: truncate(List,Module), truncate(ZZ,Module), -- see , Truncations :: truncate(List,Module), truncate(ZZ,Ring), -- see , Truncations :: truncate(List,Module), Macaulay2Doc :: truncate(Number), class => waystouse, For the programmer, The object , Macaulay2Doc :: truncate, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/shared.m2:24:0, .

## Installations

Type signatures for this function:

- `truncate(List, List, Matrix)`
- `truncate(ZZ, Matrix)`
- `truncate(List, Ring)`
- `truncate(Nothing, List, Matrix)`
- `truncate(Number)`
- `truncate(List, Matrix)`
- `truncate(ZZ, Module)`
- `truncate(ZZ, ZZ, Matrix)`
- `truncate(InfiniteNumber, InfiniteNumber, Matrix)`
- `truncate(ZZ, Ideal)`
- `truncate(List, Module)`
- `truncate(List, Ideal)`
- `truncate(ZZ, Ring)`
- `truncate(Nothing, InfiniteNumber, Matrix)`
- `truncate(Nothing, ZZ, Matrix)`
- `truncate(InfiniteNumber, Thing)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

