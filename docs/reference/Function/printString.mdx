---
title: "printString"
kind: Function
---

# printString

**printString  -- lowlevel function to print a string net or symbol**

**Type:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

`printString(os)`  -- send the string `s`  to the output file `o` . This function is intended for internal use only. The argument `s`  may also be a sequence or list in which case its elements are printed.  If an integer is encountered then it specifies a number of spaces to be printed.  If a symbol or indeterminate is encountered its name is printed.  If [null](../Instance/null.mdx)  is encountered nothing is printed.

## Examples

```macaulay2
printString(stdio (a10"b"20c))
a          b                    c
```

```text
stdio

File
```

## Debug info

```python
{
    "obj_type": "Function",
    "name": "printString",
    "safe_name": "printString",
    "type": "CompiledFunction",
    "description": "`printString(os)`  -- send the string `s`  to the output file `o` . This function is intended for in...",
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "printString",
        "full_doc": "... (1055 bytes) ...",
        "parsed_headline": "printString  -- lowlevel function to print a string net or symbol"
    }
}
```
