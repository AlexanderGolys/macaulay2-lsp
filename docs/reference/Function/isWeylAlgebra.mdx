---
title: "isWeylAlgebra"
kind: Function
---

# isWeylAlgebra

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

isWeylAlgebra, , Description, A Weyl algebra is the non-commutative algebra of algebraic differential operators on a polynomial ring. To each variable , x, corresponds the operator , dx, that differentiates with respect to that variable. The evident commutation relation takes the form , dx*x == x*dx + 1, ., , We can give any names we like to the variables in a Weyl algebra, provided we specify the correspondence between the variables and the derivatives, with the , Macaulay2Doc :: WeylAlgebra, option, as follows., , class => examples, i1 : R = QQ[x,y,dx,dy,t,WeylAlgebra => {x=>dx, y=>dy]

o1 = R

o1 : PolynomialRing, 2 differential variable(s), class => language-macaulay2, i2 : dx*dy*x*y

o2 = x*y*dx*dy + x*dx + y*dy + 1

o2 : R, class => language-macaulay2, i3 : dx*x^5

 5 4
o3 = x dx + 5x

o3 : R, class => language-macaulay2, All modules over Weyl algebras are, in Macaulay2, right modules. This means that multiplication of matrices is from the opposite side:, class => examples, i4 : dx*x

o4 = x*dx + 1

o4 : R, class => language-macaulay2, i5 : matrix{{dx * matrix{{x

o5 = | xdx |

 1 1
o5 : Matrix R <-- R, class => language-macaulay2, All Gröbner basis and related computations work over this ring. For an extensive collection of D-module routines (A D-module is a module over a Weyl algebra), see , Dmodules :: Dmodules, ., The function , isWeylAlgebra, can be used to determine whether a polynomial ring has been constructed as a Weyl algebra., class => examples, i6 : isWeylAlgebra R

o6 = true, class => language-macaulay2, i7 : S = QQ[x,y]

o7 = S

o7 : PolynomialRing, class => language-macaulay2, i8 : isWeylAlgebra S

o8 = false, class => language-macaulay2, class => waystouse, Ways to use , isWeylAlgebra, :, isWeylAlgebra(PolynomialRing), isWeylAlgebra(QuotientRing), isWeylAlgebra(Ring), class => waystouse, For the programmer, The object , Macaulay2Doc :: isWeylAlgebra, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/ov_rings.m2:1347:0, .

## Installations

Type signatures for this function:

- `isWeylAlgebra(PolynomialRing)`
- `isWeylAlgebra(QuotientRing)`
- `isWeylAlgebra(Ring)`

## Examples

```macaulay2
i2 : dx*dy*x*y

o2 = x*y*dx*dy + x*dx + y*dy + 1

o2 : R
```

## See Also

