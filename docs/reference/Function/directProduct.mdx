---
title: "directProduct"
kind: Function
---

# directProduct

**Type:** `Function`

**Instance of:** [MethodFunctionBinary](../Type/MethodFunctionBinary.mdx)

## Description

directProduct, -- direct product, Description, This function calculates the direct product of a list or sequence of compatible objects., This is a binary method, meaning it is defined for two inputs but can accept more. When three or more arguments are given, the direct product is evaluated from left to right: the direct product of the first two inputs is computed, then the result is used to compute the direct product with the third input, and so on., See also, Polyhedra :: directProduct(Cone,Cone), Polyhedra :: directProduct(Fan,Fan), Graphs :: directProduct(Graph,Graph), class => waystouse, Ways to use , directProduct, :, directProduct(List), directProduct(Sequence), Polyhedra :: directProduct(Cone,Cone), directProduct(Cone,Polyhedron), -- see , Polyhedra :: directProduct(Cone,Cone), directProduct(Polyhedron,Cone), -- see , Polyhedra :: directProduct(Cone,Cone), directProduct(Polyhedron,Polyhedron), -- see , Polyhedra :: directProduct(Cone,Cone), Polyhedra :: directProduct(Fan,Fan), class => waystouse, For the programmer, The object , Macaulay2Doc :: directProduct, is , an , Macaulay2Doc :: MethodFunctionBinary, associative binary method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/shared.m2:173:0, .

## Installations

Type signatures for this function:

- `directProduct(Sequence)`
- `directProduct(List)`
- `directProduct(Polyhedron, Polyhedron)`
- `directProduct(Cone, Cone)`
- `directProduct(Cone, Polyhedron)`
- `directProduct(Polyhedron, Cone)`
- `directProduct(Fan, Fan)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

