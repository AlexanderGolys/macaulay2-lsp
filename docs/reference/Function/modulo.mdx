---
title: "modulo"
kind: Function
---

# modulo

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

modulo, -- find the pre-image (pullback) of image of a map (low level version), class => element, Usage: , modulo(f,g), class => language-macaulay2, Inputs:, f, g, Outputs:, a , Macaulay2Doc :: Matrix, matrix, , , whose image is the pre-image (pullback) of the image of , g, under , f, Description, The maps , f, and , g, must have the same target, and their sources and targets must be free. If , f, is , Macaulay2Doc :: null, , then it is taken to be the identity. If , g, is , Macaulay2Doc :: null, , it is taken to be zero., This function is mainly for internal use., class => examples, i1 : R = QQ[x,y,z]

o1 = R

o1 : PolynomialRing, class => language-macaulay2, i2 : f = matrix {{x,y

o2 = | x y |

 1 2
o2 : Matrix R <-- R, class => language-macaulay2, i3 : g = matrix {{y,z

o3 = | y z |

 1 2
o3 : Matrix R <-- R, class => language-macaulay2, i4 : modulo(f,g)

o4 = {1 | 0 z y |
 {1 | 1 0 0 |

 2 3
o4 : Matrix R <-- R, class => language-macaulay2, i5 : kernel( inducedMap(coker g, target g) * f )

o5 = image {1 | 0 z y |
 {1 | 1 0 0 |

 2
o5 : R-module, submodule of R, class => language-macaulay2, class => waystouse, Ways to use , modulo, :, modulo(Matrix,Matrix), modulo(Matrix,Nothing), modulo(Nothing,Matrix), class => waystouse, For the programmer, The object , Macaulay2Doc :: modulo, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/modulo-doc.m2:27:0, .

## Installations

Type signatures for this function:

- `modulo(Matrix, Nothing)`
- `modulo(Nothing, Matrix)`
- `modulo(Matrix, Matrix)`

## Examples

```macaulay2
i1 : R = QQ[x,y,z]

o1 = R

o1 : PolynomialRing
```

## See Also

