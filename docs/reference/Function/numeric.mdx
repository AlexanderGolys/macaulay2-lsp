---
title: "numeric"
kind: Function
---

# numeric

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

numeric, -- convert to floating point, class => element, Usage: , numeric x, class => language-macaulay2, numeric(prec,x), class => language-macaulay2, numeric_prec x, class => language-macaulay2, Inputs:, x, prec, , , an , Macaulay2Doc :: ZZ, integer, , , the number of bits of precision to use in the computation, Outputs:, the expression obtained from , x, by converting the integers, rational numbers, and symbolic constants within it to double floating point numbers., Description, class => examples, i1 : x = {1,2/3,pi

 2
o1 = {1, -, pi
 3

o1 : List, class => language-macaulay2, i2 : numeric oo

o2 = {1, .666667, 3.14159

o2 : List, class => language-macaulay2, i3 : numeric_200 pi

o3 = 3.141592653589793238462643383279502884197169399375105820974944

o3 : RR (of precision 200), class => language-macaulay2, i4 : numeric_100 oo

o4 = 3.141592653589793238462643383279

o4 : RR (of precision 100), class => language-macaulay2, See also, Macaulay2Doc :: RR, Macaulay2Doc :: CC, Macaulay2Doc :: Constant, class => waystouse, Ways to use , numeric, :, numeric(CC), numeric(Constant), numeric(IndeterminateNumber), numeric(InfiniteNumber), numeric(InfiniteNumber,Constant), numeric(Matrix), numeric(Number), numeric(RR), numeric(RRi), numeric(Vector), numeric(VisibleList), numeric(ZZ,CC), numeric(ZZ,Constant), numeric(ZZ,IndeterminateNumber), numeric(ZZ,InfiniteNumber), numeric(ZZ,Matrix), numeric(ZZ,Number), numeric(ZZ,RRi), numeric(ZZ,Vector), numeric(ZZ,VisibleList), class => waystouse, For the programmer, The object , Macaulay2Doc :: numeric, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/doc_arithmetic.m2:55:0, .

## Installations

Type signatures for this function:

- `numeric(ZZ, Matrix)`
- `numeric(Matrix)`
- `numeric(ZZ, IndeterminateNumber)`
- `numeric(IndeterminateNumber)`
- `numeric(Vector)`
- `numeric(ZZ, Vector)`
- `numeric(VisibleList)`
- `numeric(ZZ, VisibleList)`
- `numeric(Number)`
- `numeric(ZZ, RRi)`
- `numeric(InfiniteNumber)`
- `numeric(ZZ, InfiniteNumber)`
- `numeric(Constant)`
- `numeric(ZZ, Constant)`
- `numeric(InfiniteNumber, Constant)`
- `numeric(ZZ, CC)`
- `numeric(RR)`
- `numeric(RRi)`
- `numeric(ZZ, Number)`
- `numeric(CC)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

