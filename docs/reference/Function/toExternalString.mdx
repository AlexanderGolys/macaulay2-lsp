---
title: "toExternalString"
kind: Function
---

# toExternalString

**Type:** `Function`

**Instance of:** [MethodFunctionSingle](../Type/MethodFunctionSingle.mdx)

## Description

toExternalString, -- convert to a readable string, class => element, Usage: , toExternalString x, class => language-macaulay2, Inputs:, x, , , any Macaulay2 object, Outputs:, a , Macaulay2Doc :: String, string, , , a string representation of , x, , which can be used, in conjunction with , Macaulay2Doc :: value, , to read the object back into the program later, Description, See also , Macaulay2Doc :: toString, which simply converts , x, to a string that can be displayed meaningfully., class => examples, i1 : toString {1,4,a,f,212312,2.123243242

o1 = {1, 4, a, f, 212312, 2.12324, class => language-macaulay2, class => examples, i2 : R = QQ[x_1..x_5];, class => language-macaulay2, i3 : toExternalString R

o3 = QQ[x_1..x_5, Degrees => {5:1, Heft => {1], class => language-macaulay2, Matrices and ring elements are linearized, class => examples, i4 : x_1^3-3/4*x_5*x_3

 3 3
o4 = x - -x x
 1 4 3 5

o4 : R, class => language-macaulay2, i5 : toExternalString oo

o5 = x_1^3-(3/4)*x_3*x_5, class => language-macaulay2, i6 : value oo

 3 3
o6 = x - -x x
 1 4 3 5

o6 : R, class => language-macaulay2, Caveat, Not everything can be converted to a string in such a way that it can be read back into the program later., See also, Macaulay2Doc :: toString, Macaulay2Doc :: value, class => waystouse, Ways to use , toExternalString, :, Varieties :: toExternalString(SheafMap), Varieties :: toExternalString(Variety), class => waystouse, For the programmer, The object , Macaulay2Doc :: toExternalString, is , a , Macaulay2Doc :: MethodFunctionSingle, method function with a single argument, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/toExternalString-doc.m2:34:0, .

## Installations

Type signatures for this function:

- `toExternalString(MonoidElement)`
- `toExternalString(RingMap)`
- `toExternalString(Variety)`
- `toExternalString(Vector)`
- `toExternalString(Ring)`
- `toExternalString(Monoid)`
- `toExternalString(SheafMap)`
- `toExternalString(FilePosition)`
- `toExternalString(DocumentTag)`
- `toExternalString(RingElement)`
- `toExternalString(RRi)`
- `toExternalString(Matrix)`
- `toExternalString(Ideal)`
- `toExternalString(InfiniteNumber)`
- `toExternalString(IndeterminateNumber)`
- `toExternalString(Keyword)`
- `toExternalString(Symbol)`
- `toExternalString(Thing)`
- `toExternalString(Option)`
- `toExternalString(Module)`
- `toExternalString(MonomialIdeal)`
- `toExternalString(Command)`
- `toExternalString(Function)`
- `toExternalString(Manipulator)`
- `toExternalString(Net)`
- `toExternalString(MutableMatrix)`
- `toExternalString(MutableHashTable)`
- `toExternalString(MutableList)`
- `toExternalString(HashTable)`
- `toExternalString(BasicList)`

*... and 9 more*

## Examples

```macaulay2
i2 : R = QQ[x_1..x_5];
```

## See Also

