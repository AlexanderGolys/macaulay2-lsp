---
title: "numgens"
kind: Function
---

# numgens

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

numgens, -- the number of generators, empty documentation body, class => waystouse, Ways to use , numgens, :, Varieties :: numgens(CoherentSheaf), Macaulay2Doc :: numgens(Ideal), Macaulay2Doc :: numgens(Module), Macaulay2Doc :: numgens(Monoid), numgens(EngineRing), -- see , Macaulay2Doc :: numgens(Ring), numgens(FractionField), -- see , Macaulay2Doc :: numgens(Ring), numgens(MonomialIdeal), -- see , Macaulay2Doc :: numgens(Ring), numgens(PolynomialRing), -- see , Macaulay2Doc :: numgens(Ring), numgens(QuotientRing), -- see , Macaulay2Doc :: numgens(Ring), Macaulay2Doc :: numgens(Ring), class => waystouse, For the programmer, The object , Macaulay2Doc :: numgens, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/numgens-doc.m2:12:0, .

## Installations

Type signatures for this function:

- `numgens(PolynomialRing)`
- `numgens(FractionField)`
- `numgens(EngineRing)`
- `numgens(Ideal)`
- `numgens(QuotientRing)`
- `numgens(Module)`
- `numgens(MonomialIdeal)`
- `numgens(Monoid)`
- `numgens(InexactField)`
- `numgens(Ring)`
- `numgens(CoherentSheaf)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

