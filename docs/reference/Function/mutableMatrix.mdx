---
title: "mutableMatrix"
kind: Function
---

# mutableMatrix

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

mutableMatrix, -- make a mutable matrix, class => element, Usage: , mutableMatrix m, class => language-macaulay2, Inputs:, m, , , a , Macaulay2Doc :: Matrix, matrix, , , a , Macaulay2Doc :: MutableMatrix, mutable matrix, , or , a , Macaulay2Doc :: List, list, Macaulay2Doc :: using functions with optional inputs, Optional inputs, :, Dense, => , ..., , , default value , true, , , whether the encoding of the matrix should be dense or not: see , Macaulay2Doc :: MutableMatrix, Outputs:, a new mutable matrix whose entries are obtained from , m, . If , m, is a list, it should be a doubly nested list (table) of ring elements, all from the same ring., Description, class => examples, i1 : f = mutableMatrix {{1,2,3,4

o1 = | 1 2 3 4 |

o1 : MutableMatrix, class => language-macaulay2, i2 : f_(0,2)

o2 = 3, class => language-macaulay2, i3 : f_(0,2) = 33

o3 = 33, class => language-macaulay2, i4 : f

o4 = | 1 2 33 4 |

o4 : MutableMatrix, class => language-macaulay2, i5 : R = QQ[a..z]

o5 = R

o5 : PolynomialRing, class => language-macaulay2, i6 : mutableMatrix genericMatrix(R,3,3)

o6 = | a d g |
 | b e h |
 | c f i |

o6 : MutableMatrix, class => language-macaulay2, class => waystouse, Ways to use , mutableMatrix, :, mutableMatrix(List), mutableMatrix(Matrix), mutableMatrix(MutableMatrix), mutableMatrix(Ring,List), mutableMatrix(RingFamily,List), Macaulay2Doc :: mutableMatrix(Ring,ZZ,ZZ), mutableMatrix(RingFamily,ZZ,ZZ), -- see , Macaulay2Doc :: mutableMatrix(Ring,ZZ,ZZ), class => waystouse, For the programmer, The object , Macaulay2Doc :: mutableMatrix, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/doc3.m2:32:0, .

## Installations

Type signatures for this function:

- `mutableMatrix(Matrix)`
- `mutableMatrix(Ring, List)`
- `mutableMatrix(RingFamily, List)`
- `mutableMatrix(List)`
- `mutableMatrix(MutableMatrix)`
- `mutableMatrix(Ring, ZZ, ZZ)`
- `mutableMatrix(RingFamily, ZZ, ZZ)`

## Examples

```macaulay2
i2 : f_(0,2)

o2 = 3
```

## See Also

