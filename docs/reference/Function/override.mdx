---
title: "override"
kind: Function
---

# override

**override  -- override default values for optional arguments**

**Type:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

`override(defaultsargs)`  overrides default values for optional arguments present in the argument sequence `args` .One possibility is for the argument `defaults`  to be an immutable hash table (of type [OptionTable](../Type/OptionTable.mdx) ) and `args`  should be a sequence of arguments some of which are optional arguments of the form `` .  Each such optional argument is removed from `args`  and the value in `defaults`  corresponding to the key `x`  is replaced by `v` . The value returned is the modified pair `(defaults args)` . An error is signalled if the key `x`  does not occur in `defaults` .A second possibility is for the argument `defaults`  to be [null](../Instance/null.mdx)  in which case the keys x are not checked for validity and no default values are provided.  The main use of this is to separate the optional arguments from the other arguments which can then be used for dispatching to the correct method.This function is intended for internal use only and is used in the processing of optional arguments for method functions that accept them.

## Examples

```macaulay2
override(defs (4 (4
```

## Debug info

```python
{
    "obj_type": "Function",
    "name": "override",
    "safe_name": "override",
    "type": "CompiledFunction",
    "description": "`override(defaultsargs)`  overrides default values for optional arguments present in the argument se...",
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "override",
        "full_doc": "... (1862 bytes) ...",
        "parsed_headline": "override  -- override default values for optional arguments"
    }
}
```
