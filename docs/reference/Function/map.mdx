---
title: "map"
kind: Function
---

# map

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

map, -- make a map, class => element, Usage: , map(Y,X,d) or map(Y,X), class => language-macaulay2, Inputs:, Y, , , an object, such as a ring, module, or chain complex, X, , , another object of the same type, d, , , a specification, such as a function, list, or matrix, or if omitted, understood to specify the identity map, Macaulay2Doc :: using functions with optional inputs, Optional inputs, :, Macaulay2Doc :: map(...,Degree=>...), Degree, => , ..., , , default value , null, , , specify the degree of a map, Macaulay2Doc :: map(...,DegreeLift=>...), DegreeLift, => , ..., , , default value , null, , , make a ring map, Macaulay2Doc :: map(...,DegreeMap=>...), DegreeMap, => , ..., , , default value , null, , , make a ring map, Outputs:, a map to , Y, from , X, defined by data , d, ., Description, The function , map, provides a general mechanism for constructing a map (homomorphism) between rings (, a , Macaulay2Doc :: RingMap, ring map, ), modules (, a , Macaulay2Doc :: Matrix, matrix, ), or between objects in other categories., , See also the function , Macaulay2Doc :: matrix, , which focuses on creating new matrices from rectangular arrays of ring elements or matrices., Getting information about a map, Macaulay2Doc :: source, Macaulay2Doc :: target, Creating a map (matrix) between modules, Macaulay2Doc :: map(Module,Module,Function), Macaulay2Doc :: map(Module,Module,List), Macaulay2Doc :: map(Module,Module,RingElement), Macaulay2Doc :: map(Module,Module,Matrix), Creating a map between modules, where the source module is computed, Macaulay2Doc :: map(Module,ZZ,Function), Macaulay2Doc :: map(Module,Nothing,List), Macaulay2Doc :: map(Module,ZZ,List), Macaulay2Doc :: map(Module,Nothing,Matrix), Creating a map with a different degree, Macaulay2Doc :: map(Matrix), Creating a map between modules over different rings, Macaulay2Doc :: map(Module,Module,RingMap,Matrix), Creating a map between rings, Macaulay2Doc :: map(Ring,Ring), Macaulay2Doc :: map(Ring,Ring,List), Macaulay2Doc :: map(Ring,Ring,Matrix), The Hom and End modules, Macaulay2Doc :: Hom, Macaulay2Doc :: End, class => waystouse, Ways to use , map, :, OldChainComplexes :: map(ChainComplex,ChainComplex,ChainComplexMap), OldChainComplexes :: map(ChainComplex,ChainComplex,Function), map(GradedModule,GradedModule,Function), -- see , OldChainComplexes :: map(ChainComplex,ChainComplex,Function), Varieties :: map(CoherentSheaf,CoherentSheaf,Matrix), map(CoherentSheaf,CoherentSheaf,Matrix,InfiniteNumber), -- see , Varieties :: map(CoherentSheaf,CoherentSheaf,Matrix), map(CoherentSheaf,CoherentSheaf,Matrix,ZZ), -- see , Varieties :: map(CoherentSheaf,CoherentSheaf,Matrix), map(CoherentSheaf,Nothing,Matrix), -- see , Varieties :: map(CoherentSheaf,CoherentSheaf,Matrix), map(Nothing,CoherentSheaf,Matrix), -- see , Varieties :: map(CoherentSheaf,CoherentSheaf,Matrix), Varieties :: map(CoherentSheaf,CoherentSheaf,SheafMap), Varieties :: map(CoherentSheaf,CoherentSheaf,ZZ), Varieties :: map(CoherentSheaf,Module,ZZ), Macaulay2Doc :: map(GaloisField,GaloisField), Macaulay2Doc :: map(Matrix), Macaulay2Doc :: map(Module), Varieties :: map(Module,CoherentSheaf,ZZ), Macaulay2Doc :: map(Module,Module,Function), Macaulay2Doc :: map(Module,Module,List), Macaulay2Doc :: map(Module,Module,Matrix), map(Module,Module,Number), -- see , Macaulay2Doc :: map(Module,Module,RingElement), Macaulay2Doc :: map(Module,Module,RingElement), map(Module,Module,ZZ), -- see , Macaulay2Doc :: map(Module,Module,RingElement), map(Module,ZZ,ZZ), -- see , Macaulay2Doc :: map(Module,Module,RingElement), map(Module,Module,RingMap,List), -- see , Macaulay2Doc :: map(Module,Module,RingMap,Matrix), Macaulay2Doc :: map(Module,Module,RingMap,Matrix), map(Module,Nothing,RingMap,List), -- see , Macaulay2Doc :: map(Module,Module,RingMap,Matrix), map(Module,Nothing,RingMap,Matrix), -- see , Macaulay2Doc :: map(Module,Module,RingMap,Matrix), map(Module,RingMap), -- see , Macaulay2Doc :: map(Module,Module,RingMap,Matrix), Macaulay2Doc :: map(Module,Nothing,List), Macaulay2Doc :: map(Module,Nothing,Matrix), Macaulay2Doc :: map(Module,ZZ,Function), Macaulay2Doc :: map(Module,ZZ,List), Macaulay2Doc :: map(Ring,Matrix), Macaulay2Doc :: map(Ring,Ring), Macaulay2Doc :: map(Ring,Ring,List), Macaulay2Doc :: map(Ring,Ring,Matrix), map(Ring,Ring,RingMap), -- see , Macaulay2Doc :: map(Ring,Ring,Matrix), class => waystouse, For the programmer, The object , Macaulay2Doc :: map, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/map-doc.m2:65:0, .

## Installations

Type signatures for this function:

- `map(Module, Module, RawMatrix)`
- `map(Module, Nothing, RawMatrix)`
- `map(Module)`
- `map(Module, ZZ, ZZ)`
- `map(Module, Module, ZZ)`
- `map(Module, Module, Number)`
- `map(Module, Module, RingElement)`
- `map(Module, Module, Function)`
- `map(Matrix)`
- `map(Module, ZZ, Function)`
- `map(Module, Nothing, Matrix)`
- `map(Module, Module, Matrix)`
- `map(Module, Module, List)`
- `map(Module, Nothing, List)`
- `map(Module, ZZ, List)`
- `map(RingFamily, Thing, Thing)`
- `map(Thing, RingFamily, Thing)`
- `map(Ring, Ring)`
- `map(Ring, Ring, RingMap)`
- `map(Ring, Ring, List)`
- `map(Ring, Matrix)`
- `map(Ring, Ring, Matrix)`
- `map(Module, Module, Nothing, RawMatrix)`
- `map(Module, Module, RingMap, RawMatrix)`
- `map(Module, Nothing, RingMap, RawMatrix)`
- `map(Module, Nothing, RingMap, Matrix)`
- `map(Module, Module, RingMap, Matrix)`
- `map(Module, Module, RingMap, List)`
- `map(Module, Nothing, RingMap, List)`
- `map(Module, RingMap)`

*... and 13 more*

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

