---
title: "singularLocus"
kind: Function
---

# singularLocus

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

singularLocus, -- singular locus, Description, singularLocus R, -- produce the singular locus of a ring, which is assumed to be integral., , This function can also be applied to an ideal, in which case the singular locus of the quotient ring is returned, or to a variety., class => examples, i1 : singularLocus(QQ[x,y] / (x^2 - y^3))

 QQ[x..y]
o1 = ---------------------
 3 2 2
 (- y + x , 2x, -3y )

o1 : QuotientRing, class => language-macaulay2, i2 : singularLocus Spec( QQ[x,y,z] / (x^2 - y^3) )

 / QQ[x..z] \
o2 = Spec|---------------------|
 | 3 2 2 |
 \(- y + x , 2x, -3y )/

o2 : AffineVariety, class => language-macaulay2, i3 : singularLocus Proj( QQ[x,y,z] / (x^2*z - y^3) )

 /QQ[x..z]\
o3 = Proj|--------|
 | 2 |
 \ (x, y )/

o3 : ProjectiveVariety, class => language-macaulay2, For rings over , Macaulay2Doc :: ZZ, the locus where the ring is not smooth over , Macaulay2Doc :: ZZ, is computed., class => examples, i4 : singularLocus(ZZ[x,y]/(x^2-x-y^3+y^2))

 ZZ[x..y]
o4 = ----------------------------------------
 3 2 2 2
 (- y + x + y - x, 2x - 1, - 3y + 2y)

o4 : QuotientRing, class => language-macaulay2, i5 : gens gb ideal oo

o5 = | 11 y+3 x+5 |

 1 3
o5 : Matrix (ZZ[x..y]) <-- (ZZ[x..y]), class => language-macaulay2, class => waystouse, Ways to use , singularLocus, :, singularLocus(Ideal), singularLocus(Ring), Varieties :: singularLocus(AffineVariety), singularLocus(ProjectiveVariety), -- see , Varieties :: singularLocus(AffineVariety), class => waystouse, For the programmer, The object , Macaulay2Doc :: singularLocus, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/doc7.m2:181:0, .

## Installations

Type signatures for this function:

- `singularLocus(Ring)`
- `singularLocus(Ideal)`
- `singularLocus(AffineVariety)`
- `singularLocus(ProjectiveVariety)`

## Examples

```macaulay2
i1 : singularLocus(QQ[x,y] / (x^2 - y^3))

            QQ[x..y]
o1 = ---------------------
         3    2         2
     (- y  + x , 2x, -3y )

o1 : QuotientRing

i2 : singularLocus Spec( QQ[x,y,z] / (x^2 - y^3) )

         /       QQ[x..z]      \
o2 = Spec|---------------------|
         |    3    2         2 |
         \(- y  + x , 2x, -3y )/

o2 : AffineVariety
```

## See Also

