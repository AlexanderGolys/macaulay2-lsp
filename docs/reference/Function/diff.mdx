---
title: "diff"
kind: Function
---

# diff

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

diff, -- differentiate or take difference, class => element, Usage: , diff(f,g) or diff(P) or diff(P,i), class => language-macaulay2, Description, This function has two different uses. The most common use is for differentiation: differentiate the second input by the first., , The second use, less common but sometimes useful, is to compute the difference polynomial of a Hilbert polynomial., , The arguments can also be ring elements or vectors., class => examples, i1 : R = ZZ[x,y,z]

o1 = R

o1 : PolynomialRing, class => language-macaulay2, i2 : f = vars R ** vars R

o2 = | x2 xy xz xy y2 yz xz yz z2 |

 1 9
o2 : Matrix R <-- R, class => language-macaulay2, i3 : diff(transpose vars R, f)

o3 = {1 | 2x y z y 0 0 z 0 0 |
 {1 | 0 x 0 x 2y z 0 z 0 |
 {1 | 0 0 x 0 0 y x y 2z |

 3 9
o3 : Matrix R <-- R, class => language-macaulay2, i4 : diff(x, f)

o4 = | 2x y z y 0 0 z 0 0 |

 1 9
o4 : Matrix R <-- R, class => language-macaulay2, i5 : diff(y, f)

o5 = | 0 x 0 x 2y z 0 z 0 |

 1 9
o5 : Matrix R <-- R, class => language-macaulay2, i6 : diff(z, f)

o6 = | 0 0 x 0 0 y x y 2z |

 1 9
o6 : Matrix R <-- R, class => language-macaulay2, See also, Macaulay2Doc :: diff and contract, Macaulay2Doc :: diff', Macaulay2Doc :: contract, Macaulay2Doc :: jacobian, Macaulay2Doc :: hilbertPolynomial, differentiation, Macaulay2Doc :: diff(RingElement,RingElement), Macaulay2Doc :: diff(RingElement,Matrix), Macaulay2Doc :: diff(Matrix,RingElement), Macaulay2Doc :: diff(Matrix,Matrix), difference operator for Hilbert polynomials, Macaulay2Doc :: diff(ProjectiveHilbertPolynomial), Macaulay2Doc :: diff(ProjectiveHilbertPolynomial,ZZ), class => waystouse, Ways to use , diff, :, Macaulay2Doc :: diff(Matrix,Matrix), diff(Matrix,Vector), -- see , Macaulay2Doc :: diff(Matrix,Matrix), diff(RingElement,Vector), -- see , Macaulay2Doc :: diff(Matrix,Matrix), diff(Vector,Matrix), -- see , Macaulay2Doc :: diff(Matrix,Matrix), diff(Vector,RingElement), -- see , Macaulay2Doc :: diff(Matrix,Matrix), diff(Vector,Vector), -- see , Macaulay2Doc :: diff(Matrix,Matrix), Macaulay2Doc :: diff(Matrix,RingElement), Macaulay2Doc :: diff(ProjectiveHilbertPolynomial), Macaulay2Doc :: diff(ProjectiveHilbertPolynomial,ZZ), Macaulay2Doc :: diff(RingElement,Matrix), Macaulay2Doc :: diff(RingElement,RingElement), class => waystouse, For the programmer, The object , Macaulay2Doc :: diff, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/diff-doc.m2:165:0, .

## Installations

Type signatures for this function:

- `diff(Matrix, Matrix)`
- `diff(ProjectiveHilbertPolynomial, ZZ)`
- `diff(ProjectiveHilbertPolynomial)`
- `diff(RingElement, RingElement)`
- `diff(Matrix, RingElement)`
- `diff(RingElement, Matrix)`
- `diff(Vector, RingElement)`
- `diff(RingElement, Vector)`
- `diff(Vector, Vector)`
- `diff(Matrix, Vector)`
- `diff(Vector, Matrix)`

## Examples

```macaulay2
i1 : R = ZZ[x,y,z]

o1 = R

o1 : PolynomialRing

i2 : f = vars R ** vars R

o2 = | x2 xy xz xy y2 yz xz yz z2 |

             1      9
o2 : Matrix R  <-- R
```

## See Also

