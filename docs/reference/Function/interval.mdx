---
title: "interval"
kind: Function
---

# interval

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

interval, -- construct an interval, class => element, Usage: , I = interval(n), class => language-macaulay2, I = interval(l,r), class => language-macaulay2, I = interval([l,r]), class => language-macaulay2, I = interval(n,Precition => prec), class => language-macaulay2, I = interval(l,r,Precition => prec), class => language-macaulay2, I = interval([l,r],Precition => prec), class => language-macaulay2, Inputs:, n, , , a , Macaulay2Doc :: Number, number, , , l, , , a , Macaulay2Doc :: Number, number, , , r, , , a , Macaulay2Doc :: Number, number, , , Macaulay2Doc :: using functions with optional inputs, Optional inputs, :, Precision, => , an , Macaulay2Doc :: ZZ, integer, , , default value , -1, , , specifies the desired precision of the output, a value of , -1, uses the , Macaulay2Doc :: defaultPrecision, or the minimum precisions of the inputs., Outputs:, I, , , a , Macaulay2Doc :: RRi, real interval, , , Description, Returns an interval as small as possible containing , n, or from , l, to , r, . Note that if , l, is to the right of , r, , the constructed interval is empty., See also, Macaulay2Doc :: span(List), Macaulay2Doc :: span(Sequence), Macaulay2Doc :: toRRi, class => waystouse, Ways to use , interval, :, interval(Array), interval(QQ), interval(QQ,QQ), interval(QQ,RR), interval(QQ,ZZ), interval(RR), interval(RR,QQ), interval(RR,RR), interval(RR,ZZ), interval(ZZ), interval(ZZ,QQ), interval(ZZ,RR), interval(ZZ,ZZ), class => waystouse, For the programmer, The object , Macaulay2Doc :: interval, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/doc_intervals.m2:273:0, .

## Installations

Type signatures for this function:

- `interval(RR)`
- `interval(QQ)`
- `interval(ZZ)`
- `interval(RR, RR)`
- `interval(RR, QQ)`
- `interval(QQ, RR)`
- `interval(ZZ, RR)`
- `interval(RR, ZZ)`
- `interval(QQ, QQ)`
- `interval(QQ, ZZ)`
- `interval(ZZ, QQ)`
- `interval(ZZ, ZZ)`
- `interval(Array)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

