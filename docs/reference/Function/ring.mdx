---
title: "ring"
kind: Function
---

# ring

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

ring, -- get the associated ring of an object, class => element, Usage: , ring M, class => language-macaulay2, Inputs:, M, , , an object with a ring associated to it, Outputs:, a , Macaulay2Doc :: Ring, ring, , , associated to the input object, Description, For example, ring elements, matrices, ideals, modules, chain complexes, varieties, coherent sheaves, etc., all have a base ring naturally associated to them. , class => examples, i1 : R = ZZ/101[x,y,z];, class => language-macaulay2, i2 : ring x

o2 = R

o2 : PolynomialRing, class => language-macaulay2, i3 : M = matrix {{2*x, x+y,{y^3, z*y;

 2 2
o3 : Matrix R <-- R, class => language-macaulay2, i4 : ring M

o4 = R

o4 : PolynomialRing, class => language-macaulay2, i5 : S = QQ[x,y,z];, class => language-macaulay2, i6 : ring x

o6 = S

o6 : PolynomialRing, class => language-macaulay2, i7 : I = ideal (x*y, y*z);

o7 : Ideal of S, class => language-macaulay2, i8 : ring I

o8 = S

o8 : PolynomialRing, class => language-macaulay2, See also, Macaulay2Doc :: baseRings, Macaulay2Doc :: coefficientRing, class => waystouse, Ways to use , ring, :, ring(CC), ring(GroebnerBasis), ring(Ideal), ring(Matrix), ring(Module), ring(MutableMatrix), ring(Number), ring(RingElement), ring(RR), ring(RRi), ring(Vector), OldChainComplexes :: ring(ChainComplex), OldChainComplexes :: ring(ChainComplexMap), Varieties :: ring(CoherentSheaf), ring(SheafMap), -- see , Varieties :: ring(CoherentSheaf), ring(SheafOfRings), -- see , Varieties :: ring(CoherentSheaf), ring(SumOfTwists), -- see , Varieties :: ring(CoherentSheaf), Macaulay2Doc :: ring(Constant), OldChainComplexes :: ring(GradedModule), OldChainComplexes :: ring(GradedModuleMap), OldChainComplexes :: ring(Resolution), Varieties :: ring(Variety), class => waystouse, For the programmer, The object , Macaulay2Doc :: ring, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/ring-doc.m2:33:0, .

## Installations

Type signatures for this function:

- `ring(Constant)`
- `ring(RingElement)`
- `ring(GroebnerBasis)`
- `ring(Ideal)`
- `ring(Matrix)`
- `ring(Vector)`
- `ring(Module)`
- `ring(MutableMatrix)`
- `ring(RR)`
- `ring(RRi)`
- `ring(CC)`
- `ring(ChainComplex)`
- `ring(Resolution)`
- `ring(ChainComplexMap)`
- `ring(GradedModule)`
- `ring(GradedModuleMap)`
- `ring(Variety)`
- `ring(CoherentSheaf)`
- `ring(SheafOfRings)`
- `ring(SumOfTwists)`
- `ring(SheafMap)`
- `ring(Number)`

## Examples

```macaulay2
i1 : R = ZZ/101[x,y,z];

i2 : ring x

o2 = R

o2 : PolynomialRing
```

## See Also

