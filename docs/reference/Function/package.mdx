---
title: "package"
kind: Function
---

# package

**Type:** `Function`

**Instance of:** [MethodFunctionSingle](../Type/MethodFunctionSingle.mdx)

## Description

package, -- get containing package, class => element, Usage: , package x, class => language-macaulay2, Inputs:, x, , , a , Macaulay2Doc :: Thing, thing, , , Outputs:, a , Macaulay2Doc :: Package, package, , , the package in which the documentation key , x, was defined, Description, class => examples, i1 : package sin

o1 = Core

o1 : Package, class => language-macaulay2, i2 : package poly

o2 = Classic

o2 : Package, class => language-macaulay2, class => waystouse, Ways to use , package, :, package(Array), package(Dictionary), package(DocumentTag), package(Function), package(HashTable), package(Nothing), package(Option), package(Package), package(Sequence), package(String), package(Symbol), package(Thing), class => waystouse, For the programmer, The object , Macaulay2Doc :: package, is , a , Macaulay2Doc :: MethodFunctionSingle, method function with a single argument, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/package-doc.m2:688:0, .

## Installations

Type signatures for this function:

- `package(DocumentTag)`
- `package(Nothing)`
- `package(Option)`
- `package(Array)`
- `package(Sequence)`
- `package(String)`
- `package(Thing)`
- `package(Symbol)`
- `package(HashTable)`
- `package(Function)`
- `package(Dictionary)`
- `package(Package)`

## Examples

```macaulay2
i1 : package sin

o1 = Core

o1 : Package

i2 : package poly

o2 = Classic

o2 : Package
```

## See Also

