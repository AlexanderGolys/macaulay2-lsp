---
title: "saturate"
kind: Function
---

# saturate

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

saturate, -- saturation of ideal or submodule, class => element, Usage: , saturate(I, J), class => language-macaulay2, saturate I, class => language-macaulay2, Inputs:, I, , , an , Macaulay2Doc :: Ideal, ideal, , , a , Macaulay2Doc :: MonomialIdeal, monomial ideal, , or , a , Macaulay2Doc :: Module, module, , , J, , , an , Macaulay2Doc :: Ideal, ideal, , , a , Macaulay2Doc :: MonomialIdeal, monomial ideal, , , a , Macaulay2Doc :: Module, module, , or , a , Macaulay2Doc :: RingElement, ring element, , , if not present, then the ideal generated by the variables of the ring is used, Macaulay2Doc :: using functions with optional inputs, Optional inputs, :, Saturation :: saturate(...,Strategy=>...), Strategy, => , a , Macaulay2Doc :: Thing, thing, , , default value , null, , , specifies the algorithm, MinimalGenerators, => , a , Macaulay2Doc :: Boolean, Boolean value, , , default value , true, , , indicates whether the output should be , Macaulay2Doc :: trim, trimmed, BasisElementLimit, => , an , Macaulay2Doc :: ZZ, integer, , , default value , infinity, , , passed onto , Macaulay2Doc :: gb(...,BasisElementLimit=>...), ; , DegreeLimit, => , a , Macaulay2Doc :: List, list, , , default value , {, , , passed onto , Macaulay2Doc :: gb(...,DegreeLimit=>...), ; , PairLimit, => , an , Macaulay2Doc :: ZZ, integer, , , default value , infinity, , , passed onto , Macaulay2Doc :: gb(...,PairLimit=>...), ; , Outputs:, an , Macaulay2Doc :: Ideal, ideal, or , a , Macaulay2Doc :: Module, module, , , the saturation $I:J^\infty = \{f | f J^N\subset I \text{ for some N>0\$, Description, If $I$ is either an ideal or a submodule of a module $M$, the saturation $I : J^\infty$ is defined to be the set of elements $f$ in the ring or $M$ such that $f J^N$ is contained in $I$, for some $N$ large enough., For example, one way to homogenize an ideal is to homogenize the generators and then saturate with respect to the homogenizing variable., class => examples, i1 : R = ZZ/32003[a..d];, class => language-macaulay2, i2 : I = ideal(a^3-b, a^4-c)

 3 4
o2 = ideal (a - b, a - c)

o2 : Ideal of R, class => language-macaulay2, i3 : Ih = homogenize(I,d)

 2 2 3 2 3 2
o3 = ideal (a*b - c*d, a c - b d, b - a*c , a - b*d )

o3 : Ideal of R, class => language-macaulay2, i4 : saturate(Ih,d)

 2 2 3 2 3 2
o4 = ideal (a*b - c*d, a c - b d, b - a*c , a - b*d )

o4 : Ideal of R, class => language-macaulay2, We can use this command to remove graded submodules of finite length., class => examples, i5 : m = ideal vars R

o5 = ideal (a, b, c, d)

o5 : Ideal of R, class => language-macaulay2, i6 : M = R^1 / (a * m^2)

o6 = cokernel | a3 a2b a2c a2d ab2 abc abd ac2 acd ad2 |

 1
o6 : R-module, quotient of R, class => language-macaulay2, i7 : M / saturate 0_M

o7 = cokernel | a a3 a2b a2c a2d ab2 abc abd ac2 acd ad2 |

 1
o7 : R-module, quotient of R, class => language-macaulay2, If $I$ and $J$ are both monomial ideals, then a faster algorithm is used. Otherwise, when needed, Gröbner bases will be computed, and if the computation succeeds the result is cached inside the first argument. Partial computations are not yet cached, but this may change in a future version., See also, Macaulay2Doc :: quotient(Ideal,Ideal), Saturation :: annihilator, MinimalPrimes :: MinimalPrimes, PrimaryDecomposition :: PrimaryDecomposition, Saturation :: isSupportedInZeroLocus, class => waystouse, Ways to use , saturate, :, saturate(Ideal), saturate(Ideal,Ideal), saturate(Ideal,RingElement), saturate(Module), saturate(Module,Ideal), saturate(Module,RingElement), saturate(MonomialIdeal,RingElement), saturate(Vector), saturate(Vector,Ideal), saturate(Vector,RingElement), class => waystouse, For the programmer, The object , Saturation :: saturate, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Saturation/saturate-doc.m2:139:0, .

## Installations

Type signatures for this function:

- `saturate(Ideal, Ideal)`
- `saturate(Ideal, Number)`
- `saturate(Ideal, RingElement)`
- `saturate(Ideal)`
- `saturate(MonomialIdeal, RingElement)`
- `saturate(Module, Ideal)`
- `saturate(Module, Number)`
- `saturate(Module, RingElement)`
- `saturate(Module)`
- `saturate(Vector, Ideal)`
- `saturate(Vector, RingElement)`
- `saturate(Vector, Number)`
- `saturate(Vector)`
- `saturate(Ideal, List)`
- `saturate(Module, List)`
- `saturate(Vector, List)`

## Examples

```macaulay2
i1 : R = ZZ/32003[a..d];

i2 : I = ideal(a^3-b, a^4-c)

             3       4
o2 = ideal (a  - b, a  - c)

o2 : Ideal of R
```

## See Also

