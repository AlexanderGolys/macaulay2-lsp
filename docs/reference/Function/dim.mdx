---
title: "dim"
kind: Function
---

# dim

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

dim, -- compute the Krull dimension, empty documentation body, Caveat, To compute the dimension of a vector space, one should use , Macaulay2Doc :: rank, ., , Over the integers, the computation effectively tensors first with the rational numbers, yielding the wrong answer in some cases., See also, Macaulay2Doc :: codim, class => waystouse, Ways to use , dim, :, Varieties :: dim(AffineVariety), Macaulay2Doc :: dim(Ideal), dim(MonomialIdeal), -- see , Macaulay2Doc :: dim(Ideal), Macaulay2Doc :: dim(Module), Polyhedra :: dim(PolyhedralObject), Macaulay2Doc :: dim(ProjectiveHilbertPolynomial), Varieties :: dim(ProjectiveVariety), dim(FractionField), -- see , Macaulay2Doc :: dim(Ring), dim(GaloisField), -- see , Macaulay2Doc :: dim(Ring), dim(InexactField), -- see , Macaulay2Doc :: dim(Ring), dim(PolynomialRing), -- see , Macaulay2Doc :: dim(Ring), dim(QuotientRing), -- see , Macaulay2Doc :: dim(Ring), Macaulay2Doc :: dim(Ring), class => waystouse, For the programmer, The object , Macaulay2Doc :: dim, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/dim-doc.m2:16:0, .

## Installations

Type signatures for this function:

- `dim(FractionField)`
- `dim(GaloisField)`
- `dim(Ideal)`
- `dim(Module)`
- `dim(ProjectiveHilbertPolynomial)`
- `dim(MonomialIdeal)`
- `dim(PolynomialRing)`
- `dim(QuotientRing)`
- `dim(InexactField)`
- `dim(Ring)`
- `dim(PolyhedralObject)`
- `dim(AffineVariety)`
- `dim(ProjectiveVariety)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

