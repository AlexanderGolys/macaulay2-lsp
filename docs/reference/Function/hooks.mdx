---
title: "hooks"
kind: Function
---

# hooks

**Type:** `Function`

**Instance of:** [MethodFunctionSingle](../Type/MethodFunctionSingle.mdx)

## Description

hooks, -- list hooks attached to a key, class => element, Usage: , hooks key, class => language-macaulay2, hooks store, class => language-macaulay2, hooks(store, key), class => language-macaulay2, Inputs:, key, , , a , Macaulay2Doc :: Sequence, sequence, or , a , Macaulay2Doc :: Symbol, symbol, , , store, , , a , Macaulay2Doc :: HashTable, hash table, , , the hash table where the hooks are stored, Macaulay2Doc :: using functions with optional inputs, Optional inputs, :, Strategy, => , a , Macaulay2Doc :: Thing, thing, , , default value , null, , , only list hooks with the given strategy, Outputs:, a , Macaulay2Doc :: NumberedVerticalList, numbered vertical list, , , of those hooks associated with , key, or stored in , store, Description, class => examples, i1 : hooks(intersect, Ideal, Ideal)

o1 = {0 => (intersect, Ideal, Ideal, Strategy => Default) 
 {1 => (intersect, Ideal, Ideal, Strategy => Elimination)
 {2 => (intersect, Ideal, Ideal, Strategy => Monomial) 

o1 : NumberedVerticalList, class => language-macaulay2, i2 : code 0

o2 = -- code for strategy: intersect(Ideal,Ideal,Strategy => Default)
 ../../m2/intersect.m2:116:15-116:67: --source code:
 Default => (opts, L) -> ideal intersect(opts, apply(L, module)),, class => language-macaulay2, i3 : hooks(quotient, Strategy => Iterate)

o3 = {0 => (quotient, Ideal, Ideal, Strategy => Iterate) 
 {1 => (quotient, Module, Ideal, Strategy => Iterate) 
 {2 => (quotient, Module, Module, Strategy => Iterate)

o3 : NumberedVerticalList, class => language-macaulay2, See also, Macaulay2Doc :: addHook, Macaulay2Doc :: runHooks, class => waystouse, Ways to use , hooks, :, hooks(HashTable), hooks(List), hooks(Sequence), hooks(Symbol), hooks(Thing), hooks(ZZ), class => waystouse, For the programmer, The object , Macaulay2Doc :: hooks, is , a , Macaulay2Doc :: MethodFunctionSingle, method function with a single argument, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/hooks-doc.m2:181:0, .

## Installations

Type signatures for this function:

- `hooks(ZZ)`
- `hooks(List)`
- `hooks(Thing)`
- `hooks(Symbol)`
- `hooks(Sequence)`
- `hooks(HashTable)`

## Examples

```macaulay2
i2 : code 0

o2 = -- code for strategy: intersect(Ideal,Ideal,Strategy => Default)
     ../../m2/intersect.m2:116:15-116:67: --source code:
         Default => (opts, L) -> ideal intersect(opts, apply(L, module)),
```

## See Also

