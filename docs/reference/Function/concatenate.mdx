---
title: "concatenate"
kind: Function
---

# concatenate

**Type:** `Function`

**Instance of:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

concatenate, -- join strings, Description, concatenate(s,t,...,u), yields the concatenation of the strings s,t,...,u., , The arguments may also be lists or sequences of strings and symbols, in which case they are concatenated recursively. Additionally, an integer may be used to represent a number of spaces, and , Macaulay2Doc :: null, will be represented by the empty string., class => examples, i1 : concatenate {"a",("s",3,"d",),"f"

o1 = as df, class => language-macaulay2, See also, Macaulay2Doc :: String, class => waystouse, Ways to use , concatenate, :, concatenate(BasicList), concatenate(Nothing), concatenate(String), concatenate(Symbol), concatenate(ZZ), class => waystouse, For the programmer, The object , Macaulay2Doc :: concatenate, is , a , Macaulay2Doc :: CompiledFunction, compiled function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/ov_strings.m2:245:0, .

## Installations

Type signatures for this function:

- `concatenate(Symbol)`
- `concatenate(Nothing)`
- `concatenate(BasicList)`
- `concatenate(ZZ)`
- `concatenate(String)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

