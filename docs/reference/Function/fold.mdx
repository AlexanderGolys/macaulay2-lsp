---
title: "fold"
kind: Function
---

# fold

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

fold, -- apply a binary operator repeatedly, class => element, Usage: , fold(f, x, L), class => language-macaulay2, fold(f, L), class => language-macaulay2, fold(L, x, f), class => language-macaulay2, fold(L, f), class => language-macaulay2, Inputs:, f, , , a , Macaulay2Doc :: Function, function, , , x, , , a , Macaulay2Doc :: Thing, thing, , , L, , , a , Macaulay2Doc :: VisibleList, visible list, , , or an instance of a class with the , Macaulay2Doc :: iterator, method installed, Outputs:, M, , , a , Macaulay2Doc :: List, list, , , Description, Suppose , L=\{x0, x1, ..., xn\, . Then for any binary operator , f, , , fold(f, L), computes , f(...f(f(x0, x1), x2), ...), . In other words, the binary operator is applied to the first two elements of , L, , then to that result along with the next unused element of , L, , and so forth., class => examples, i1 : fold(plus, {0,1,2,3,4,5)

o1 = 15, class => language-macaulay2, i2 : fold(identity, {a,b,c,d,e)

o2 = ((((a, b), c), d), e)

o2 : Sequence, class => language-macaulay2, i3 : fold((i,j) -> i|j|i, {"a","b","c","d","e")

o3 = abacabadabacabaeabacabadabacaba, class => language-macaulay2, If , fold(f, x, L), is called, the element , x, is used as the first argument of the binary function , f, . In other words, , fold(f, \{x0, x1, \ldots, xn\), is equivalent to , fold(f, x0, \{x1, \ldots, xn\), ., class => examples, i4 : fold(plus, 0, {1,2,3,4,5)

o4 = 15, class => language-macaulay2, i5 : fold((x, y) -> x^y, 2, {3,2,1,2)

o5 = 4096, class => language-macaulay2, The function , fold(\{x_0, x_1, \ldots, x_n\, f), returns , f...f(f(x_{n-2, f(x_{n-1, x_n))), . That is, , f, is applied to the last two elements of the list first, then the repeated calls to , f, proceed backwards through the list. The optional argument , x, in , fold(L, x, f), is used as the second argument in the first evaluation of , f, . So , fold(\{x_0, x_1, \ldots, x_{n-1\, x_n, f), is equivalent to , fold(\{x_0, x_1, \ldots, x_n\, f), ., class => examples, i6 : fold({a,b,c,d,e, identity)

o6 = (a, (b, (c, (d, e))))

o6 : Sequence, class => language-macaulay2, i7 : fold({a,b,c,d, e, identity)

o7 = (a, (b, (c, (d, e))))

o7 : Sequence, class => language-macaulay2, i8 : fold({2,3,2,1, 2, (x, y) -> x^y)

o8 = 512, class => language-macaulay2, If , L, is an instance of class the the , Macaulay2Doc :: iterator, method installed, e.g., a string, then it may also be used with , fold, , but only the versions with , f, as the first argument., class => examples, i9 : fold(identity, "abcde")

o9 = ((((a, b), c), d), e)

o9 : Sequence, class => language-macaulay2, i10 : fold(identity, "a", "bcde")

o10 = ((((a, b), c), d), e)

o10 : Sequence, class => language-macaulay2, The difference between , Macaulay2Doc :: fold, and , accumulate, is that , fold, returns the final result of all the nested evaluations of , f, , while , accumulate, lists all the intermediate values as well., class => examples, i11 : accumulate({2,3,2,1, 2, (x, y) -> x^y)

o11 = {512, 9, 2, 1

o11 : List, class => language-macaulay2, See also, Macaulay2Doc :: apply, Macaulay2Doc :: accumulate, Macaulay2Doc :: lists and sequences, class => waystouse, Ways to use , fold, :, fold(Function,Thing), fold(Function,Thing,Thing), fold(Function,VisibleList), fold(VisibleList,Function), fold(VisibleList,Thing,Function), class => waystouse, For the programmer, The object , Macaulay2Doc :: fold, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/fold-doc.m2:74:0, .

## Installations

Type signatures for this function:

- `fold(VisibleList, Thing, Function)`
- `fold(Function, VisibleList)`
- `fold(VisibleList, Function)`
- `fold(Function, Thing)`
- `fold(Function, Thing, Thing)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

