---
title: "vector"
kind: Function
---

# vector

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

vector, -- make a vector, class => element, Usage: , vector(M, x), class => language-macaulay2, vector x, class => language-macaulay2, Inputs:, M, , , a , Macaulay2Doc :: Module, module, , , x, , , a , Macaulay2Doc :: List, list, , , a , Macaulay2Doc :: Matrix, matrix, , , a , Macaulay2Doc :: Number, number, , or , a , Macaulay2Doc :: RingElement, ring element, , , Outputs:, a , Macaulay2Doc :: Vector, vector, , , Description, For any $R$-module $M$, there exists an isomorphism between $\operatorname{Hom(R,M)$ and $M$ given by $f\mapsto f(1)$. Therefore, internally all , Macaulay2Doc :: Vector, objects representing elements of $M$ correspond to matrices with source $R^1$ and target $M$. A vector may be constructed from such a matrix using , vector, ., class => examples, i1 : R = QQ[x,y,z]

o1 = R

o1 : PolynomialRing, class => language-macaulay2, i2 : f = matrix {{x, {y, {z

o2 = | x |
 | y |
 | z |

 3 1
o2 : Matrix R <-- R, class => language-macaulay2, i3 : vector f

o3 = | x |
 | y |
 | z |

 3
o3 : R, class => language-macaulay2, Alternatively, $M$ may be specified if it differs from the target of the matrix., class => examples, i4 : g = matrix {{1, {2, {3

o4 = | 1 |
 | 2 |
 | 3 |

 3 1
o4 : Matrix ZZ <-- ZZ, class => language-macaulay2, i5 : vector(R^3, g)

o5 = | 1 |
 | 2 |
 | 3 |

 3
o5 : R, class => language-macaulay2, If the matrix would have only one element, then that element may be given instead. If the module is not provided, then the result will be an element of the free module of rank one of the ring of the given element., class => examples, i6 : vector 2

o6 = | 2 |

 1
o6 : ZZ, class => language-macaulay2, i7 : vector x

o7 = | x |

 1
o7 : R, class => language-macaulay2, i8 : vector(R^1, 2)

o8 = | 2 |

 1
o8 : R, class => language-macaulay2, Alternatively, a list of elements may be provided. If the module is not specified, then the vector will be an element of a free module over a ring containing all the elements of the list., class => examples, i9 : vector {1, 2, 3

o9 = | 1 |
 | 2 |
 | 3 |

 3
o9 : ZZ, class => language-macaulay2, i10 : vector {1, x, y

o10 = | 1 |
 | x |
 | y |

 3
o10 : R, class => language-macaulay2, i11 : vector(R^3, {1, 2, 3)

o11 = | 1 |
 | 2 |
 | 3 |

 3
o11 : R, class => language-macaulay2, Alternatively, the ring $R$ may be provided instead of $M$, and the resulting vector will be an element of the appropriate free module over $R$., class => examples, i12 : vector(QQ, {1, 2, 3)

o12 = | 1 |
 | 2 |
 | 3 |

 3
o12 : QQ, class => language-macaulay2, i13 : vector(R, {1, 2, 3)

o13 = | 1 |
 | 2 |
 | 3 |

 3
o13 : R, class => language-macaulay2, i14 : vector(R, 2)

o14 = | 2 |

 1
o14 : R, class => language-macaulay2, class => waystouse, Ways to use , vector, :, vector(List), vector(Matrix), vector(Module,List), vector(Module,Matrix), vector(Module,Number), vector(Module,RingElement), vector(Number), vector(Ring,List), vector(Ring,Matrix), vector(Ring,Number), vector(Ring,RingElement), vector(RingElement), vector(RingFamily,List), vector(RingFamily,Matrix), vector(RingFamily,Number), vector(RingFamily,RingElement), Macaulay2Doc :: vector(Vector), class => waystouse, For the programmer, The object , Macaulay2Doc :: vector, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/doc_module.m2:292:0, .

## Installations

Type signatures for this function:

- `vector(Matrix)`
- `vector(List)`
- `vector(RingElement)`
- `vector(Number)`
- `vector(Module, Matrix)`
- `vector(Module, List)`
- `vector(Module, Number)`
- `vector(Module, RingElement)`
- `vector(RingFamily, Matrix)`
- `vector(Ring, Matrix)`
- `vector(RingFamily, List)`
- `vector(Ring, List)`
- `vector(Ring, RingElement)`
- `vector(Ring, Number)`
- `vector(RingFamily, RingElement)`
- `vector(RingFamily, Number)`
- `vector(Vector)`

## Examples

```macaulay2
i1 : R = QQ[x,y,z]

o1 = R

o1 : PolynomialRing
```

## See Also

