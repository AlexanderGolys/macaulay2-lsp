---
title: "check"
kind: Function
---

# check

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

check, -- perform tests of a package, class => element, Usage: , check pkg, class => language-macaulay2, check(i, pkg), class => language-macaulay2, check(L, pkg), class => language-macaulay2, Inputs:, pkg, , , a , Macaulay2Doc :: Package, package, or , a , Macaulay2Doc :: String, string, , , the package to test, i, , , an , Macaulay2Doc :: ZZ, integer, , , the index of a test to run, L, , , a , Macaulay2Doc :: List, list, , , containing indices of tests to run, or , {, to run all tests, Macaulay2Doc :: using functions with optional inputs, Optional inputs, :, UserMode, => , a , Macaulay2Doc :: Boolean, Boolean value, , , default value , null, , , if true, do not use the , -q, in arguments to the Macaulay2 executable when running tests, thereby allowing it to load the user's , Macaulay2Doc :: initialization file, , allowing it to load packages previously installed in the user's , Macaulay2Doc :: applicationDirectory, application directory, and allowing packages it loads to read their configuration files from the user's , Macaulay2Doc :: applicationDirectory, application directory, . If false, the , -q, argument is added. If , Macaulay2Doc :: null, , then add , -q, if it appears as an option in , Macaulay2Doc :: commandLine, ., Verbose, => , a , Macaulay2Doc :: Boolean, Boolean value, , , default value , false, , , if true, then print the output of all failing tests, Consequences:, the tests in the package , pkg, are run (in separate Macaulay2 processes, with the random number seed initialized to 0), and any errors are reported., if , i, is given, only the i-th test in the package is run and any errors are reported., Description, It is important for package authors to provide tests to ensure that the package is functioning properly. One provides tests using the , Macaulay2Doc :: TEST, function following the , Macaulay2Doc :: beginDocumentation, call in the source of the package., Optionally, one can add files to the , tests, directory under the auxiliary subdirectory of the package. The contents of each file will automatically be included as one of the package's tests., For example, to run the tests for the , FirstPackage :: FirstPackage, package, use:, class => examples, i1 : needsPackage "FirstPackage"

o1 = FirstPackage

o1 : Package, class => language-macaulay2, i2 : check_1 FirstPackage
 -- warning: reloading FirstPackage; recreate instances of types from this package
 -- capturing check(1, "FirstPackage") -- .0828494s elapsed, class => language-macaulay2, i3 : check FirstPackage
 -- capturing check(0, "FirstPackage") -- .0829999s elapsed
 -- capturing check(1, "FirstPackage") -- .0830992s elapsed, class => language-macaulay2, Alternatively, if the package is installed somewhere accessible, one can do the following., class => examples, i4 : check_1 "FirstPackage"
 -- capturing check(1, "FirstPackage") -- .082661s elapsed, class => language-macaulay2, i5 : check "FirstPackage"
 -- capturing check(0, "FirstPackage") -- .0831016s elapsed
 -- capturing check(1, "FirstPackage") -- .0836374s elapsed, class => language-macaulay2, A , Macaulay2Doc :: TestInput, object (or a list of such objects) can also be run directly., class => examples, i6 : tests(1, "FirstPackage")

o6 = TestInput[/usr/share/Macaulay2/FirstPackage.m2:58:5-60:3]

o6 : TestInput, class => language-macaulay2, i7 : check oo
 -- capturing check(1, "FirstPackage") -- .0831956s elapsed, class => language-macaulay2, i8 : tests "FirstPackage"

o8 = {0 => TestInput[/usr/share/Macaulay2/FirstPackage.m2:54:5-56:3]
 {1 => TestInput[/usr/share/Macaulay2/FirstPackage.m2:58:5-60:3]

o8 : NumberedVerticalList, class => language-macaulay2, i9 : check oo
 -- capturing check(0, "FirstPackage") -- .082223s elapsed
 -- capturing check(1, "FirstPackage") -- .0833828s elapsed, class => language-macaulay2, If only an integer is passed as an argument, then the test with that index from the last call to , Macaulay2Doc :: tests, is run., class => examples, i10 : tests "FirstPackage"

o10 = {0 => TestInput[/usr/share/Macaulay2/FirstPackage.m2:54:5-56:3]
 {1 => TestInput[/usr/share/Macaulay2/FirstPackage.m2:58:5-60:3]

o10 : NumberedVerticalList, class => language-macaulay2, i11 : check 1
 -- capturing check(1, "FirstPackage") -- .0836207s elapsed, class => language-macaulay2, Caveat, Currently, if the package was only partially loaded because the documentation was obtainable from a database (see , Macaulay2Doc :: beginDocumentation, ), then the package will be reloaded, this time completely, to ensure that all tests are considered; this may affect user objects of types declared by the package, as they may be not usable by the new instance of the package. In a future version, either the tests and the documentation will both be cached, or neither will., See also, Macaulay2Doc :: packages, Macaulay2Doc :: creating a package, Macaulay2Doc :: TEST, Macaulay2Doc :: installPackage, Macaulay2Doc :: loadPackage, Macaulay2Doc :: tests, class => waystouse, Ways to use , check, :, check(List), check(List,Package), check(List,String), check(Package), check(String), check(TestInput), check(ZZ), check(ZZ,Package), check(ZZ,String), class => waystouse, For the programmer, The object , Macaulay2Doc :: check, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/check-doc.m2:125:0, .

## Installations

Type signatures for this function:

- `check(String)`
- `check(Package)`
- `check(ZZ, Package)`
- `check(ZZ, String)`
- `check(List, String)`
- `check(List, Package)`
- `check(TestInput)`
- `check(ZZ)`
- `check(List)`

## Examples

```macaulay2
i1 : needsPackage "FirstPackage"

o1 = FirstPackage

o1 : Package

i2 : check_1 FirstPackage
 -- warning: reloading FirstPackage; recreate instances of types from this package
 -- capturing check(1, "FirstPackage")        -- .0828494s elapsed
```

## See Also

