---
title: "leadTerm"
kind: Function
---

# leadTerm

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

leadTerm, -- get the greatest term, Description, Every polynomial ring in Macaulay2 comes equipped with a monomial ordering. For ring elements and matrices, this function returns the greatest term in this order., , For an ideal, a Gröbner basis is first computed, and the ideal of lead terms is returned., , If an initial integer , n, is specified, then the returned value contains the sum of all of the terms with the greatest value on the first , n, , parts of the monomial order., class => waystouse, Ways to use , leadTerm, :, Macaulay2Doc :: leadTerm(Ideal), leadTerm(GroebnerBasis), -- see , Macaulay2Doc :: leadTerm(Matrix), Macaulay2Doc :: leadTerm(Matrix), leadTerm(Vector), -- see , Macaulay2Doc :: leadTerm(Matrix), Macaulay2Doc :: leadTerm(RingElement), Macaulay2Doc :: leadTerm(ZZ,Ideal), leadTerm(ZZ,GroebnerBasis), -- see , Macaulay2Doc :: leadTerm(ZZ,Matrix), Macaulay2Doc :: leadTerm(ZZ,Matrix), leadTerm(ZZ,Vector), -- see , Macaulay2Doc :: leadTerm(ZZ,Matrix), Macaulay2Doc :: leadTerm(ZZ,RingElement), class => waystouse, For the programmer, The object , Macaulay2Doc :: leadTerm, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/doc7.m2:143:0, .

## Installations

Type signatures for this function:

- `leadTerm(RingElement)`
- `leadTerm(GroebnerBasis)`
- `leadTerm(ZZ, GroebnerBasis)`
- `leadTerm(ZZ, Matrix)`
- `leadTerm(ZZ, RingElement)`
- `leadTerm(Matrix)`
- `leadTerm(Ideal)`
- `leadTerm(ZZ, Ideal)`
- `leadTerm(Vector)`
- `leadTerm(ZZ, Vector)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

