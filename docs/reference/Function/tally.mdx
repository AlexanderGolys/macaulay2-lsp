---
title: "tally"
kind: Function
---

# tally

**tally  -- tally the elements of a list sequence array or string**

**Type:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

It produces a hash table (multiset) `y` which tallies the frequencies of the occurrences of items in the list or string `x` i.e.`y_i` is the number of times `i` appears in `x` or is `0` if `i` doesn't appear in the list or string.

## Examples

```macaulay2
y = tally x
```

```macaulay2
tally "Hello
```

## Additional Information

- Inputs:
- `x` a [ visible list](../Type/VisibleList.mdx) or a [ string](../Type/op_Coredollar_String.mdx)
- Outputs:
- `y` a [ tally](../Type/Tally.mdx)


### See also


- Macaulay2Doc:: Tally

## Debug info

```python
{
    "obj_type": "Function",
    "name": "tally",
    "safe_name": "op_Coredollar_tally",
    "type": "CompiledFunction",
    "description": "It produces a hash table (multiset) `y` which tallies the frequencies of the occurrences of items in...",
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$tally",
        "full_doc": "... (1981 bytes) ...",
        "parsed_headline": "tally  -- tally the elements of a list sequence array or string"
    }
}
```
