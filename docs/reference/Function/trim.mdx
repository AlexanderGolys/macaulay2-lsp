---
title: "trim"
kind: Function
---

# trim

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

trim, -- minimize generators and relations, Description, There are two ways to present an $R$-module $M$. One way is to take a free module $F$ (whose generators are called the , Macaulay2Doc :: generators, ) and form the quotient $M = F/H$ by a submodule $H\subset F$ (whose generators are called the , Macaulay2Doc :: relations, )., Another way is take a free module $F$, a submodule $G\subset F$ (whose generators are called the , Macaulay2Doc :: generators, ), a submodule $H\subset F$ (whose generators are called the , Macaulay2Doc :: relations, ), and form the , Macaulay2Doc :: subquotient modules, subquotient module, $M = (G+H)/H$, obtained also as the image of $G$ in $F/H$., The purpose of , trim, is to minimize presentations of the latter type. This applies also to rings and ideals., class => examples, i1 : R = ZZ/101[x,y,z,u,w]

o1 = R

o1 : PolynomialRing, class => language-macaulay2, i2 : I = ideal(x^2-x^2-y^2, z^2+x*y, w^2-u^2, x^2-y^2)

 2 2 2 2 2 2
o2 = ideal (-y , x*y + z , - u + w , x - y )

o2 : Ideal of R, class => language-macaulay2, i3 : trim I

 2 2 2 2 2
o3 = ideal (u - w , y , x*y + z , x )

o3 : Ideal of R, class => language-macaulay2, i4 : trim (R^1/I)

o4 = cokernel | u2-w2 y2 xy+z2 x2 |

 1
o4 : R-module, quotient of R, class => language-macaulay2, class => examples, i5 : R = ZZ/32003[a..d]

o5 = R

o5 : PolynomialRing, class => language-macaulay2, i6 : M = coker matrix {{a,1,b,{c,3,b+d

o6 = cokernel | a 1 b |
 | c 3 b+d |

 2
o6 : R-module, quotient of R, class => language-macaulay2, i7 : trim M

o7 = subquotient (| 1 |, | 10668 b+16001d a-10668c |)
 | 0 | | 1 0 0 |

 2
o7 : R-module, subquotient of R, class => language-macaulay2, i8 : prune M

o8 = cokernel | b+16001d a-10668c |

 1
o8 : R-module, quotient of R, class => language-macaulay2, See also, Macaulay2Doc :: prune, Macaulay2Doc :: mingens, Macaulay2Doc :: subquotient modules, Macaulay2Doc :: generators, Macaulay2Doc :: relations, Macaulay2Doc :: subquotient, class => waystouse, Ways to use , trim, :, trim(Ideal), trim(Module), trim(MonomialIdeal), trim(QuotientRing), trim(Ring), class => waystouse, For the programmer, The object , Macaulay2Doc :: trim, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/trim-doc.m2:84:0, .

## Installations

Type signatures for this function:

- `trim(Ring)`
- `trim(QuotientRing)`
- `trim(Ideal)`
- `trim(Module)`
- `trim(MonomialIdeal)`

## Examples

```macaulay2
i1 : R = ZZ/101[x,y,z,u,w]

o1 = R

o1 : PolynomialRing

i2 : I = ideal(x^2-x^2-y^2, z^2+x*y, w^2-u^2, x^2-y^2)

              2         2     2    2   2    2
o2 = ideal (-y , x*y + z , - u  + w , x  - y )

o2 : Ideal of R
```

## See Also

