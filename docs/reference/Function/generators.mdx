---
title: "generators"
kind: Function
---

# generators

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

generators, -- provide matrix or list of generators, class => element, Usage: , generators x, class => language-macaulay2, gens x, class => language-macaulay2, Inputs:, x, Macaulay2Doc :: using functions with optional inputs, Optional inputs, :, Macaulay2Doc :: generators(...,CoefficientRing=>...), CoefficientRing, => , ..., , , default value , null, , , only used if , x, is a ring, Outputs:, provides the generators of , x, in a convenient form, as a list or a matrix, depending on the type, Description, , Produces the generators of a Gröbner basis, a polynomial ring, an ideal, a free module, a free group, a submodule given by means of generators (or for which generators have been computed), or a free monoid., , Usually the result is a list of generators, but the generators of a module or Gröbner basis are provided as the columns in a matrix. The matrix is stored in a module M under M.generators, unless the matrix is the identity matrix., , The symbol , gens, is a synonym for , generators, ., See also, Macaulay2Doc :: numgens, Macaulay2Doc :: Monoid, Macaulay2Doc :: GroebnerBasis, Macaulay2Doc :: Module, Macaulay2Doc :: relations, Macaulay2Doc :: subquotient, class => waystouse, Ways to use , generators, :, Macaulay2Doc :: generators(GroebnerBasis), Macaulay2Doc :: generators(Ideal), generators(MonomialIdeal), -- see , Macaulay2Doc :: generators(Ideal), Macaulay2Doc :: generators(Module), Macaulay2Doc :: generators(Monoid), Macaulay2Doc :: generators(Ring), class => waystouse, For the programmer, The object , Macaulay2Doc :: generators, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/generators-doc.m2:36:0, .

## Installations

Type signatures for this function:

- `generators(FractionField)`
- `generators(EngineRing)`
- `generators(GaloisField)`
- `generators(GroebnerBasis)`
- `generators(Module)`
- `generators(Ideal)`
- `generators(MonomialIdeal)`
- `generators(Monoid)`
- `generators(PolynomialRing)`
- `generators(QuotientRing)`
- `generators(InexactField)`
- `generators(Ring)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

