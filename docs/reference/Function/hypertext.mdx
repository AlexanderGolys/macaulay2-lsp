---
title: "hypertext"
kind: Function
---

# hypertext

**Type:** `Function`

**Instance of:** [MethodFunctionSingle](../Type/MethodFunctionSingle.mdx)

## Description

hypertext, -- prepare hypertext for display, class => element, Usage: , hypertext x, class => language-macaulay2, Inputs:, x, , , a , Macaulay2Doc :: List, list, , , a list of strings and hypertext mark-up lists, Outputs:, a , Text :: Hypertext, markup list, , , a new list of mark-up lists obtained from the old by making the format more suitable for ultimate display, Description, Here is a list of some of the transformations that are performed., occurrences of , null, , such as those that might have been produced by the insertion of an extra comma, are removed, Macaulay2Doc :: Sequence, sequences, , such as those produced by extra sets of parentheses, are , Macaulay2Doc :: splice, spliced, into the lists containing them, the contents of lists and sequences are merged into the mark-up lists containing them, mark-up lists of type , Text :: TO, occurring a mark-up list of type , Text :: UL, are converted to lists of type , Text :: TOH, , so the headlines of the items will appear in the resulting menu, the targets of the links in mark-up lists of type TO, TOH, and TO2, are converted into , Macaulay2Doc :: DocumentTag, document tags, mark-up types, such as , Text :: PARA, , are converted into lists of length 0 of that type, strings spanning multiple lines are wrapped into one long line after appropriately trimming the spaces at the beginning and end of each line, an error message is produced if something not recognizable as documentation is encountered, We may phase out this function in favor of performing the listed transformations automatically when hypertext elements are created., See also, Text :: Hypertext, class => waystouse, For the programmer, The object , Macaulay2Doc :: hypertext, is , a , Macaulay2Doc :: MethodFunctionSingle, method function with a single argument, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/hypertext-doc.m2:28:0, .

## Installations

Type signatures for this function:

- `hypertext(Descent)`
- `hypertext(Time)`
- `hypertext(String)`
- `hypertext(IndeterminateNumber)`
- `hypertext(Boolean)`
- `hypertext(Type)`
- `hypertext(Package)`
- `hypertext(Nothing)`
- `hypertext(Dictionary)`
- `hypertext(Manipulator)`
- `hypertext(File)`
- `hypertext(FunctionBody)`
- `hypertext(Command)`
- `hypertext(Function)`
- `hypertext(Net)`
- `hypertext(FilePosition)`
- `hypertext(VerticalList)`
- `hypertext(NumberedVerticalList)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

