---
title: "export"
kind: Function
---

# export

**Type:** `Function`

**Instance of:** [MethodFunctionSingle](../Type/MethodFunctionSingle.mdx)

## Description

export, -- export symbols from a package, class => element, Usage: , export {"symbol1", "symbol2", ..., class => language-macaulay2, Inputs:, a , Macaulay2Doc :: List, list, , , containing strings or options with string keys and values belonging to the package, Outputs:, a , Macaulay2Doc :: List, list, , , the list of exported symbols, Consequences:, the symbols whose names are in the list as strings, which should refer to functions or other symbols defined in the package, are made available to the user of the package, and are marked non-mutable. The strings are converted to symbols with those names in the dictionary of the package. An option of the form , "name" => "symbol", creates a symbol with the name , name, that is a synonym of the symbol , symbol, ., Description, A package can contain the code for many functions, only some of which should be made visible to the user. The function , export, allows one to specify which symbols are to be made visible. For an example see , Macaulay2Doc :: an example of a package, ., No single-letter symbol should be exported, since such symbols are reserved as variables for the user., Use , Macaulay2Doc :: exportMutable, to export symbols whose values the user is permitted to modify., Use , Macaulay2Doc :: importFrom, to import private symbols of a package., See also, Macaulay2Doc :: exportFrom, Macaulay2Doc :: exportMutable, Macaulay2Doc :: importFrom, class => waystouse, Ways to use , export, :, export(List), export(String), class => waystouse, For the programmer, The object , Macaulay2Doc :: export, is , a , Macaulay2Doc :: MethodFunctionSingle, method function with a single argument, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/export-doc.m2:140:0, .

## Installations

Type signatures for this function:

- `export(String)`
- `export(List)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

