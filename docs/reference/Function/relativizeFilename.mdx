---
title: "relativizeFilename"
kind: Function
---

# relativizeFilename

**relativizeFilename  -- relativize a file name**

**Type:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

Paths of the form `foo/x/../bar`  are shortened to `foo/bar`  without checking the file system to see whether `x`  is a symbolic link.  For the other behavior see [realpath](../Function/realpath.mdx) .

## Examples

```macaulay2
relativizeFilename(dirfn)
```

```macaulay2
relativizeFilename("a/b/""a/b/c/d")

```

```text
c/d
```

```macaulay2
relativizeFilename("a/b/c/d""a/b/")

```

```text
../../
```

```macaulay2
relativizeFilename("a/b/c/d""a/b/e/f")

```

```text
../../e/f
```

## Additional Information

- Inputs:
- `dir` a path to a directory
- `fn` a path to a file
- Outputs:
- a relativized path equivalent to `fn` when starting from `dir`


### See also


- Macaulay2Doc:: File
- Macaulay2Doc:: minimizeFilename
- Macaulay2Doc:: baseFilename
- Macaulay2Doc:: toAbsolutePath
- Macaulay2Doc:: searchPath
- Macaulay2Doc:: path

## Debug info

```python
{
    "obj_type": "Function",
    "name": "relativizeFilename",
    "safe_name": "op_Coredollar_relativizeFilename",
    "type": "CompiledFunction",
    "description": "Paths of the form `foo/x/../bar`  are shortened to `foo/bar`  without checking the file system to se...",
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$relativizeFilename",
        "full_doc": "... (1524 bytes) ...",
        "parsed_headline": "relativizeFilename  -- relativize a file name"
    }
}
```
