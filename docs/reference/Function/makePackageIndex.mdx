---
title: "makePackageIndex"
kind: Function
---

# makePackageIndex

**Type:** `Function`

**Instance of:** [MethodFunctionSingle](../Type/MethodFunctionSingle.mdx)

## Description

makePackageIndex, -- create an index of installed packages, class => element, Usage: , makePackageIndex(), class => language-macaulay2, Consequences:, Creates a file , index.html, in the , ~/.Macaulay2, directory, containing links to the documentation for Macaulay2 and all installed packages., Description, This command may need to be run after installing a package via , Macaulay2Doc :: installPackage, ., This command is run each time the program is started, updating the index file. Thus, one can simply restart Macaulay2 to obtain the same consequence., See also, Macaulay2Doc :: packages, class => waystouse, Ways to use , makePackageIndex, :, makePackageIndex(List), makePackageIndex(Sequence), class => waystouse, For the programmer, The object , Macaulay2Doc :: makePackageIndex, is , a , Macaulay2Doc :: MethodFunctionSingle, method function with a single argument, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/package-doc.m2:688:0, .

## Installations

Type signatures for this function:

- `makePackageIndex(Sequence)`
- `makePackageIndex(List)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

