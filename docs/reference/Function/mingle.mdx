---
title: "mingle"
kind: Function
---

# mingle

**Type:** `Function`

**Instance of:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

mingle, -- mingle elements of several lists, class => element, Usage: , mingle(L), class => language-macaulay2, Inputs:, L, , , a , Macaulay2Doc :: BasicList, basic list, , , a list of lists , L=\{L1, L2, ..., Ln\, , Outputs:, M, , , a , Macaulay2Doc :: List, list, , , a new list mingling the elements of all lists in , L, , Description, The output list , M, takes the first element of each , Li, i=1,...,n, , followed by the second element of , Li, i=1,...,n, , and so forth., class => examples, i1 : mingle {{a1, a2, a3, {b1, b2, b3, {c1, c2, c3

o1 = {a1, b1, c1, a2, b2, c2, a3, b3, c3

o1 : List, class => language-macaulay2, The lists can have different lengths. After a list is exhausted, it will be silently ignored., class => examples, i2 : mingle {{a1, a2, a3, a4, {b1, b2, {c1

o2 = {a1, b1, c1, a2, b2, a3, a4

o2 : List, class => language-macaulay2, To transpose a nested list (thinking of it as a matrix), try using , mingle, with , Macaulay2Doc :: pack, ., class => examples, i3 : pack(3, mingle ((a1, a2, a3), (b1, b2, b3), (c1, c2, c3)))

o3 = {{a1, b1, c1, {a2, b2, c2, {a3, b3, c3

o3 : List, class => language-macaulay2, Notice from the previous example that , mingle, accepts sequences and other types of , Macaulay2Doc :: BasicList, s as input, but the output will always be a , Macaulay2Doc :: List, ., Further examples:, class => examples, i4 : concatenate mingle( {"a","b","c" , {",","," )

o4 = a,b,c, class => language-macaulay2, i5 : netList pack(3, mingle( (0..5), apply(6, i -> i^2), apply(6, i -> i^3)))

 +-+--+---+
o5 = |0|0 |0 |
 +-+--+---+
 |1|1 |1 |
 +-+--+---+
 |2|4 |8 |
 +-+--+---+
 |3|9 |27 |
 +-+--+---+
 |4|16|64 |
 +-+--+---+
 |5|25|125|
 +-+--+---+, class => language-macaulay2, See also, Macaulay2Doc :: apply, Macaulay2Doc :: insert, Macaulay2Doc :: join, Macaulay2Doc :: pack, Macaulay2Doc :: sort, Macaulay2Doc :: sublists, Macaulay2Doc :: lists and sequences, class => waystouse, Ways to use , mingle, :, mingle(BasicList), class => waystouse, For the programmer, The object , Macaulay2Doc :: mingle, is , a , Macaulay2Doc :: CompiledFunction, compiled function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/mingle-doc.m2:52:0, .

## Installations

Type signatures for this function:

- `mingle(BasicList)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

