---
title: "expression"
kind: Function
---

# expression

**Type:** `Function`

**Instance of:** [MethodFunctionSingle](../Type/MethodFunctionSingle.mdx)

## Description

expression, -- convert to an expression, class => element, Usage: , expression x, class => language-macaulay2, Inputs:, x, Outputs:, an , Macaulay2Doc :: Expression, expression, , , constructed from , x, using the mathematical structure of , x, , if present, Description, Here is example of a function that expresses small rational numbers as Egyptian fractions using , Macaulay2Doc :: expression, ., class => examples, i1 : egyptian = method();, class => language-macaulay2, i2 : egyptian QQ := x -> if x == 0 then 0 else (
 	 n := ceiling(1/x);
 	 expression(1/n) + egyptian(x - 1/n));, class => language-macaulay2, i3 : egyptian(30/31)

 1 1 1 1 1 1
o3 = - + - + - + --- + ----- + ---------
 2 3 8 107 15922 633759288

o3 : Expression of class Sum, class => language-macaulay2, class => waystouse, Ways to use , expression, :, Varieties :: expression(CoherentSheaf), Varieties :: expression(SheafMap), Varieties :: expression(SheafOfRings), Varieties :: expression(SumOfTwists), Varieties :: expression(Variety), class => waystouse, For the programmer, The object , Macaulay2Doc :: expression, is , a , Macaulay2Doc :: MethodFunctionSingle, method function with a single argument, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/expression-doc.m2:91:0, .

## Installations

Type signatures for this function:

- `expression(Eliminate)`
- `expression(EngineRing)`
- `expression(FractionField)`
- `expression(ZZ)`
- `expression(VisibleList)`
- `expression(Function)`
- `expression(Symbol)`
- `expression(Boolean)`
- `expression(Thing)`
- `expression(Type)`
- `expression(Option)`
- `expression(Set)`
- `expression(GaloisField)`
- `expression(ScriptedFunctor)`
- `expression(ProjectiveHilbertPolynomial)`
- `expression(Matrix)`
- `expression(Ideal)`
- `expression(Vector)`
- `expression(Module)`
- `expression(MonomialIdeal)`
- `expression(MonoidElement)`
- `expression(Monoid)`
- `expression(MutableMatrix)`
- `expression(PolynomialRing)`
- `expression(QuotientRing)`
- `expression(QQ)`
- `expression(RealField)`
- `expression(ComplexField)`
- `expression(RR)`
- `expression(CC)`

*... and 10 more*

## Examples

```macaulay2
i1 : egyptian = method();

i2 : egyptian QQ := x -> if x == 0 then 0 else (
     	    n := ceiling(1/x);
     	    expression(1/n) + egyptian(x - 1/n));
```

## See Also

