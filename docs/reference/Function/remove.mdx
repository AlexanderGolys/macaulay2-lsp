---
title: "remove"
kind: Function
---

# remove

**Type:** `Function`

**Instance of:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

remove, -- remove an entry from a mutable hash table, list, or database, class => element, Usage: , remove(T, k), class => language-macaulay2, Inputs:, T, , , a , Macaulay2Doc :: HashTable, hash table, , , a , Macaulay2Doc :: MutableList, mutable list, , or , a , Macaulay2Doc :: Database, database, , , k, , , the key to remove (must be , an , Macaulay2Doc :: ZZ, integer, if , T, is a mutable list or , a , Macaulay2Doc :: String, string, if it is a database), Outputs:, a , Macaulay2Doc :: Thing, thing, , , the removed value; , Description, , remove(T, k), removes and returns the entry of , T, stored under the key , k, ., class => examples, i1 : T = new MutableHashTable from {a => 1, b => 2, c => 3; peek T

o2 = MutableHashTable{a => 1
 b => 2
 c => 3, class => language-macaulay2, i3 : remove(T, a)

o3 = 1, class => language-macaulay2, i4 : peek T

o4 = MutableHashTable{b => 2
 c => 3, class => language-macaulay2, If , T, is not a mutable hash table, an error is thrown. One way to remove an entry from an immutable hash table is with the function , Macaulay2Doc :: applyPairs, :, class => examples, i5 : T = new HashTable from {a => 1, b => 2, c => 3

o5 = HashTable{a => 1
 b => 2
 c => 3

o5 : HashTable, class => language-macaulay2, i6 : T = applyPairs(T, (k, v) -> if k =!= a then (k, v))

o6 = HashTable{b => 2
 c => 3

o6 : HashTable, class => language-macaulay2, remove, works similarly when , T, is a database. See , Macaulay2Doc :: Database, for more information., If , T, is a mutable list, then , k, gives the index of the element to be removed., class => examples, i7 : T = new MutableList from {1, 2, 3, 4; peek T

o8 = MutableList{1, 2, 3, 4, class => language-macaulay2, i9 : remove(T, 0)

o9 = 1, class => language-macaulay2, i10 : peek T

o10 = MutableList{2, 3, 4, class => language-macaulay2, If , k, is negative, then the index is determined by counting backwards from the end of , T, ., class => examples, i11 : remove(T, -1)

o11 = 4, class => language-macaulay2, i12 : peek T

o12 = MutableList{2, 3, class => language-macaulay2, See also, Macaulay2Doc :: applyKeys, Macaulay2Doc :: applyPairs, Macaulay2Doc :: applyValues, Macaulay2Doc :: delete, Macaulay2Doc :: drop, Macaulay2Doc :: keys, Macaulay2Doc :: isMutable, Macaulay2Doc :: scanKeys, Macaulay2Doc :: hash tables, class => waystouse, Ways to use , remove, :, remove(Database,String), remove(HashTable,Thing), remove(MutableList,ZZ), class => waystouse, For the programmer, The object , Macaulay2Doc :: remove, is , a , Macaulay2Doc :: CompiledFunction, compiled function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/ov_hashtables.m2:542:0, .

## Installations

Type signatures for this function:

- `remove(MutableList, ZZ)`
- `remove(HashTable, Thing)`
- `remove(Database, String)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

