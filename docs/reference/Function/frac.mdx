---
title: "frac"
kind: Function
---

# frac

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

frac, -- construct a fraction field, class => element, Usage: , frac R, class => language-macaulay2, Inputs:, R, , , a , Macaulay2Doc :: Ring, ring, , , an integral domain, Outputs:, a , Macaulay2Doc :: FractionField, fraction field, , , the field of fractions of , R, Description, class => examples, i1 : F = frac ZZ

o1 = QQ

o1 : Ring, class => language-macaulay2, i2 : F = frac (ZZ[a,b])

o2 = F

o2 : FractionField, class => language-macaulay2, After invoking the , frac, command, , the elements of the ring are treated as elements , of the fraction field:, class => examples, i3 : R = ZZ/101[x,y];, class => language-macaulay2, i4 : gens gb ideal(x^2*y - y^3)

o4 = | x2y-y3 |

 1 1
o4 : Matrix R <-- R, class => language-macaulay2, i5 : K = frac R;, class => language-macaulay2, i6 : gens gb ideal(x^2*y - y^3)

o6 = | 1 |

 1 1
o6 : Matrix K <-- K, class => language-macaulay2, Another way to obtain , frac R, is with , x, Macaulay2Doc :: /, y, where , x, y, are elements of , R, :, class => examples, i7 : a*b/b^4

 a
o7 = --
 3
 b

o7 : F, class => language-macaulay2, Fractions are reduced to the extent possible., class => examples, i8 : f = (x-y)/(x^6-y^6)

 1
o8 = ----------------------------------
 5 4 3 2 2 3 4 5
 x + x y + x y + x y + x*y + y

o8 : K, class => language-macaulay2, i9 : (x^3 - y^3) * f

 x - y
o9 = -------
 3 3
 x + y

o9 : K, class => language-macaulay2, The parts of a fraction may be extracted., class => examples, i10 : numerator f

o10 = 1

o10 : R, class => language-macaulay2, i11 : denominator f

 5 4 3 2 2 3 4 5
o11 = x + x y + x y + x y + x*y + y

o11 : R, class => language-macaulay2, Alternatively, the functions , Macaulay2Doc :: lift, and , Macaulay2Doc :: liftable, can be used., class => examples, i12 : liftable(1/f,R)

o12 = true, class => language-macaulay2, i13 : liftable(f,R)

o13 = false, class => language-macaulay2, i14 : lift(1/f,R)

 5 4 3 2 2 3 4 5
o14 = x + x y + x y + x y + x*y + y

o14 : R, class => language-macaulay2, One can form resolutions and Gröbner bases of ideals in polynomial rings over fraction fields, as in the following example. Note that computations over fraction fields can be quite slow., class => examples, i15 : S = K[u,v];, class => language-macaulay2, i16 : I = ideal(y^2*u^3 + x*v^3, u^2*v, u^4);

o16 : Ideal of S, class => language-macaulay2, i17 : gens gb I

o17 = | u2v u3+x/y2v3 v4 uv3 |

 1 4
o17 : Matrix S <-- S, class => language-macaulay2, i18 : Ires = res I

 1 3 2
o18 = S <-- S <-- S <-- 0
 
 0 1 2 3

o18 : ChainComplex, class => language-macaulay2, i19 : Ires.dd_2

o19 = {3 | 0 y2/xuv |
 {3 | -v2 -y2/xu2 |
 {4 | u -v |

 3 2
o19 : Matrix S <-- S, class => language-macaulay2, One way to compute a blowup of an ideal , I, in , R, , is to compute the kernel of a map of a new polynomial ring into a fraction field of , R, , as shown below., class => examples, i20 : A = ZZ/101[a,b,c];, class => language-macaulay2, i21 : f = map(K, A, {x^3/y^4, x^2/y^2, (x^2+y^2)/y^4);

o21 : RingMap K <-- A, class => language-macaulay2, i22 : kernel f

 3 2 2 3 2 3 3
o22 = ideal (b c - a b - a , a*b c - a b*c - a c)

o22 : Ideal of A, class => language-macaulay2, Caveat, The input ring should be an integral domain., , Currently, for , S, as above, one cannot define , frac S, or fractions , u/v, . One can get around that by defining , B = ZZ/101[x,y,u,v], and identify , frac S, with , frac B, ., , Note that expressions such as , frac QQ[x], are parsed as , (frac QQ)[x], . To obtain the fraction field of , QQ[x], use instead , frac (QQ[x]), ., See also, Macaulay2Doc :: numerator, Macaulay2Doc :: denominator, Macaulay2Doc :: liftable, Macaulay2Doc :: lift, class => waystouse, Ways to use , frac, :, frac(EngineRing), frac(FractionField), frac(Ring), class => waystouse, For the programmer, The object , Macaulay2Doc :: frac, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/frac-doc.m2:85:0, .

## Installations

Type signatures for this function:

- `frac(Ring)`
- `frac(EngineRing)`
- `frac(FractionField)`
- `frac(InexactField)`

## Examples

```macaulay2
i1 : F = frac ZZ

o1 = QQ

o1 : Ring

i2 : F = frac (ZZ[a,b])

o2 = F

o2 : FractionField
```

## See Also

