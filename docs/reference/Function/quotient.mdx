---
title: "quotient"
kind: Function
---

# quotient

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

quotient, -- quotient or division, empty documentation body, class => waystouse, Ways to use , quotient, :, quotient(Ideal), -- see , Macaulay2Doc :: comodule, quotient(Module), -- see , Macaulay2Doc :: comodule, quotient(Matrix,GroebnerBasis), -- see , Macaulay2Doc :: quotient(Matrix,Matrix), Macaulay2Doc :: quotient(Matrix,Matrix), quotient(Ideal,Ideal), -- see , Saturation :: quotient(Module,Module), quotient(Ideal,RingElement), -- see , Saturation :: quotient(Module,Module), quotient(Module,Ideal), -- see , Saturation :: quotient(Module,Module), Saturation :: quotient(Module,Module), quotient(Module,RingElement), -- see , Saturation :: quotient(Module,Module), quotient(MonomialIdeal,RingElement), -- see , Saturation :: quotient(Module,Module), Varieties :: quotient(SheafMap,SheafMap), class => waystouse, For the programmer, The object , Macaulay2Doc :: quotient, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/quotient-remainder-doc.m2:4:0, .

## Installations

Type signatures for this function:

- `quotient(Matrix, GroebnerBasis)`
- `quotient(Module)`
- `quotient(Ideal)`
- `quotient(Matrix, Matrix)`
- `quotient(Ideal, Ideal)`
- `quotient(Ideal, RingElement)`
- `quotient(Ideal, Number)`
- `quotient(MonomialIdeal, RingElement)`
- `quotient(Module, Ideal)`
- `quotient(Module, RingElement)`
- `quotient(Module, Number)`
- `quotient(Module, Module)`
- `quotient(SheafMap, SheafMap)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

