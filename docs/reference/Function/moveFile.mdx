---
title: "moveFile"
kind: Function
---

# moveFile

**Type:** `Function`

**Instance of:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

moveFile, , class => element, Usage: , moveFile(src,dst), class => language-macaulay2, Inputs:, src, , , a , Macaulay2Doc :: String, string, , , the filename or path to an existing file, dst, , , a , Macaulay2Doc :: String, string, , , the new filename or path to a location (on the same file system). Omit this argument and an appropriately numbered backup filename will be invented., Macaulay2Doc :: using functions with optional inputs, Optional inputs, :, Verbose, => , a , Macaulay2Doc :: Boolean, Boolean value, , , default value , false, , , whether to report individual file operations, Outputs:, the name of the backup file if one was created, or , Macaulay2Doc :: null, Consequences:, the file will be moved by creating a new link to the file and removing the old one, Description, class => examples, i1 : src = temporaryFileName()

o1 = /tmp/M2-705706-0/0, class => language-macaulay2, i2 : dst = temporaryFileName()

o2 = /tmp/M2-705706-0/1, class => language-macaulay2, i3 : src << "hi there" << close

o3 = /tmp/M2-705706-0/0

o3 : File, class => language-macaulay2, i4 : moveFile(src,dst,Verbose=>true)
--moving: /tmp/M2-705706-0/0 -> /tmp/M2-705706-0/1, class => language-macaulay2, i5 : get dst

o5 = hi there, class => language-macaulay2, i6 : bak = moveFile(dst,Verbose=>true)
--backup file created: /tmp/M2-705706-0/1.bak

o6 = /tmp/M2-705706-0/1.bak, class => language-macaulay2, i7 : removeFile bak, class => language-macaulay2, See also, Macaulay2Doc :: copyFile, class => waystouse, Ways to use , moveFile, :, moveFile(String), Macaulay2Doc :: moveFile(String,String), class => waystouse, For the programmer, The object , Macaulay2Doc :: moveFile, is , a , Macaulay2Doc :: MethodFunctionWithOptions, method function with options, ., , class => waystouse, The source of this document is in , Macaulay2Doc/ov_system.m2:1219:0, .

## Installations

Type signatures for this function:

- `moveFile(String, String)`
- `moveFile(String)`

## Examples

```macaulay2
i1 : src = temporaryFileName()

o1 = /tmp/M2-705706-0/0

i2 : dst = temporaryFileName()

o2 = /tmp/M2-705706-0/1
```

## See Also

