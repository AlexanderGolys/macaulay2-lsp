---
title: "quotientRemainder"
kind: Function
---

# quotientRemainder

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

quotientRemainder, -- matrix quotient and remainder, class => element, Usage: , (q,r) = quotientRemainder(f,g), class => language-macaulay2, Inputs:, f, , , a , Macaulay2Doc :: Matrix, matrix, g, , , a , Macaulay2Doc :: GroebnerBasis, Gröbner basis, or , a , Macaulay2Doc :: Matrix, matrix, , with the same target as , f, Outputs:, q, , , the quotient of , f, upon division by , g, r, , , the remainder of , f, upon division by , g, Description, The equation , g*q+r == f, will hold. The source of , f, should be a free module., class => examples, i1 : R = ZZ[x,y]

o1 = R

o1 : PolynomialRing, class => language-macaulay2, i2 : f = random(R^2,R^{2:-1)

o2 = | 8x+y 8x+3y |
 | 3x+7y 3x+7y |

 2 2
o2 : Matrix R <-- R, class => language-macaulay2, i3 : g = vars R ++ vars R

o3 = | x y 0 0 |
 | 0 0 x y |

 2 4
o3 : Matrix R <-- R, class => language-macaulay2, i4 : (q,r) = quotientRemainder(f,g)

o4 = ({1 | 8 8 |, 0)
 {1 | 1 3 |
 {1 | 3 3 |
 {1 | 7 7 |

o4 : Sequence, class => language-macaulay2, i5 : g*q+r == f

o5 = true, class => language-macaulay2, i6 : f = f + map(target f, source f, id_(R^2))

o6 = | 8x+y+1 8x+3y |
 | 3x+7y 3x+7y+1 |

 2 2
o6 : Matrix R <-- R, class => language-macaulay2, i7 : (q,r) = quotientRemainder(f,g)

o7 = ({1 | 8 8 |, | 1 0 |)
 {1 | 1 3 | | 0 1 |
 {1 | 3 3 |
 {1 | 7 7 |

o7 : Sequence, class => language-macaulay2, i8 : g*q+r == f

o8 = true, class => language-macaulay2, See also, Macaulay2Doc :: quotientRemainder', class => waystouse, Ways to use , quotientRemainder, :, quotientRemainder(Matrix,GroebnerBasis), quotientRemainder(Matrix,Matrix), quotientRemainder(InexactNumber,RingElement), -- see , Macaulay2Doc :: quotientRemainder(RingElement,RingElement), quotientRemainder(Number,RingElement), -- see , Macaulay2Doc :: quotientRemainder(RingElement,RingElement), quotientRemainder(RingElement,InexactNumber), -- see , Macaulay2Doc :: quotientRemainder(RingElement,RingElement), quotientRemainder(RingElement,Number), -- see , Macaulay2Doc :: quotientRemainder(RingElement,RingElement), Macaulay2Doc :: quotientRemainder(RingElement,RingElement), quotientRemainder(ZZ,ZZ), -- see , Macaulay2Doc :: quotientRemainder(RingElement,RingElement), class => waystouse, For the programmer, The object , Macaulay2Doc :: quotientRemainder, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/quotient-remainder-doc.m2:205:0, .

## Installations

Type signatures for this function:

- `quotientRemainder(InexactNumber, RingElement)`
- `quotientRemainder(RingElement, Number)`
- `quotientRemainder(Number, RingElement)`
- `quotientRemainder(RingElement, InexactNumber)`
- `quotientRemainder(RingElement, RingElement)`
- `quotientRemainder(Matrix, GroebnerBasis)`
- `quotientRemainder(Matrix, Matrix)`
- `quotientRemainder(ZZ, ZZ)`

## Examples

```macaulay2
i1 : R = ZZ[x,y]

o1 = R

o1 : PolynomialRing
```

## See Also

