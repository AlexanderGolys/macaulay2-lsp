---
title: "diagonalMatrix"
kind: Function
---

# diagonalMatrix

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

diagonalMatrix, -- make a diagonal matrix, empty documentation body, See also, Macaulay2Doc :: matrices, Macaulay2Doc :: diagonalMatrix(Matrix), Macaulay2Doc :: diagonalMatrix(Ring,ZZ,ZZ,List), class => waystouse, Ways to use , diagonalMatrix, :, Macaulay2Doc :: diagonalMatrix(Matrix), diagonalMatrix(List), -- see , Macaulay2Doc :: diagonalMatrix(Ring,ZZ,ZZ,List), diagonalMatrix(Ring,List), -- see , Macaulay2Doc :: diagonalMatrix(Ring,ZZ,ZZ,List), Macaulay2Doc :: diagonalMatrix(Ring,ZZ,ZZ,List), diagonalMatrix(RingFamily,List), -- see , Macaulay2Doc :: diagonalMatrix(Ring,ZZ,ZZ,List), diagonalMatrix(RingFamily,ZZ,ZZ,List), -- see , Macaulay2Doc :: diagonalMatrix(Ring,ZZ,ZZ,List), diagonalMatrix(ZZ,ZZ,List), -- see , Macaulay2Doc :: diagonalMatrix(Ring,ZZ,ZZ,List), class => waystouse, For the programmer, The object , Macaulay2Doc :: diagonalMatrix, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/diagonalMatrix-doc.m2:14:0, .

## Installations

Type signatures for this function:

- `diagonalMatrix(RingFamily, ZZ, ZZ, List)`
- `diagonalMatrix(Ring, ZZ, ZZ, List)`
- `diagonalMatrix(ZZ, ZZ, List)`
- `diagonalMatrix(RingFamily, List)`
- `diagonalMatrix(Ring, List)`
- `diagonalMatrix(List)`
- `diagonalMatrix(Matrix)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

