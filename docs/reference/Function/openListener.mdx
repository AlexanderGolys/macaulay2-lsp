---
title: "openListener"
kind: Function
---

# openListener

**openListener  -- open a port for listening**

**Type:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

Use `openInOut f`  to accept an incoming connection on the listener returning a new input output file that serves as the connection.  The function [isReady](../Function/op_Coredollar_isReady.mdx)  can be used to determine whether an incoming connection has arrived without blocking.

## Examples

```macaulay2
f = openListener s
```

## Additional Information

- Inputs:
- `s` a [ string](../Type/op_Coredollar_String.mdx) of the form `"$interface:port"`. Both parts are optional. If the port is omitted the colon is optional.
- Outputs:
- `f` a [ file](../Type/op_Coredollar_File.mdx) an open listener on the specified interface of the local host at the specified service port. If the port is omitted it is taken to be port 2500. If the interface is omitted the listener accepts connections on all interfaces.


### See also


- Macaulay2Doc:: openInOut
- Macaulay2Doc:: isReady

## Debug info

```python
{
    "obj_type": "Function",
    "name": "openListener",
    "safe_name": "op_Coredollar_openListener",
    "type": "CompiledFunction",
    "description": "Use `openInOut f`  to accept an incoming connection on the listener returning a new input output fil...",
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$openListener",
        "full_doc": "... (1503 bytes) ...",
        "parsed_headline": "openListener  -- open a port for listening"
    }
}
```
