---
title: "isAffineRing"
kind: Function
---

# isAffineRing

**Type:** `Function`

**Instance of:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

isAffineRing, -- whether something is an affine ring, class => element, Usage: , isAffineRing R, class => language-macaulay2, Inputs:, R, , , a , Macaulay2Doc :: Ring, ring, Outputs:, a , Macaulay2Doc :: Boolean, Boolean value, , , Macaulay2Doc :: true, if , R, is an affine ring and , Macaulay2Doc :: false, otherwise, Description, For our purposes, an affine ring is a quotient of a (not necessarily commutative) , Macaulay2Doc :: PolynomialRing, polynomial ring, over a field., class => examples, i1 : isAffineRing (ZZ[a,b,c,d])

o1 = false, class => language-macaulay2, i2 : isAffineRing (ZZ/101[a,b,c,d])

o2 = true, class => language-macaulay2, i3 : isAffineRing (ZZ/2[x,y,z]/(x^2-y*z))

o3 = true, class => language-macaulay2, i4 : isAffineRing (QQ[x,dx, WeylAlgebra => {x => dx])

o4 = false, class => language-macaulay2, See also, Macaulay2Doc :: coefficientRing, Macaulay2Doc :: isField, Macaulay2Doc :: ambient, class => waystouse, Ways to use , isAffineRing, :, isAffineRing(PolynomialRing), isAffineRing(QuotientRing), isAffineRing(Ring), class => waystouse, For the programmer, The object , Macaulay2Doc :: isAffineRing, is , a , Macaulay2Doc :: MethodFunction, method function, ., , class => waystouse, The source of this document is in , Macaulay2Doc/functions/isAffineRing-doc.m2:26:0, .

## Installations

Type signatures for this function:

- `isAffineRing(PolynomialRing)`
- `isAffineRing(QuotientRing)`
- `isAffineRing(Ring)`

## Examples

```macaulay2
i1 : isAffineRing (ZZ[a,b,c,d])

o1 = false

i2 : isAffineRing (ZZ/101[a,b,c,d])

o2 = true
```

## See Also

