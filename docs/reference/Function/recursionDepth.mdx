---
title: "recursionDepth"
kind: Function
---

# recursionDepth

**recursionDepth  -- the current recursion depth**

**Type:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

 Tail recursion optimization may be in effect so there the answer may not always be what you expect.

## Examples

```macaulay2
recursionDepth()
```

```macaulay2
recursionDepth()

```

```text
14
```

```macaulay2
f = x -> recursionDepth()

```

```text
f

FunctionClosure
```

```macaulay2
r = i -> if i == 100 then recursionDepth() else r(i+1)

```

```text
r

FunctionClosure
```

## Additional Information

- Outputs:
- an [ integer](../Type/ZZ.mdx) the current depth of recursion in the interpreter


### See also


- Macaulay2Doc:: recursionLimit

## Debug info

```python
{
    "obj_type": "Function",
    "name": "recursionDepth",
    "safe_name": "op_Coredollar_recursionDepth",
    "type": "CompiledFunction",
    "description": " Tail recursion optimization may be in effect so there the answer may not always be what you expect....",
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$recursionDepth",
        "full_doc": "... (1473 bytes) ...",
        "parsed_headline": "recursionDepth  -- the current recursion depth"
    }
}
```
