---
title: "mergePairs"
kind: Function
---

# mergePairs

**mergePairs  -- merge sorted lists of pairs**

**Type:** [CompiledFunction](../Type/CompiledFunction.mdx)

## Description

This function merges sorted lists of pairs.It merges `x` and `y` which should be lists of pairs `(kv)` arranged in increasing order according to the key `k`. The result will be a list of pairs also arranged in increasing order each of which is either from `x` or from `y` or in the case where a key `k` occurs in both with say `(kv)` in `x` and `(kw)` in `y` then the result will contain the pair `(kf(vw))`. Thus the function `f` is used for combining the values when the keys collide.The class of the result is taken to be the minimal common ancestor of the class of `x` and the class of `y`.

## Examples

```macaulay2
mergePairs(xyf)
```

## Additional Information

- Inputs:
- `x` a [ basic list](../Type/op_Coredollar_BasicList.mdx)
- `y` a [ basic list](../Type/op_Coredollar_BasicList.mdx)
- `f` a [ function](../Type/Function.mdx)
- Outputs:
- a [ basic list](../Type/op_Coredollar_BasicList.mdx)


### See also


- Macaulay2Doc:: merge

## Debug info

```python
{
    "obj_type": "Function",
    "name": "mergePairs",
    "safe_name": "op_Coredollar_mergePairs",
    "type": "CompiledFunction",
    "description": "This function merges sorted lists of pairs.It merges `x` and `y` which should be lists of pairs `(kv...",
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$mergePairs",
        "full_doc": "... (1828 bytes) ...",
        "parsed_headline": "mergePairs  -- merge sorted lists of pairs"
    }
}
```
