---
title: ">>"
kind: Operator
---

# >>

**Type:** `Operator`

**Instance of:** [Keyword](../Type/Keyword.mdx)

## Description

>>, -- a binary operator, used for bit shifting or attaching optional inputs to functions, empty documentation body, See also, Macaulay2Doc :: OptionTable >> Function, Macaulay2Doc :: right shift, class => waystouse, Ways to use , symbol >>, :, Boolean >> Function, -- see , Macaulay2Doc :: OptionTable >> Function, List >> Function, -- see , Macaulay2Doc :: OptionTable >> Function, Macaulay2Doc :: OptionTable >> Function, CC >> ZZ, -- see , Macaulay2Doc :: right shift, RR >> ZZ, -- see , Macaulay2Doc :: right shift, RRi >> ZZ, -- see , Macaulay2Doc :: right shift, ZZ >> ZZ, -- see , Macaulay2Doc :: right shift, class => waystouse, For the programmer, The object , Macaulay2Doc :: >>, is , a , Macaulay2Doc :: Keyword, keyword, ., This operator may be used as a binary operator in an expression like , x>>y, . , The user may install , Macaulay2Doc :: binary methods, for handling such expressions with code such as, X >> Y := (x,y) -> ..., where , X, is the class of , x, and , Y, is the class of , y, ., , class => waystouse, The source of this document is in , Macaulay2Doc/operators/shift.m2:24:0, .

## Operator Installations

### Binary Operator

Usage: `x >> y`

- `>>(OptionTable, Function)`
- `>>(List, Function)`
- `>>(Boolean, Function)`
- `>>(Thing, Thing)`
- `>>(CC, ZZ)`
- `>>(RR, ZZ)`
- `>>(ZZ, ZZ)`
- `>>(RRi, ZZ)`

### Augmented Assignment

Usage: `x >>= y` (modifies `x` in place)

:::info[Special Installation Syntax]
Augmented assignment methods require special installation using `installAssignmentMethod`. See [Macaulay2 documentation](https://macaulay2.com/doc/Macaulay2/share/doc/Macaulay2/Macaulay2Doc/html/_installing_spaugmented_spassignment_spmethods.html) for details.
:::

- `(>>,=)(Type, Type)`
- `(>>,=)(Thing, Thing)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

