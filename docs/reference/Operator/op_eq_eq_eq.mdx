---
title: "==="
kind: Operator
---

# ===

**Type:** `Operator`

**Instance of:** [Keyword](../Type/Keyword.mdx)

## Description

===, -- strict equality, class => element, Usage: , x === y, class => language-macaulay2, Inputs:, x, y, Outputs:, a , Macaulay2Doc :: Boolean, Boolean value, , , whether the expressions , x, and , y, are strictly equal, Description, Strictly equal expressions have the same type, so , 0===0., and , 0===0/1, are false; the three types involved here are , Macaulay2Doc :: ZZ, , , Macaulay2Doc :: RR, , and , Macaulay2Doc :: QQ, ., If x and y are , Macaulay2Doc :: isMutable, then they are strictly equal only if they are identical (i.e., at the same address in memory). For details about why strict equality cannot depend on the contents of mutable hash tables, see , Macaulay2Doc :: hashing, . On the other hand, if x and y are non-mutable, then they are strictly equal if and only if all their contents are strictly equal., class => examples, i1 : {1,2,3 === {1,2,3

o1 = true, class => language-macaulay2, i2 : {1,2,3 === {2,1,3

o2 = false, class => language-macaulay2, For some types, such as ring elements and matrices, strict equality is the same as mathematical equality. This tends to be the case for objects for which not much computation is not required to test equality. For certain other types, such as , Macaulay2Doc :: Ideal, or , Macaulay2Doc :: Module, , where extensive computations may be required, the operator , Macaulay2Doc :: ==, implements the desired comparison., class => examples, i3 : R = QQ[a..d];, class => language-macaulay2, i4 : a^2+b === b+a^2

o4 = true, class => language-macaulay2, i5 : ideal(a^2+b,c*d) === ideal(b+a^2,c*d+b+a^2)

o5 = false, class => language-macaulay2, i6 : matrix{{a,b,c === matrix{{a,b,c

o6 = true, class => language-macaulay2, i7 : matrix{{a,b,c === transpose matrix{{a,{b,{c

o7 = false, class => language-macaulay2, As it happens, polynomial rings are mutable objects, and new ones are easily created, which are distinct from each other. For example, the rings , A, and , B, below are not strictly equal., class => examples, i8 : A = QQ[x]; B = QQ[x];, class => language-macaulay2, i10 : A === B

o10 = false, class => language-macaulay2, See also, Macaulay2Doc :: ==, Macaulay2Doc :: =!=, Macaulay2Doc :: operators, class => waystouse, Ways to use , symbol ===, :, Thing === Thing, class => waystouse, For the programmer, The object , Macaulay2Doc :: ===, is , a , Macaulay2Doc :: Keyword, keyword, ., , class => waystouse, The source of this document is in , Macaulay2Doc/operators/equality.m2:50:0, .

## Operator Installations

### Binary Operator

Usage: `x === y`

- `===(Thing, Thing)`

## Examples

*Examples would be extracted from M2 documentation here*

## See Also

