---
title: "*"
kind: Operator
---

# *

**Type:** `Operator`

**Instance of:** [Keyword](../Type/Keyword.mdx)

## Description

*, -- a binary operator, usually used for multiplication, class => element, Usage: , x * y, class => language-macaulay2, Description, The return type depends on the types of x and y. If they have the same type, then usually the return type is the common type of x and y., , Multiplication involving ring elements (including integers, rational numbers, real and complex numbers), ideals, vectors, matrices, modules is generally the usual multiplication, or composition of functions., , The intersection of sets is given by multiplication. See , Macaulay2Doc :: Set * Set, ., class => examples, i1 : set{hi,you,there * set{hi,us,here,you

o1 = set {you, hi

o1 : Set, class => language-macaulay2, , Multiplication involving a list attempts to multiply each element of the list., class => examples, i2 : R = QQ[a..d];, class => language-macaulay2, i3 : a * {b,c,d

o3 = {a*b, a*c, a*d

o3 : List, class => language-macaulay2, , Multiplication of matrices (, Macaulay2Doc :: Matrix * Matrix, ) or ring maps is the same as composition., class => examples, i4 : f = map(R,R,{b,c,a,d)

o4 = map (R, R, {b, c, a, d)

o4 : RingMap R <-- R, class => language-macaulay2, i5 : g = map(R,R,{(a+b)^2,b^2,c^2,d^2)

 2 2 2 2 2
o5 = map (R, R, {a + 2a*b + b , b , c , d )

o5 : RingMap R <-- R, class => language-macaulay2, i6 : f*g

 2 2 2 2 2
o6 = map (R, R, {b + 2b*c + c , c , a , d )

o6 : RingMap R <-- R, class => language-macaulay2, i7 : (f*g)(a) == f(g(a))

o7 = true, class => language-macaulay2, , Submodules of modules may be produced using multiplication and addition., class => examples, i8 : M = R^2; I = ideal(a+b,c);

o9 : Ideal of R, class => language-macaulay2, i10 : N = I*M + a*R^2

o10 = image | a+b 0 c 0 a 0 |
 | 0 a+b 0 c 0 a |

 2
o10 : R-module, submodule of R, class => language-macaulay2, i11 : isHomogeneous N

o11 = true, class => language-macaulay2, Intervals, If one of the factors is an , Macaulay2Doc :: RRi, , the output is an interval containing all products of pairs in the factors., class => examples, i12 : 2*interval(1,3)

o12 = [2,6]

o12 : RRi (of precision 53), class => language-macaulay2, i13 : interval(1,3)*interval(-1,2)

o13 = [-3,6]

o13 : RRi (of precision 53), class => language-macaulay2, i14 : interval(-1,1)*interval(-1,1)

o14 = [-1,1]

o14 : RRi (of precision 53), class => language-macaulay2, See also, Macaulay2Doc :: times, Macaulay2Doc :: product, class => waystouse, Ways to use , symbol *, :, CC * CC, CC * QQ, CC * RR, CC * ZZ, Constant * Constant, Constant * InexactNumber, Constant * Number, Ideal * Module, Ideal * Ring, Ideal * Vector, InexactNumber * Constant, List * Thing, Matrix * Number, Matrix * RingElement, Matrix * Vector, MonomialIdeal * MonomialIdeal, MonomialIdeal * Ring, MutableMatrix * MutableMatrix, Number * Constant, Number * Matrix, Number * Vector, ProjectiveHilbertPolynomial * ZZ, QQ * CC, QQ * QQ, QQ * RR, QQ * RRi, QQ * ZZ, Ring * Ideal, Ring * MonomialIdeal, Ring * RingElement, Ring * Vector, RingElement * Ideal, RingElement * Matrix, RingElement * Module, RingElement * MonomialIdeal, RingElement * MutableMatrix, RingElement * RingElement, RingElement * Vector, RingMap * RingMap, RR * CC, RR * QQ, RR * RR, RR * RRi, RR * ZZ, RRi * QQ, RRi * RR, RRi * RRi, RRi * ZZ, Thing * List, Vector * Number, Vector * RingElement, ZZ * CC, ZZ * ProjectiveHilbertPolynomial, ZZ * QQ, ZZ * RR, ZZ * RRi, ZZ * ZZ, QQ * BettiTally, -- see , Macaulay2Doc :: BettiTally, ZZ * BettiTally, -- see , Macaulay2Doc :: BettiTally, OldChainComplexes :: ChainComplexMap * ChainComplexMap, Polyhedra :: Cone * Cone, Polyhedra :: Cone * Polyhedron, Polyhedra :: Fan * Fan, OldChainComplexes :: GradedModuleMap * GradedModuleMap, Varieties :: Ideal * CoherentSheaf, Macaulay2Doc :: Ideal * Ideal, Set * Set, -- see , Macaulay2Doc :: intersect(Set,Set), Macaulay2Doc :: Matrix * Matrix, Polyhedra :: Polyhedron * Cone, Polyhedra :: Polyhedron * Polyhedron, Polyhedra :: QQ * Polyhedron, ZZ * Polyhedron, -- see , Polyhedra :: QQ * Polyhedron, OldChainComplexes :: RingElement * ChainComplexMap, OldChainComplexes :: RingElement * GradedModuleMap, Varieties :: SheafMap * RingElement, Varieties :: SheafMap * ZZ, OldChainComplexes :: ZZ * ChainComplexMap, OldChainComplexes :: ZZ * GradedModuleMap, class => waystouse, For the programmer, The object , Macaulay2Doc :: *, is , a , Macaulay2Doc :: Keyword, keyword, ., This operator may be used as a binary operator in an expression like , x*y, . , The user may install , Macaulay2Doc :: binary methods, for handling such expressions with code such as, X * Y := (x,y) -> ..., where , X, is the class of , x, and , Y, is the class of , y, ., This operator may be used as a prefix unary operator in an expression like , *y, . , The user may , Macaulay2Doc :: installing methods, install a method, for handling such expressions with code such as, * Y := (y) -> ..., where , Y, is the class of , y, ., , class => waystouse, The source of this document is in , Macaulay2Doc/operators/times.m2:148:0, .

## Operator Installations

### Binary Operator

Usage: `x * y`

- `*(QQ, BettiTally)`
- `*(ZZ, BettiTally)`
- `*(Ideal, Module)`
- `*(Number, RingElement)`
- `*(MonomialIdeal, MonomialIdeal)`
- `*(InexactNumber, RingElement)`
- `*(Ideal, Ideal)`
- `*(RingElement, Number)`
- `*(RingElement, InexactNumber)`
- `*(RingElement, RingElement)`
- `*(Holder, Product)`
- `*(Product, Holder)`
- `*(Expression, Expression)`
- `*(Expression, Thing)`
- `*(Thing, Expression)`
- `*(Expression, OneExpression)`
- `*(OneExpression, Expression)`
- `*(Product, OneExpression)`
- `*(OneExpression, Product)`
- `*(ZeroExpression, Expression)`

*Showing 20 of 130 binary signatures*

### Unary Operator

Usage: `* x` or `x *`

- `*(String)`
- `*(Thing)`

### Augmented Assignment

Usage: `x *= y` (modifies `x` in place)

:::info[Special Installation Syntax]
Augmented assignment methods require special installation using `installAssignmentMethod`. See [Macaulay2 documentation](https://macaulay2.com/doc/Macaulay2/share/doc/Macaulay2/Macaulay2Doc/html/_installing_spaugmented_spassignment_spmethods.html) for details.
:::

- `(*,=)(Type, Type)`
- `(*,=)(Type)`
- `(*,=)(Thing, Thing)`
- `(*,=)(Thing)`

## Examples

```macaulay2
i2 : R = QQ[a..d];
```

## See Also

