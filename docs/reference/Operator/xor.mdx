---
title: "xor"
kind: Operator
---

# xor

**Type:** `Operator`

**Instance of:** [Keyword](../Type/Keyword.mdx)

## Description

xor, -- exclusive disjunction, class => element, Usage: , t xor u, class => language-macaulay2, Inputs:, t, , , a , Macaulay2Doc :: Boolean, Boolean value, or , a , Macaulay2Doc :: Function, function, , , u, , , a , Macaulay2Doc :: Boolean, Boolean value, or , a , Macaulay2Doc :: Function, function, , , Outputs:, a , Macaulay2Doc :: Boolean, Boolean value, or , a , Macaulay2Doc :: Function, function, , , Description, If , t, and , u, are booleans, then , t xor u, class => language-macaulay2, returns true if exactly one of them is true., class => examples, i1 : even 7 xor isPrime 7

o1 = true, class => language-macaulay2, If they are both functions than return booleans, then the return value is also a function., class => examples, i2 : isEvenXorPrime = even xor isPrime

o2 = isEvenXorPrime

o2 : FunctionClosure, class => language-macaulay2, i3 : isEvenXorPrime 7

o3 = true, class => language-macaulay2, i4 : isEvenXorPrime 2

o4 = false, class => language-macaulay2, See also, Macaulay2Doc :: and, Macaulay2Doc :: or, Macaulay2Doc :: not, class => waystouse, Ways to use , symbol xor, :, Boolean xor Boolean, Function xor Function, class => waystouse, For the programmer, The object , Macaulay2Doc :: xor, is , a , Macaulay2Doc :: Keyword, keyword, ., This operator may be used as a binary operator in an expression like , x xor y, . , The user may install , Macaulay2Doc :: binary methods, for handling such expressions with code such as, X xor Y := (x,y) -> ..., where , X, is the class of , x, and , Y, is the class of , y, ., , class => waystouse, The source of this document is in , Macaulay2Doc/operators.m2:428:0, .

## Operator Installations

### Binary Operator

Usage: `x xor y`

- `xor(Expression, Expression)`
- `xor(Expression, Holder)`
- `xor(Holder, Expression)`
- `xor(Holder, Holder)`
- `xor(Expression, Thing)`
- `xor(Thing, Expression)`
- `xor(Boolean, Boolean)`
- `xor(Function, Function)`
- `xor(Thing, Thing)`

### Augmented Assignment

Usage: `x xor= y` (modifies `x` in place)

:::info[Special Installation Syntax]
Augmented assignment methods require special installation using `installAssignmentMethod`. See [Macaulay2 documentation](https://macaulay2.com/doc/Macaulay2/share/doc/Macaulay2/Macaulay2Doc/html/_installing_spaugmented_spassignment_spmethods.html) for details.
:::

- `(xor,=)(Type, Type)`
- `(xor,=)(Thing, Thing)`

## Examples

```macaulay2
i1 : even 7 xor isPrime 7

o1 = true

i2 : isEvenXorPrime = even xor isPrime

o2 = isEvenXorPrime

o2 : FunctionClosure
```

## See Also

