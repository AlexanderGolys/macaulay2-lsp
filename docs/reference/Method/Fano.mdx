---
title: "Fano"
kind: Method
---

# Fano

**Fano  -- compute the ideal of a Fano scheme in the Grassmannian**

**Type:** [MethodFunction](../Type/MethodFunction.mdx)

## Description


### Fano scheme


- Inputs:
- `k` a positive integer less than `r`
- `I` an ideal representing a variety in projective `r` -space
- Outputs:
- the ideal of a Fano scheme in the Grassmannian

 Given an ideal `I`  representing a projective variety `X`  in `P^r`  a positive integer k<r and optionally a ring `GR`  with (exactly) `r+1`  choose `k+1`  variables representing the ambient space of the Grassmannian of k-planes in `P^r`  this routine returns the ideal in `GR`  of the Fano scheme that parametrizes the k-planes lying on `X` .  If the optional third argument is not present the routine fabricates its own ring and returns an ideal in it.
### Synopsis


- Inputs:
- `k` a positive integer less than `r`
- `I` an ideal representing a variety in projective `r` -space
- `GR`
- Outputs:
- the ideal of a Fano scheme in the Grassmannian

 Given an ideal `I`  representing a projective variety `X`  in `P^r`  a positive integer k<r and optionally a ring `GR`  with (exactly) `r+1`  choose `k+1`  variables representing the ambient space of the Grassmannian of k-planes in `P^r`  this routine returns the ideal in `GR`  of the Fano scheme that parametrizes the k-planes lying on `X` .  If the optional third argument is not present the routine fabricates its own ring and returns an ideal in it.

## Installed For

### Fano([ZZ](../Type/ZZ.mdx), [Ideal](../Type/Ideal.mdx))

### Fano([ZZ](../Type/ZZ.mdx), [Ideal](../Type/Ideal.mdx), [Ring](../Type/Ring.mdx))

## Additional Information

### See also


- Macaulay2Doc:: Grassmannian
- Macaulay2Doc:: Tutorial: Fano varieties

## Debug info

```python
{
    "obj_type": "Method",
    "name": "Fano",
    "safe_name": "op_Coredollar_Fano",
    "type": "MethodFunction",
    "description": "\n### Fano scheme\n\n\n- Inputs:\n- `k` a positive integer less than `r`\n- `I` an ideal representing a va...",
    "installations": [
        "<Installation Fano(ZZ, Ideal, Ring)>",
        "<Installation Fano(ZZ, Ideal)>"
    ],
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$Fano",
        "full_doc": "... (2398 bytes) ...",
        "parsed_headline": "Fano  -- compute the ideal of a Fano scheme in the Grassmannian"
    }
}
```
