---
title: "inversePermutation"
kind: Method
---

# inversePermutation

**inversePermutation  -- inverse permutation**

**Type:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

We compose permutations with `_` ; see [VisibleList _ List](../Instance/VisibleList _ List.mdx) .

## Installed For

### inversePermutation([VisibleList](../Type/VisibleList.mdx))

## Examples

```macaulay2
y = inversePermutation x
```

```macaulay2
y = inversePermutation x

```

```text
{5, 0, 1, 2, 3, 4}

List
```

## Additional Information

- Inputs:
- `x` a [ list](../Type/op_Coredollar_List.mdx) a list of length `n` whose elements are the numbers 0 1 2... `n-1` in some order representing the permutation defined by sending `i` to `x#i`
- Outputs:
- `y` a [ list](../Type/op_Coredollar_List.mdx) the list representing the inverse permutation of `x`

## Debug info

```python
{
    "obj_type": "Method",
    "name": "inversePermutation",
    "safe_name": "op_Coredollar_inversePermutation",
    "type": "MethodFunction",
    "description": "We compose permutations with `_` ; see [VisibleList _ List](../Instance/VisibleList _ List.mdx) ....",
    "installations": [
        "<Installation inversePermutation(VisibleList)>"
    ],
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$inversePermutation",
        "full_doc": "... (1974 bytes) ...",
        "parsed_headline": "inversePermutation  -- inverse permutation"
    }
}
```
