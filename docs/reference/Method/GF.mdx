---
title: "GF"
kind: Method
---

# GF

**GF  -- make a finite field**

**Type:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description


### Synopsis


- Inputs:
- `p` a prime number
- `n`
- [](../Instance/GF(....mdx)``a [ symbol](../Type/op_Coredollar_Symbol.mdx) default value null the name to use for the generator of the field. If null then a is used.
- [](../Instance/GF(....mdx)``an [ integer](../Type/ZZ.mdx) default value 10000 the limit on the size of a Galois field whose elements will be represented internally as powers of the primitive element
- Outputs:
- a [ Galois field](../Type/GaloisField.mdx) a finite field with `q = p^n` elements

 The generator of this ring is a primitive element: it generates the multiplicative group of non-zero elements.If the single argument form GF(q) is given q should be a prime power q = p^n
### Synopsis


- Inputs:
- `R` a [ ring](../Type/Ring.mdx) A quotient of a polynomial ring over `ZZ/p` in one variable modulo an irreducible polynomial
- [](../Instance/GF(....mdx)```...` default value FindOne either an element of `R` or the symbol [FindOne](../Method/op_Coredollar_FindOne.mdx). An element is primitive if it generates the multiplicative group of non-zero elements of R
- [](../Instance/GF(....mdx)``an [ integer](../Type/ZZ.mdx) default value 10000 the limit on the size of a Galois field whose elements will be represented internally as powers of the primitive element
- Outputs:
- a [ Galois field](../Type/GaloisField.mdx) a finite field isomorphic to `R`



## Installed For

### GF([Ring](../Type/Ring.mdx))

### GF([ZZ](../Type/ZZ.mdx))

### GF([ZZ](../Type/ZZ.mdx), [ZZ](../Type/ZZ.mdx))

## Options

- [Strategy](../Option/Strategy.mdx) => null
- [Variable](../Option/Variable.mdx) => null
- [PrimitiveElement](../Method/PrimitiveElement.mdx) => FindOne
- [SizeLimit](../Method/SizeLimit.mdx) => 10000

## Examples

```macaulay2
A = GF(32
```

```macaulay2
K = GF 8

```

```text
K

GaloisField
```

```macaulay2
B = GF A

```

```text
B

GaloisField
```

```macaulay2
D = GF C

```

```text
D

GaloisField
```

## Additional Information

### See also


- Macaulay2Doc:: toField
- Macaulay2Doc:: finite fields

## Debug info

```python
{
    "obj_type": "Method",
    "name": "GF",
    "safe_name": "op_Coredollar_GF",
    "type": "MethodFunctionWithOptions",
    "description": "\n### Synopsis\n\n\n- Inputs:\n- `p` a prime number\n- `n`\n- [](../Instance/GF(....mdx)``a [ symbol](../Ty...",
    "installations": [
        "<Installation GF(ZZ, ZZ)>",
        "<Installation GF(ZZ)>",
        "<Installation GF(Ring)>"
    ],
    "options": [
        {
            "name": "Strategy",
            "default": "null"
        },
        {
            "name": "Variable",
            "default": "null"
        },
        {
            "name": "PrimitiveElement",
            "default": "FindOne"
        },
        {
            "name": "SizeLimit",
            "default": "10000"
        }
    ],
    "extra": {
        "isSymbol": false,
        "codeName": "Core$GF",
        "full_doc": "... (3873 bytes) ...",
        "parsed_headline": "GF  -- make a finite field"
    }
}
```
