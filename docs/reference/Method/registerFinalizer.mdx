---
title: "registerFinalizer"
kind: Method
---

# registerFinalizer

**registerFinalizer  -- register a string that will be displayed when an object is garbage collected**

**Type:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

registerFinalizer  -- register a string that will be displayed when an object is garbage collected

## Installed For

### registerFinalizer([Thing](../Type/Thing.mdx), [String](../Type/String.mdx))

## Examples

```macaulay2
registerFinalizer(xstr)
```

```macaulay2
for i from 1 to 9 do (x := 0 .. 10000 ; registerFinalizer(x "-- finalizing sequence #"|i|" --"))
```

## Additional Information

- Inputs:
- `x` a [ thing](../Type/Thing.mdx)
- `str` a [ string](../Type/op_Coredollar_String.mdx)
- Consequences:
- A finalizer is registered with the garbage collector to print a string when that object is collected as garbage


### Caveat

 This function should mainly be used for debugging.  Having a large number of finalizers might degrade the performance of the program.  Moreover registering two or more objects that are members of a circular chain of pointers for finalization will result in a memory leak with none of the objects in the chain being freed even if nothing else points to any of them.
### See also


- Macaulay2Doc:: collectGarbage

## Debug info

```python
{
    "obj_type": "Method",
    "name": "registerFinalizer",
    "safe_name": "op_Coredollar_registerFinalizer",
    "type": "MethodFunction",
    "description": "registerFinalizer  -- register a string that will be displayed when an object is garbage collected...",
    "installations": [
        "<Installation registerFinalizer(Thing, String)>"
    ],
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$registerFinalizer",
        "full_doc": "... (2173 bytes) ...",
        "parsed_headline": "registerFinalizer  -- register a string that will be displayed when an object is garbage collected"
    }
}
```
