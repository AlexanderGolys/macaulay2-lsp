---
title: "apropos"
kind: Method
---

# apropos

**apropos  -- symbols matching a pattern**

**Type:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

In the simplest case the list of symbols containing the given string is returned.[ Regular expressions](../Instance/regular expressions.mdx) allow for more complicated requests. For example to find all functions that start with `mat` or `Mat`:It is also possible to view a table of headlines corresponding to the results.

## Installed For

### apropos([String](../Type/String.mdx)) -> [List](../Type/List.mdx)

## Examples

```macaulay2
apropos pattern
```

```macaulay2
apropos "atrix"

```

```text
{blockMatrixForm, ChangeMatrix, compactMatrixForm, diagonalMatrix,
     ------------------------------------------------------------------------
     fillMatrix, genericMatrix, genericSkewMatrix, genericSymmetricMatrix,
     ------------------------------------------------------------------------
     getChangeMatrix, Matrix, matrix, MatrixExpression, MinimalMatrix,
     ------------------------------------------------------------------------
     MutableMatrix, mutableMatrix, randomMutableMatrix, submatrix,
     ------------------------------------------------------------------------
     submatrix', submatrixByDegrees, sylvesterMatrix, SyzygyMatrix,
     ------------------------------------------------------------------------
     vertexEdgeMatrix, vertexFacetMatrix}

List
```

```macaulay2
apropos "^[mM]at"

```

```text
{match, mathML, Matrix, matrix, MatrixExpression}

List
```

```macaulay2
headlines apropos "hilbert"

```

```text
0.  "Polyhedra :: hilbertBasis"           -- computes the Hilbert basis of a Cone
     1.  "Macaulay2Doc :: hilbertFunction"     -- the Hilbert function                
     2.  "Macaulay2Doc :: hilbertPolynomial"   -- compute the Hilbert polynomial      
     3.  "Macaulay2Doc :: hilbertSeries"       -- compute the Hilbert series
```

## Additional Information

- Inputs:
- `pattern` a [ string](../Type/op_Coredollar_String.mdx) a regular expression pattern to match
- Outputs:
- a [ list](../Type/op_Coredollar_List.mdx) of global symbols matching the given pattern


### See also


- Macaulay2Doc:: help
- Macaulay2Doc:: about
- Macaulay2Doc:: headlines
- Macaulay2Doc:: findSynonyms
- Macaulay2Doc:: regular expressions

## Debug info

```python
{
    "obj_type": "Method",
    "name": "apropos",
    "safe_name": "op_Coredollar_apropos",
    "type": "MethodFunction",
    "description": "In the simplest case the list of symbols containing the given string is returned.[ Regular expressio...",
    "installations": [
        "<Installation apropos(String)>"
    ],
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$apropos",
        "full_doc": "... (2970 bytes) ...",
        "parsed_headline": "apropos  -- symbols matching a pattern"
    }
}
```
