---
title: "eigenvectors"
kind: Method
---

# eigenvectors

**eigenvectors  -- find eigenvectors of a matrix over real or complex numbers**

**Type:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

 The resulting matrix is over [CC](../Type/op_x226_x132_x130.mdx)  and contains the eigenvectors of `M` .  The LAPACK and Eigen libraries are used to compute eigenvectors of real and complex matrices. Recall that if `v`  is a non-zero vector such that `Mv = av`  for a scalar a then `v`  is called an eigenvector corresponding to the eigenvalue `a` . If the matrix is symmetric (over [RR](../Type/op_x226_x132_x157.mdx) ) or Hermitian (over [CC](../Type/op_x226_x132_x130.mdx) ) this information should be provided as an optional argument `` .  In this case the resulting eigenvalues will be returned as real numbers and if `M`  is real the matrix of eigenvectors will be real.

## Installed For

### eigenvectors([Matrix](../Type/Matrix.mdx)) -> [VerticalList](../Type/VerticalList.mdx)

### eigenvectors([MutableMatrix](../Type/MutableMatrix.mdx))

## Options

- [Hermitian](../Method/Hermitian.mdx) => false

## Examples

```macaulay2
(eigvals eigvecs) = eigenvectors M
```

```macaulay2
eigenvectors M

```

```text
({-.358899} | -.827138 -.262266 |)
      {8.3589  }  | .561999  -.964996 |

Sequence
```

```macaulay2
(ev) = eigenvectors(M | -.707107 .707107 |)
      {3 }  | .707107  .707107 |

```

```text
Sequence
```

## Additional Information

- Inputs:
- `M` a [ matrix](../Type/op_Coredollar_Matrix.mdx) or a [MutableMatrix](../Type/op_Coredollar_MutableMatrix.mdx) over [RR](../Type/op_x226_x132_x157.mdx) or [CC](../Type/op_x226_x132_x130.mdx) which is a square n by n matrix
- [ Optional inputs](../Instance/using functions with optional inputs.mdx):
- [](../Instance/eigenvectors(....mdx)```...` default value false whether to assume the matrix is symmetric or Hermitian
- Outputs:
- `eigvals` a [ vertical list](../Type/VerticalList.mdx) a list of the eigenvalues of `M`
- `eigvecs` a [ matrix](../Type/op_Coredollar_Matrix.mdx) or a [ mutable matrix](../Type/op_Coredollar_MutableMatrix.mdx) if `M` is one) whose columns are the corresponding eigenvectors of `M`


### Caveat

 The eigenvectors are approximate.
### See also


- Macaulay2Doc:: eigenvalues
- Macaulay2Doc:: SVD

[eigenvectors(...](../Instance/eigenvectors(....mdx)

## Debug info

```python
{
    "obj_type": "Method",
    "name": "eigenvectors",
    "safe_name": "eigenvectors",
    "type": "MethodFunctionWithOptions",
    "description": " The resulting matrix is over [CC](../Type/op_x226_x132_x130.mdx)  and contains the eigenvectors of ...",
    "installations": [
        "<Installation eigenvectors(MutableMatrix)>",
        "<Installation eigenvectors(Matrix)>"
    ],
    "options": [
        {
            "name": "Hermitian",
            "default": "false"
        }
    ],
    "extra": {
        "isSymbol": false,
        "codeName": "eigenvectors",
        "full_doc": "... (3535 bytes) ...",
        "parsed_headline": "eigenvectors  -- find eigenvectors of a matrix over real or complex numbers"
    }
}
```
