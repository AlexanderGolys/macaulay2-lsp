---
title: "fold"
kind: Method
---

# fold

**fold  -- apply a binary operator repeatedly**

**Type:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

Suppose `L=\{x0, x1, ..., xn\}` . Then for any binary operator `f`  `fold(f L)`  computes `f(...f(f(x0 x1) x2) ...)` . In other words the binary operator is applied to the first two elements of `L`  then to that result along with the next unused element of `L`  and so forth.If `fold(f x L)`  is called the element `x`  is used as the first argument of the binary function `f` . In other words `fold(f \{x0, x1, \ldots, xn\})`  is equivalent to `fold(f x0 \{x1, \ldots, xn\})` .The function `fold(\{x_0, x_1, \ldots, x_n\} f)`  returns `f...f(f(x_{n-2} f(x_{n-1} x_n)))` . That is `f`  is applied to the last two elements of the list first then the repeated calls to `f`  proceed backwards through the list. The optional argument `x`  in `fold(L x f)`  is used as the second argument in the first evaluation of `f` . So `fold(\{x_0, x_1, \ldots, x_{n-1}\} x_n f)`  is equivalent to `fold(\{x_0, x_1, \ldots, x_n\} f)` .If `L` is an instance of class the the [iterator](../Method/iterator.mdx) method installed e.g. a string then it may also be used with `fold` but only the versions with `f` as the first argument.The difference between [fold](../Method/op_Coredollar_fold.mdx) and `accumulate`  is that `fold`  returns the final result of all the nested evaluations of `f`  while `accumulate`  lists all the intermediate values as well.

## Installed For

### fold([Function](../Type/Function.mdx), [Thing](../Type/Thing.mdx))

### fold([Function](../Type/Function.mdx), [Thing](../Type/Thing.mdx), [Thing](../Type/Thing.mdx))

### fold([Function](../Type/Function.mdx), [VisibleList](../Type/VisibleList.mdx))

### fold([VisibleList](../Type/VisibleList.mdx), [Function](../Type/Function.mdx))

### fold([VisibleList](../Type/VisibleList.mdx), [Thing](../Type/Thing.mdx), [Function](../Type/Function.mdx))

## Examples

```macaulay2
fold(f x L)
fold(f L)
fold(L x f)
fold(L f)
```

```macaulay2
fold(plus {0,1,2,3,4,5})

```

```text
15
```

```macaulay2
fold(identity {a,b,c,d,e})

```

```text
((((a b) c) d) e)

Sequence
```

```macaulay2
fold((ij) -> i|j|i {"a","b","c","d","e"})

```

```text
abacabadabacabaeabacabadabacaba
```

```macaulay2
fold(plus 0 {1,2,3,4,5})

```

```text
15
```

```macaulay2
fold((x y) -> x^y 2 {3,2,1,2})

```

```text
4096
```

```macaulay2
fold({a,b,c,d,e} identity)

```

```text
(a (b (c (d e))))

Sequence
```

```macaulay2
fold({a,b,c,d} e identity)

```

```text
(a (b (c (d e))))

Sequence
```

```macaulay2
fold({2,3,2,1} 2 (x y) -> x^y)

```

```text
512
```

```macaulay2
fold(identity "abcde")

```

```text
((((a b) c) d) e)

Sequence
```

```macaulay2
fold(identity "a" "bcde")

```

```text
((((a b) c) d) e)

Sequence
```

## Additional Information

- Inputs:
- `f` a [ function](../Type/Function.mdx)
- `x` a [ thing](../Type/Thing.mdx)
- `L` a [ visible list](../Type/VisibleList.mdx) or an instance of a class with the [iterator](../Method/iterator.mdx) method installed
- Outputs:
- `M` a [ list](../Type/op_Coredollar_List.mdx)


### See also


- Macaulay2Doc:: apply
- Macaulay2Doc:: accumulate
- Macaulay2Doc:: lists and sequences

## Debug info

```python
{
    "obj_type": "Method",
    "name": "fold",
    "safe_name": "op_Coredollar_fold",
    "type": "MethodFunction",
    "description": "Suppose `L=\\{x0, x1, ..., xn\\}` . Then for any binary operator `f`  `fold(f L)`  computes `f(...f(f(...",
    "installations": [
        "<Installation fold(VisibleList, Thing, Function)>",
        "<Installation fold(Function, VisibleList)>",
        "<Installation fold(VisibleList, Function)>",
        "<Installation fold(Function, Thing)>",
        "<Installation fold(Function, Thing, Thing)>"
    ],
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$fold",
        "full_doc": "... (4459 bytes) ...",
        "parsed_headline": "fold  -- apply a binary operator repeatedly"
    }
}
```
