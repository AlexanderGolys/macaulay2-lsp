---
title: "pseudoRemainder"
kind: Method
---

# pseudoRemainder

**pseudoRemainder  -- compute the pseudo-remainder**

**Type:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

 Let `x`  be the first variable of `R`  appearing in `g` .  Suppose that `g`  has degree `d`  in `x`  and that the coefficient of `x^d`  in `g`  (as an element of `R`  but not involving the variable `x` ) is `c` . The pseudo remainder of `f`  by `g`  is the polynomial `h`  of degree less than `d`  in `x`  such that `c^(e-d+1) * f = q*g + h`  where `f`  has degree `e`  in `x` .

## Installed For

### pseudoRemainder([RingElement](../Type/RingElement.mdx), [RingElement](../Type/RingElement.mdx)) -> [RingElement](../Type/RingElement.mdx)

## Examples

```macaulay2
pseudoRemainder(fg)
```

```macaulay2
h = pseudoRemainder(fg)

            6        4       3      4        2            2
```

```text
- 27x*y  + 87x*y  - 2x*y  + 14y  - 27x*y  + 6x*y - 6y  + 27x + y + 9

R
```

## Additional Information

- Inputs:
- `f` a [ ring element](../Type/RingElement.mdx)
- `g` a [ ring element](../Type/RingElement.mdx) in the same polynomial ring `R` as `f`
- Outputs:
- a [ ring element](../Type/RingElement.mdx) the pseudo remainder of the polynomial `f` by the polynomial `g`


### Caveat

 There is no pseudo-division implemented and the only way to change the notion of what the top variable is is to change to a ring where the variables are in a different order
### See also


- Macaulay2Doc:: topCoefficients

## Debug info

```python
{
    "obj_type": "Method",
    "name": "pseudoRemainder",
    "safe_name": "pseudoRemainder",
    "type": "MethodFunction",
    "description": " Let `x`  be the first variable of `R`  appearing in `g` .  Suppose that `g`  has degree `d`  in `x`...",
    "installations": [
        "<Installation pseudoRemainder(RingElement, RingElement)>"
    ],
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "pseudoRemainder",
        "full_doc": "... (2981 bytes) ...",
        "parsed_headline": "pseudoRemainder  -- compute the pseudo-remainder"
    }
}
```
