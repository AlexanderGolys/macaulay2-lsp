---
title: "about"
kind: Method
---

# about

**about  -- search the documentation**

**Type:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

The documentation corresponding to the keys in the list returned can be displayed by applying the function [help](../Instance/op_Coredollar_help.mdx) to it. To see the documentation corresponding to just one or some of the keys give [help](../Instance/op_Coredollar_help.mdx) an integer or a list of integers to be used as indices in the list returned by the most recent application of [about](../Method/op_Coredollar_about.mdx). The functions [viewHelp](../Instance/viewHelp.mdx) and [infoHelp](../Instance/infoHelp.mdx) can also be given an integer for viewing the documentation.The packages searched are the loaded packages and the packages installed under one of the prefixes listed in [prefixPath](../Instance/prefixPath.mdx). The first search will take a few seconds while it reads all the documentation keys into memory.It is also possible to view a table of headlines corresponding to the results.

## Installed For

### about([Function](../Type/Function.mdx))

### about([ScriptedFunctor](../Type/ScriptedFunctor.mdx))

### about([String](../Type/String.mdx)) -> [NumberedVerticalList](../Type/NumberedVerticalList.mdx)

### about([Symbol](../Type/Symbol.mdx))

### about([Type](../Type/Type.mdx))

## Options

- [Body](../Method/Body.mdx) => false

## Examples

```macaulay2
about s
```

```macaulay2
headlines about firstFunction

```

```text
0.  "FirstPackage :: firstFunction"       -- a silly first function
     1.  "FirstPackage :: firstFunction(ZZ)"   -- a silly first function
```

## Additional Information

- Inputs:
- `s` a [ string](../Type/op_Coredollar_String.mdx) a [ function](../Type/Function.mdx) a [ symbol](../Type/op_Coredollar_Symbol.mdx) or a [ type](../Type/op_Coredollar_Type.mdx)
- [ Optional inputs](../Instance/using functions with optional inputs.mdx):
- `Body```a [ Boolean value](../Type/op_Coredollar_Boolean.mdx) default value false whether also to search the bodies of the documentation nodes. By default just their keys are searched.
- Outputs:
- a [ numbered vertical list](../Type/NumberedVerticalList.mdx) a list of documentation node keys matching the regular expression in the string `s` if `s` is a string. Otherwise the search matches against the name of `s` as a complete word.


### Caveat

Since `s` is taken as a regular expression parentheses serve for grouping subexpressions rather than matching themselves.
### See also


- Macaulay2Doc:: ? Symbol
- Macaulay2Doc:: apropos
- Macaulay2Doc:: headlines
- Macaulay2Doc:: findSynonyms
- Macaulay2Doc:: regular expressions
- Macaulay2Doc:: examples(ZZ)

## Debug info

```python
{
    "obj_type": "Method",
    "name": "about",
    "safe_name": "op_Coredollar_about",
    "type": "MethodFunctionWithOptions",
    "description": "The documentation corresponding to the keys in the list returned can be displayed by applying the fu...",
    "installations": [
        "<Installation about(ScriptedFunctor)>",
        "<Installation about(Symbol)>",
        "<Installation about(Type)>",
        "<Installation about(Function)>",
        "<Installation about(String)>"
    ],
    "options": [
        {
            "name": "Body",
            "default": "false"
        }
    ],
    "extra": {
        "isSymbol": false,
        "codeName": "Core$about",
        "full_doc": "... (4260 bytes) ...",
        "parsed_headline": "about  -- search the documentation"
    }
}
```
