---
title: "submatrix"
kind: Method
---

# submatrix

**submatrix  -- select part of a matrix**

**Type:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

 Yields an r by c matrix where r is the length of the list of integers `rows`  and c is the length of the list of integers `cols` . The `(ij)` -th entry of the result is `f_(rows_i cols_j)` . Each list of integers may contain ranges and repetitions as in `{3, 5..7, 3:0}` If `rows`  or `cols`  is omitted all the corresponding indices are used. Both `rows`  and/or `cols`  may contain duplicate values in which case the result will contain duplicate rows and/or columns.`submatrix(fcols)`  can be written as `submatrix(fcols)`  or `f_cols` .

## Installed For

### submatrix([Matrix](../Type/Matrix.mdx), [Nothing](../Type/Nothing.mdx), [Nothing](../Type/Nothing.mdx))

### submatrix([Matrix](../Type/Matrix.mdx), [Nothing](../Type/Nothing.mdx), [VisibleList](../Type/VisibleList.mdx))

### submatrix([Matrix](../Type/Matrix.mdx), [VisibleList](../Type/VisibleList.mdx))

### submatrix([Matrix](../Type/Matrix.mdx), [VisibleList](../Type/VisibleList.mdx), [Nothing](../Type/Nothing.mdx))

### submatrix([Matrix](../Type/Matrix.mdx), [VisibleList](../Type/VisibleList.mdx), [VisibleList](../Type/VisibleList.mdx))

### submatrix([MutableMatrix](../Type/MutableMatrix.mdx), [Nothing](../Type/Nothing.mdx), [Nothing](../Type/Nothing.mdx))

### submatrix([MutableMatrix](../Type/MutableMatrix.mdx), [Nothing](../Type/Nothing.mdx), [VisibleList](../Type/VisibleList.mdx))

### submatrix([MutableMatrix](../Type/MutableMatrix.mdx), [VisibleList](../Type/VisibleList.mdx))

### submatrix([MutableMatrix](../Type/MutableMatrix.mdx), [VisibleList](../Type/VisibleList.mdx), [Nothing](../Type/Nothing.mdx))

### submatrix([MutableMatrix](../Type/MutableMatrix.mdx), [VisibleList](../Type/VisibleList.mdx), [VisibleList](../Type/VisibleList.mdx))

## Examples

```macaulay2
submatrix(f rows cols)
submatrix(fcols)
submatrix(frows)
```

```macaulay2
submatrix(f {1,2,0} {0..2, 4})

```

```text
| b e h n |
     | c f i o |
     | a d g m |

             3      4
Matrix R  <-- R
```

```macaulay2
submatrix(f {1,2} )

```

```text
| b e h k n |
     | c f i l o |

             2      5
Matrix R  <-- R
```

```macaulay2
submatrix(f{0,1})

```

```text
| a d |
     | b e |
     | c f |

             3      2
Matrix R  <-- R
```

## Additional Information

- Inputs:
- `f` a map between free modules
- `rows` a list of integers denoting the choice of rows. If omitted use all rows
- `cols` a list of integers denoting the choice of columns. If omitted use all columns
- Outputs:
- a [ matrix](../Type/op_Coredollar_Matrix.mdx) the submatrix of `f` corresponding to the lists `rows` and `cols`


### See also


- Macaulay2Doc:: submatrix'
- Macaulay2Doc:: Matrix _ List
- Macaulay2Doc:: Matrix ^ List
- Macaulay2Doc:: submatrix(MatrixVisibleList)
- Macaulay2Doc:: Matrix _ Array
- Macaulay2Doc:: Matrix ^ Array

## Debug info

```python
{
    "obj_type": "Method",
    "name": "submatrix",
    "safe_name": "op_Coredollar_submatrix",
    "type": "MethodFunction",
    "description": " Yields an r by c matrix where r is the length of the list of integers `rows`  and c is the length o...",
    "installations": [
        "<Installation submatrix(Matrix, VisibleList, VisibleList)>",
        "<Installation submatrix(Matrix, VisibleList, Nothing)>",
        "<Installation submatrix(Matrix, VisibleList)>",
        "<Installation submatrix(Matrix, Nothing, VisibleList)>",
        "<Installation submatrix(Matrix, Nothing, Nothing)>",
        "<Installation submatrix(MutableMatrix, VisibleList, VisibleList)>",
        "<Installation submatrix(MutableMatrix, VisibleList)>",
        "<Installation submatrix(MutableMatrix, Nothing, VisibleList)>",
        "<Installation submatrix(MutableMatrix, VisibleList, Nothing)>",
        "<Installation submatrix(MutableMatrix, Nothing, Nothing)>"
    ],
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$submatrix",
        "full_doc": "... (3312 bytes) ...",
        "parsed_headline": "submatrix  -- select part of a matrix"
    }
}
```
