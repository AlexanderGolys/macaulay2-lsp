---
title: "irreducibleCharacteristicSeries"
kind: Method
---

# irreducibleCharacteristicSeries

**irreducibleCharacteristicSeries  -- irreducible characteristic series of an ideal**

**Type:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

 As we see in the example below an irreducible characteristic series for $I$ consists of a collection of triangular sets. Here given a polynomial $f$  write $lvar(f)$ for the largest variable appearing in $f$ (with respect to the lexicographic order). In the example $lvar(-y w+x^2) = y$ . A triangular set consists of polynomials $f_1\dotsf_r$ such that $lvar(f_1)< \dots < lvar(f_r)$. In the example $lvar(-x*y^2+z^3) = x < w = lvar(-w*y+z^2)$ . If $T_1\dotsT_s$ form an irreducible characteristic series for $I$  and if $J_i$ is the ideal generated by the largest variables of the elements of $T_i$  then the algebraic set $V(I)$ defined by $I$ is the union of the sets $V(T_i) \setminus V(I_i)$ for $i=1\dotss$. The minimal associated primes of $I$ can thus be recovered from the irreducible characteristic series by saturation and by throwing away superfluous primes.  This is done by [minimalPrimes](../Instance/minimalPrimes.mdx)  which uses this routine.

## Installed For

### irreducibleCharacteristicSeries([Ideal](../Type/Ideal.mdx)) -> [List](../Type/List.mdx)

## Examples

```macaulay2
(icsp) = irreducibleCharacteristicSeries I
```

```macaulay2
(Lp) = irreducibleCharacteristicSeries ideal(x^2-y*wx^3-z*w^2)

```

```text
({| zw2-x3 yw-x2 |, | x w |} map (R QQ[y..z w..x] {y, z, w, x}))

Sequence
```

## Additional Information

- Inputs:
- `I` an [ ideal](../Type/op_Coredollar_Ideal.mdx)
- Outputs:
- `ics` a [ list](../Type/op_Coredollar_List.mdx) a list of matrices representing an irreducible characteristic series for `I`
- `p` a [ ring map](../Type/op_Coredollar_RingMap.mdx) an isomorphism from the ring of `I` to the ring of the characteristic series. The ring retains the names and degrees of the variables but reorders the variables and uses a default monomial ordering.


### See also


- MinimalPrimes:: minimalPrimes

## Debug info

```python
{
    "obj_type": "Method",
    "name": "irreducibleCharacteristicSeries",
    "safe_name": "op_Coredollar_irreducibleCharacteristicSeries",
    "type": "MethodFunction",
    "description": " As we see in the example below an irreducible characteristic series for $I$ consists of a collectio...",
    "installations": [
        "<Installation irreducibleCharacteristicSeries(Ideal)>"
    ],
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "Core$irreducibleCharacteristicSeries",
        "full_doc": "... (2895 bytes) ...",
        "parsed_headline": "irreducibleCharacteristicSeries  -- irreducible characteristic series of an ideal"
    }
}
```
