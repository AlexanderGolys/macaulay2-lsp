---
title: "GRevLex"
kind: Method
---

# GRevLex

**GRevLex  -- graded reverse lexicographical monomial ordering**

**Type:** [Symbol](../Type/Symbol.mdx)

## Description

 The graded reverse lexicographic order is defined by: $x^A > x^B$ if either $degree(x^A) > degree(x^B)$ or $degree(x^A) = degree(x^B)$ and the LAST non-zero entry of the vector of integers $A-B$ is NEGATIVE.  This is the default order in Macaulay2 in large part because it is often the most efficient order for use with Gröbner bases.  By giving GRevLex a list of integers one may change the definition of the order: $degree(x^A)$ is the dot product of $A$ with the argument of GRevLex. The largest possible exponent of variables in the `GRevLex`  order is 2^31-1.  For efficiency reasons it is sometimes useful to limit the size of monomials (this often makes computations more efficient). Use ``  which allows maximal exponent 2^15-1 or ``  which allows maximal exponent 2^7-1.

## Additional Information

### See also


- Macaulay2Doc:: packing monomials for efficiency

## Debug info

```python
{
    "obj_type": "Method",
    "name": "GRevLex",
    "safe_name": "op_Coredollar_GRevLex",
    "type": "Symbol",
    "description": " The graded reverse lexicographic order is defined by: $x^A > x^B$ if either $degree(x^A) > degree(x...",
    "installations": [],
    "options": null,
    "extra": {
        "isSymbol": true,
        "codeName": "Core$GRevLex",
        "full_doc": "... (2375 bytes) ...",
        "stub": true,
        "parsed_headline": "GRevLex  -- graded reverse lexicographical monomial ordering"
    }
}
```
