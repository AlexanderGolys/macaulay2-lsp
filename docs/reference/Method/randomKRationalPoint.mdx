---
title: "randomKRationalPoint"
kind: Method
---

# randomKRationalPoint

**randomKRationalPoint  -- pick a random K rational point on the scheme X defined by I**

**Type:** [MethodFunction](../Type/MethodFunction.mdx)

## Description

If X has codimension 1 then we intersect X with a randomly chosen line and hope that the decomposition of the intersection contains a K-rational point. If n=degree X then the probability P that this happens is the proportion  of permutations in $S_n$ with a fix point on $\{1,\ldots,n \}$ i.e. $$P=\sum_{j=1}^n (-1)^{j-1} binomial(nj)(n-j)!/n! = 1-1/2+1/3! + \ldots $$ which approaches $1-exp(-1) = 0.63\ldots$. Thus a probabilistic approach works.For higher codimension we first project X birationally onto a hypersurface Y and find a point on Y. Then we take the preimage of this point.The claim that $63 \%$ of the intersections contain a K-rational point can be experimentally tested:

## Installed For

### randomKRationalPoint([Ideal](../Type/Ideal.mdx)) -> [Ideal](../Type/Ideal.mdx)

## Examples

```macaulay2
randomKRationalPoint I
```

```macaulay2
time randomKRationalPoint(I)
 -- used 0.102829s (cpu); 0.0584514s (thread); 0s (gc)

```

```text
ideal (x  - 53x  x  + 8x  x  - 4x )
             2      3   1     3   0     3

Ideal of R
```

```macaulay2
time randomKRationalPoint(I)
 -- used 0.44476s (cpu); 0.166767s (thread); 0s (gc)

```

```text
ideal (x  - 27x  x  - 16x  x  - 9x  x  + 44x  x  - 52x )
              4      5   3      5   2     5   1      5   0      5

Ideal of R
```

## Additional Information

- Inputs:
- `I` an [ ideal](../Type/op_Coredollar_Ideal.mdx) in a polynomial ring over a finite ground field K
- Outputs:
- an [ ideal](../Type/op_Coredollar_Ideal.mdx) of a K-rational point on V(I)

## Debug info

```python
{
    "obj_type": "Method",
    "name": "randomKRationalPoint",
    "safe_name": "randomKRationalPoint",
    "type": "MethodFunction",
    "description": "If X has codimension 1 then we intersect X with a randomly chosen line and hope that the decompositi...",
    "installations": [
        "<Installation randomKRationalPoint(Ideal)>"
    ],
    "options": null,
    "extra": {
        "isSymbol": false,
        "codeName": "randomKRationalPoint",
        "full_doc": "... (3543 bytes) ...",
        "parsed_headline": "randomKRationalPoint  -- pick a random K rational point on the scheme X defined by I"
    }
}
```
