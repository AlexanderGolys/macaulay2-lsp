---
title: "PairLimit"
kind: Method
---

# PairLimit

**PairLimit  -- an optional argument**

**Type:** [Symbol](../Type/Symbol.mdx)

## Description

 A symbol used as the name of an optional argument.

## Additional Information

### Functions with optional argument named `PairLimit`:


- gb(... -- see Macaulay2Doc:: gb
- intersectInP(... -- see ReesAlgebra:: intersectInP(...
- pushForward(... -- see Macaulay2Doc:: pushForward(RingMapModule)
- quotient(... -- see Saturation:: quotient(ModuleModule)
- analyticSpread(... -- see ReesAlgebra:: reesIdeal(...
- associatedGradedRing(... -- see ReesAlgebra:: reesIdeal(...
- distinguished(... -- see ReesAlgebra:: reesIdeal(...
- isLinearType(... -- see ReesAlgebra:: reesIdeal(...
- isReduction(... -- see ReesAlgebra:: reesIdeal(...
- minimalReduction(... -- see ReesAlgebra:: reesIdeal(...
- multiplicity(... -- see ReesAlgebra:: reesIdeal(...
- normalCone(Ideal -- see ReesAlgebra:: reesIdeal(...
- normalCone(IdealRingElement -- see ReesAlgebra:: reesIdeal(...
- reesAlgebra(... -- see ReesAlgebra:: reesIdeal(...
- ReesAlgebra:: reesIdeal(...
- specialFiber(... -- see ReesAlgebra:: reesIdeal(...
- specialFiberIdeal(... -- see ReesAlgebra:: reesIdeal(...
- OldChainComplexes:: resolution(...
- saturate(... -- see Saturation:: saturate
- syz(... -- see Macaulay2Doc:: syz(Matrix)

## Debug info

```python
{
    "obj_type": "Method",
    "name": "PairLimit",
    "safe_name": "op_Coredollar_PairLimit",
    "type": "Symbol",
    "description": " A symbol used as the name of an optional argument....",
    "installations": [],
    "options": null,
    "extra": {
        "isSymbol": true,
        "codeName": "Core$PairLimit",
        "full_doc": "... (2517 bytes) ...",
        "stub": true,
        "parsed_headline": "PairLimit  -- an optional argument"
    }
}
```
