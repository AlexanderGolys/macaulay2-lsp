---
title: "netList"
kind: Method
---

# netList

**netList  -- a table of boxes**

**Type:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

netList  -- a table of boxes

## Installed For

### netList([VisibleList](../Type/VisibleList.mdx))

## Options

- [Alignment](../Method/Alignment.mdx) => Left
- [VerticalSpace](../Method/VerticalSpace.mdx) => 0
- [Boxes](../Option/Boxes.mdx) => true
- [BaseRow](../Method/BaseRow.mdx) => 0
- [HorizontalSpace](../Method/HorizontalSpace.mdx) => 0

## Examples

```macaulay2
netList v
```

```macaulay2
netList f

     +--------+-------------+
```

```text
|hi there|foo          |
     +--------+-------------+
     |-3      |1099511627776|
     +--------+-------------+
```

```macaulay2
netList(f
```

```macaulay2
netList(f
```

```macaulay2
netList(f
```

```macaulay2
netList(f
```

```macaulay2
netList(f
```

```macaulay2
netList apply(5i->apply(i+1j->(ij)))

     +------+------+------+------+------+
```

```text
|(0 0)|      |      |      |      |
     +------+------+------+------+------+
     |(1 0)|(1 1)|      |      |      |
     +------+------+------+------+------+
     |(2 0)|(2 1)|(2 2)|      |      |
     +------+------+------+------+------+
     |(3 0)|(3 1)|(3 2)|(3 3)|      |
     +------+------+------+------+------+
     |(4 0)|(4 1)|(4 2)|(4 3)|(4 4)|
     +------+------+------+------+------+
```

```macaulay2
netList(apply(5i->apply(i+1j->(ij))) 0)                        
     ------------------------------
     (1 0)(1 1)                  
     ------------------------------
     (2 0)(2 1)(2 2)            
     ------------------------------
     (3 0)(3 1)(3 2)(3 3)      
     ------------------------------
     (4 0)(4 1)(4 2)(4 3)(4 4)
     ------------------------------
```

## Additional Information

- Inputs:
- `v` a list of lists of things to be converted to nets and displayed as a table in a net
- [ Optional inputs](../Instance/using functions with optional inputs.mdx):
- `Boxes````...` default value true whether to draw boxes around the individual nets. Can be a Boolean or a pair controlling separately the horizontal and vertical lines of the boxes. Each element of the pair is either a Boolean (draw all or none) or a list of rows/columns where lines are to inserted.
- `BaseRow```an [ integer](../Type/ZZ.mdx) default value 0 the index of the base row for the purpose of setting the baseline of the net produced. The value is allowed to be as large as the length of `v` larger by 1 than one might expect.
- `HorizontalSpace```an [ integer](../Type/ZZ.mdx) default value 0 the amount of space horizontally between entries or between entries and their enclosing boxes
- `VerticalSpace```an [ integer](../Type/ZZ.mdx) default value 0 the amount of space vertically between entries or between entries and their enclosing boxes
- `Alignment````...` default value Left `Center` `Left` `Right` or a list of those symbols indicating horizontal adjustment; if it's a list the `i` -th entry specifies the adjustment in the `i` -th column; if not the symbol applies to all columns.
- Outputs:
- a net obtained by converting the elements of each list in the list of lists `v` to nets and arranging them in a table as specified by the options


### See also


- Macaulay2Doc:: net

## Debug info

```python
{
    "obj_type": "Method",
    "name": "netList",
    "safe_name": "op_Coredollar_netList",
    "type": "MethodFunctionWithOptions",
    "description": "netList  -- a table of boxes...",
    "installations": [
        "<Installation netList(VisibleList)>"
    ],
    "options": [
        {
            "name": "Alignment",
            "default": "Left"
        },
        {
            "name": "VerticalSpace",
            "default": "0"
        },
        {
            "name": "Boxes",
            "default": "true"
        },
        {
            "name": "BaseRow",
            "default": "0"
        },
        {
            "name": "HorizontalSpace",
            "default": "0"
        }
    ],
    "extra": {
        "isSymbol": false,
        "codeName": "Core$netList",
        "full_doc": "... (5090 bytes) ...",
        "parsed_headline": "netList  -- a table of boxes"
    }
}
```
