---
title: "trim"
kind: Method
---

# trim

**trim  -- minimize generators and relations**

**Type:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

There are two ways to present an $R$-module $M$. One way is to take a free module $F$ (whose generators are called the [generators](../Method/gens.mdx)) and form the quotient $M = F/H$ by a submodule $H\subset F$ (whose generators are called the [relations](../Method/op_Coredollar_relations.mdx)).Another way is take a free module $F$ a submodule $G\subset F$ (whose generators are called the [generators](../Method/gens.mdx)) a submodule $H\subset F$ (whose generators are called the [relations](../Method/op_Coredollar_relations.mdx)) and form the [ subquotient module](../Instance/subquotient modules.mdx) $M = (G+H)/H$ obtained also as the image of $G$ in $F/H$.The purpose of `trim` is to minimize presentations of the latter type. This applies also to rings and ideals.

## Installed For

### trim([Ideal](../Type/Ideal.mdx))

### trim([Module](../Type/Module.mdx))

### trim([MonomialIdeal](../Type/MonomialIdeal.mdx))

### trim([QuotientRing](../Type/QuotientRing.mdx))

### trim([Ring](../Type/Ring.mdx))

## Options

- [Strategy](../Option/Strategy.mdx) => null

## Examples

```macaulay2
trim I

             2    2   2         2   2
```

```text
ideal (u  - w  y  x*y + z  x )

Ideal of R
```

```macaulay2
trim (R^1/I)

```

```text
cokernel | u2-w2 y2 xy+z2 x2 |

                            1
R-module quotient of R
```

```macaulay2
trim M

```

```text
subquotient (| 1 | | 10668 b+16001d a-10668c |)
                  | 0 |  | 1     0        0        |

                               2
R-module subquotient of R
```

## Additional Information

### See also


- Macaulay2Doc:: prune
- Macaulay2Doc:: mingens
- Macaulay2Doc:: subquotient modules
- Macaulay2Doc:: generators
- Macaulay2Doc:: relations
- Macaulay2Doc:: subquotient

## Debug info

```python
{
    "obj_type": "Method",
    "name": "trim",
    "safe_name": "op_Coredollar_trim",
    "type": "MethodFunctionWithOptions",
    "description": "There are two ways to present an $R$-module $M$. One way is to take a free module $F$ (whose generat...",
    "installations": [
        "<Installation trim(Ring)>",
        "<Installation trim(QuotientRing)>",
        "<Installation trim(Ideal)>",
        "<Installation trim(Module)>",
        "<Installation trim(MonomialIdeal)>"
    ],
    "options": [
        {
            "name": "Strategy",
            "default": "null"
        }
    ],
    "extra": {
        "isSymbol": false,
        "codeName": "Core$trim",
        "full_doc": "... (3075 bytes) ...",
        "parsed_headline": "trim  -- minimize generators and relations"
    }
}
```
