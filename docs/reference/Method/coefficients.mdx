---
title: "coefficients"
kind: Method
---

# coefficients

**coefficients  -- get the monomials and coefficients of a polynomial or matrix**

**Type:** [MethodFunctionWithOptions](../Type/MethodFunctionWithOptions.mdx)

## Description

 The resulting matrices have the following property. The Sylvester matrix of two generic quadratic forms: We may give the monomials directly.  This is useful if we are taking coefficients of several elements or matrices and need a consistent choice of monomials. If not all of the monomials are used no error is signaled but `M*C == P`  no longer holds.

## Installed For

### coefficients([Matrix](../Type/Matrix.mdx)) -> [Matrix](../Type/Matrix.mdx)

### coefficients([RingElement](../Type/RingElement.mdx))

## Options

- [Variables](../Option/Variables.mdx) => null
- [Monomials](../Method/Monomials.mdx) => null

## Examples

```macaulay2
(MC) = coefficients f
```

```macaulay2
(MC) = coefficients F

```

```text
(| x2 xy y2 | {2, 0} | a |)
                    {2, 0} | b |
                    {2, 0} | c |

Sequence
```

```macaulay2
(MC) = coefficients P

```

```text
(| x3 x2y xy2 y3 | {3, 0} | a 0 d 0 |)
                         {3, 0} | b a e d |
                         {3, 0} | c b f e |
                         {3, 0} | 0 c 0 f |

Sequence
```

```macaulay2
(MC) = coefficients(P {3, 0} | a 0 d 0 |)
                         {3, 0} | 0 c 0 f |
                         {3, 0} | b a e d |
                         {3, 0} | c b f e |

```

```text
Sequence
```

```macaulay2
(MC) = coefficients(P {3, 0} | a 0 d 0 |)
                  {3, 0} | 0 c 0 f |

```

```text
Sequence
```

## Additional Information

- Inputs:
- `f` a one-row [Matrix](../Type/op_Coredollar_Matrix.mdx) with `n` columns say or a [RingElement](../Type/RingElement.mdx) to be interpreted as a one-by-one matrix. (A future implementation will handle matrices with more than one row.)
- [ Optional inputs](../Instance/using functions with optional inputs.mdx):
- `Variables```a [ list](../Type/op_Coredollar_List.mdx) default value null a list `v` of variables. If a value for this option is not specified all of the (top-level) variables are used.
- `Monomials````...` default value null a list or one-row matrix of monomials each of which is formed using just variables in `v`.
- Outputs:
- `M` a [ matrix](../Type/op_Coredollar_Matrix.mdx) either the value of the `Monomials` option if specified (converted to a one-row matrix if necessary) or a one-row matrix of those monomials appearing in `f` that involve just variables in `v` in descending order. Let `m` denote the number of columns it has.
- `C` a [ matrix](../Type/op_Coredollar_Matrix.mdx) the `m` by `n` matrix `C` such that `C_(ij)` is the coefficient in `f_(0j)` of the monomial `M_(0i)`. In other words `C` is the unique matrix not involving the (specified) variables such that `M*C == f` unless a value was specified for the `Monomials` option that did not include all the monomials in the variables `v` used by `f`


### See also


- Macaulay2Doc:: monomials
- Macaulay2Doc:: content
- Macaulay2Doc:: exponents
- Macaulay2Doc:: listForm
- Macaulay2Doc:: standardForm

## Debug info

```python
{
    "obj_type": "Method",
    "name": "coefficients",
    "safe_name": "coefficients",
    "type": "MethodFunctionWithOptions",
    "description": " The resulting matrices have the following property. The Sylvester matrix of two generic quadratic f...",
    "installations": [
        "<Installation coefficients(RingElement)>",
        "<Installation coefficients(Matrix)>"
    ],
    "options": [
        {
            "name": "Variables",
            "default": "null"
        },
        {
            "name": "Monomials",
            "default": "null"
        }
    ],
    "extra": {
        "isSymbol": false,
        "codeName": "coefficients",
        "full_doc": "... (4749 bytes) ...",
        "parsed_headline": "coefficients  -- get the monomials and coefficients of a polynomial or matrix"
    }
}
```
